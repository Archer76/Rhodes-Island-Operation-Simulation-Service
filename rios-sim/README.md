# rios-sim

R.I.O.S. 的**第二份**战斗模拟器，Go 写的。原版是 `ak_tactic/battle/`（Python）。

## 为什么要另写一份

HS-EX-4 一次解算 **214 秒**——几千场完整战斗在纯 Python 里跑。原版已经做过就地展开
的优化（1.09–1.18×，见 `docs/tui-plan.md` 第十二节），**再榨只有 20%，要的是一个量级**。
微基准：2×10⁷ 次浮点循环，Python 2.49 秒 / Go 0.017 秒。

## 与原版的关系

博士 2026-09-18 的裁定：「保留原版项目结构不动另写一份新的模拟器」。

* `ak_tactic/battle/*` **一行不改**——它继续是三关基线（1-7 / SR-6 / SR-EX-8）与
  `tools/check_battle.py` 那 793 项自检的**权威实现**；
* 这一份是独立的：自带 `go.mod`、自己的构建、自己的二进制；
* **判决必须与它逐位对齐**才有资格接班。对拍台是这套东西的第一件产物，不是最后一件。

## 边界：Go 侧只跑战斗，不做取数

输入是**已经完全算好的数字**（干员最终属性、技能数值、敌人属性、波次时间轴、地图
可部署格），输出是判决与时间线。Go 侧**不读数据库、不联网、不做练度折算**——那条链
在 Python 里已经验证过，搬过来只会多一处会漂的实现。

## 协议：stdin/stdout 上的 JSON 行

一次进程、批量作业。选它而不是 Python 扩展模块，是为了免掉 FFI 构建链，并直接复用
`ak_tactic/parallel.py` 那套多进程并行。

```jsonc
// 请求（一行一个）
{"id": 1, "cmd": "ping"}
{"id": 2, "cmd": "sim", "spec": { /* 见下 */ }}

// 应答（一行一个，与请求同序）
{"id": 1, "ok": true, "pong": {"version": 1, "go": "go1.26.0", "spec_done": false}}
{"id": 2, "ok": true, "verdict": { /* … */ }}
```

* `ping` 用来在开跑之前核对**协议版本**。二进制与调用方版本不一致的症状是"判决微妙地
  对不上"，那是最难查的一类错，所以在门口挡住。
* 未实现或出错一律 `ok:false` + `error`，**绝不返回假判决**——调用方拿假判决当"跑完了"，
  对拍台就再也测不出东西。

### 规格里的 `unsupported`：拒跑的口子

最小版本覆盖 **常规关卡**（出怪、推进含分段计划、阻挡、我方平A、敌方平A、费用门、
漏怪结算）。用到别的机制时，**Python 侧把原因逐条写进 `spec.unsupported`，Go 侧见到
就拒跑**：`{"ok": false, "error": "这一局用到了最小版本还没覆盖的机制，不跑：[…]"}`。

这条口径是有代价的（NS-EX 那类关卡一场都跑不了），但反过来更坏——**缺了机制的"对齐"
会把 bug 固化成基线**。所以每加一条机制，就补一组对拍用例，再放宽一次这里。

对拍台有两个**必须带证据**才能打开的口子（`build_spec(allow_devices=…,
allow_skills=…)`），打开之前要先量：

* `allow_devices` —— 把装置摘掉重跑一遍，结果一字不变（1-7 那个 `trap_002_emp`
  就是这么做实出来的）；
* `allow_skills` —— 原版那一趟 `skill_activations == 0`，于是"无技能帧的数值"就是
  全场的数值。

## 构建与自测

```bash
cd rios-sim
go build -o rios-sim.exe .      # 本机 Go 1.26，无需联网（GOTOOLCHAIN=local）
echo '{"id":1,"cmd":"ping"}' | ./rios-sim.exe
```

Python 侧的门：

```bash
python tools/check_simgo.py     # 编译 + 协议 + 拒跑口径 + 3 例对拍（约 3 秒）
python _proto/simgo_parity.py   # 全量对拍：12 关 × 2 阵容（约 10 秒）
```

本机没装 Go、或没同步数据库时**跳过**而不是失败——克隆仓库的人不该被一个新组件拦在
门外。

## 进度

| 步 | 状态 |
|---|---|
| 骨架：协议、`ping`、错误口径 | **已落地** |
| 最小版本：常规关卡的完整 tick 循环 | **已落地**（技能/装置/召唤/位移/控制仍拒跑） |
| 对拍台：两版判决逐项比对 | **已落地**，`12 关 × 2 阵容`全绿（单场 Go 0.5–2.1ms vs 原版 60–320ms，约 **100×**） |
| 接进搜索（默认仍走原版，对拍全绿后切换） | 未开始（要先补技能：真作业一定开技能） |

一次运行的实际数字（2026-09-18，本机，`_proto/simgo_parity.py`）：

```
✅ 1-7   单人    5杀/10漏/ 74.8s  原版  122ms  Go 1.1ms  加速 107.7×
✅ 2-1   单人   18杀/ 8漏/130.2s  原版  224ms  Go 1.2ms  加速 185.6×
全部一致（24 例）
```

> 对拍口径：`won` / 击杀 / 漏怪 / 部署 / 阵亡 / 出怪数 / 超时 逐位相等；
> 耗时与总伤害按 1e-6 相对容忍（"差一个帧"必须是错，所以容忍度取得很紧）；
> 漏怪明细逐笔比（时刻 + 名字 + 扣命）。
