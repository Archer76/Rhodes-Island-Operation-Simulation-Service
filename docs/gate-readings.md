# 整闸读数台账（六道闸门里的第 ⑥ 道）

> **这份台账记的是「一次完整的整闸读数」**：`tools/check_go_all.py --selfcheck` 在
> **某一棵具体的树**上、用**某一枚具体的仪器**、喂**具体的一批输入**跑出来的结果。
>
> 它存在的理由：这类读数此前散落在各分支文档里，而**引它的人看不出「这是哪一次、在什么上跑的」**。
> 本仓记过一条——**读数出错先分三个变量：固定尺子换对象／固定对象换尺子／工作区 vs 入库**。
> 这张表就是那三件套的落点。

## 一 · 一次读数必须带齐的四件

| 件 | 为什么 |
|---|---|
| **树**（`git log --oneline -1` 的 sha ＋ 是否干净） | 「工作区 vs 入库」是三个变量之一 |
| **仪器**（exe 路径 ＋ `sha256(16)`） | 不记仪器，将来绿红翻转时没人能判是**对象**变了还是**仪器**换了 |
| **输入**（`data/` 怎么来的、有没有那份个人名册） | 干员那一套需要 `docs/roster-*.md`，它是**个人数据、不入库** |
| **结果**（几套绿 ／ 守卫成立几套 ／ 红的是哪套、什么码） | 「25 绿 1 红」与「26 绿」是两件事，**红的是什么码**又是第三件事 |

## 二 · 读数

| # | 日期 | 树 | 仪器 sha16 | 输入 | 结果 | 留证 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-24 | `b65ac03`（detached 冻结树 `%TEMP%\gatechk`） | `2cfca5eb487b3e0b` | `data/` **复制**进去（无 junction）＋ **名册未拷** | **25 / 26 绿**，**26 / 26 守卫成立**；红的是 `干员 rc=3` | 本节第 3 条 |
| 2 | 2026-09-24 | 同上 | 同上（**同一枚**） | 同上 ＋ **补拷 `docs/roster-<uid>.md`** | **26 / 26 绿**，**26 / 26 守卫成立** | `out/_gate_frozen_b65ac03.log` |
| 3 | 2026-09-24 | **`5ee2707`**（detached 冻结树 `%TEMP%\gatechk`，**旧的已先断链接再摘掉**） | `2cfca5eb487b3e0b`（**与第 2 次同一枚** —— 两次之间没动 `rios-sim/`） | `data/` 复制 ＋ **名册已拷** | **27 / 27 套全绿**，**27 / 27 守卫成立** | `out/_gate_frozen_5ee2707.log` |
| 4 | 2026-09-24 | **`39d7713`** | **`6cdfe6e2982f298b`** ★ | `data/` 复制 ＋ 名册已拷 | **27 / 27 套全绿**，**27 / 27 守卫成立** | `out/pinned-gate-39d7713.log` |
| 5 | 2026-09-24 22:4x | **`3559424`**（同一工具第 2 次全流程真跑） | **`d26a699bb78b8ff1`** ★ | `data/` 复制 ＋ 名册已拷 | **27 / 27 套全绿**，**27 / 27 守卫成立**，rc=0 | `out/pinned-gate-3559424.log` |

★ **第 4 行是下面那条警告的现成实例**：它与第 1–3 行**同一份源码**（`rios-sim/` 与 `5ee2707` 之间零改动），
但冻结树建在**另一个目录**（`%TEMP%\rios-pinned-tree`）⇒ **sha 不同**。
⇒ 再次印证：**这个 sha 只回答「是不是同一次构建」**。

第 4 行由 `tools/verify_pinned_tree.py --sha 39d7713` 跑出（**这是那个工具的第一次全流程真跑**）：
建树 → 复制 `data/` ＋ 补名册 → 树内 `go build` → 整闸 → **先删 150 MB 拷贝、再摘树（不带 `--force`）**，
全过程 rc=0，留证文件在 `out/`。

★ **第 5 行（`3559424`）是同一个工具的第二次真跑**，也是**取证范围变了之后**的第一次：
缓存从 320 关键长到 **562 关键（286 份不同内容）**，所以下面这几行的分母与前几行**不可比**。

第 5 次那一跑的关键行（原文）：

```
取证范围：缓存可达的关卡**键** 562 个（＝不同关卡**内容** 286 份 × 难度与别名标签；喂给需要清单的那两套判据）
✓ 关卡     562 / 562 关逐字段一致
✓ 敌人     3077 只敌人逐字段一致
✓ 干员     真实练度 679 / 679（深扫 468 ＋ 名册 211）；合成练度 1324 / 1324（char_ 段 460 位全量宽扫）
✓ 关卡静态 1140 例逐字段一致 · 格表 562 关 · 部分规格 562 关的 12 个值键
✓ 寻路     32736 例逐格一致；路线分段 33650 段；路线计划表 16368 条
✓ 机制规格 562 关逐字段一致（有田地 26 关 / 无田地 536 关）
✓ 单一入口 580 例逐路径一致（A 24 ＋ B 553 ＋ C 3）；19 键全部造齐
★ 27 套守卫全部成立
结论：27 / 27 套全绿
```

★ 第 5 行与前几行**分母不同**（320 → 562 键），**不许跨行比大小**；
且它的仪器 sha 又是新的一枚（构建身份，见下面那条警告）。
★ 这一跑之前刚做过两件事，都属于「新内容暴露真问题」那一类：
① 闸门接上「关卡本地敌人定义」的回退（第 14/16/17 章 4 关的出怪表真的引用了它）；
② 八套冻结基线按新缓存**重录**（否则冻结档会 `rc=6`——那是「基线该重录」，不是 Go 漂移）。


第 2 次那一跑的关键行：

```
取证范围：缓存可达的关卡 320 个
✓ 关卡 320/320 · 敌人 1745 只 · 干员 真实练度 679/679 ＋ 合成练度 1324/1324
✓ 范围 292/292 · 技能 11012/11012 · 分类 1056/1056 · 面板 13848/13848
✓ 关卡静态 656 例 · 格表 320 关 · 部分规格 320 关 · 寻路 19460 例
✓ 闸门 34 例 · 出怪规格 1154/1154 · 机制规格 320 关 · 单一入口 339 例
✓ 干员规格 64/64 · 自造规格 25 例 · 调用链 24/24 ＋ 静态 4 条 · 命令面
★ 26 套守卫全部成立
结论：26 / 26 套全绿
```

第 3 次（**27 套**）新增与变化的关键行：

```
取证范围：缓存可达的关卡**键** 320 个（＝不同关卡**内容** 159 份 × 难度与别名标签；…）
✓ 干员     rc=0  真实练度 679 / 679（深扫 468 ＋ 名册 211）；合成练度 1324 / 1324（char_ 段 460 位全量宽扫）
✓ 干员规格  rc=0  干员规格 64 / 64 人次逐位一致
✓ 敌方机制  rc=0  已消费 41 ／ 具名 unported 17（逐条见文档「具名 unported 登记」一节）／ 真红 0
★ 27 套守卫全部成立
结论：27 / 27 套全绿
```

★ 「取证范围」那一行**从「关卡 320 个」改成了两个数并排**（索引键 320 ＝ 不同内容 159 份）——
原来的说法把**键数**说成了**关数**。口径注释里还记着：144 组共用同一份内容，
其中 **15 组不是 `#f#`** 而是 `easy_XX-YY` 与 `main_XX-YY` 这对**别名**，所以口径取「按 `data_path` 去重」。

## 三 · 第 1 次那条红**不是判据红**，而且它是个结构性事实

### ⚠ 先记住一条：「仪器 sha16」是**构建身份**，不是**源码身份**

本台账记的是 exe 的 `sha256(16)`。**同一份源码，在不同目录里构建，会得到不同的 sha。**
今晚实测（`rios-sim/` 自 `b65ac03` 起**一个字节没改**）：

| 构建在哪 | sha16 |
|---|---|
| 冻结树 `%TEMP%\gatechk` | `2cfca5eb487b3e0b` ← 本台账第 1–3 行记的就是它 |
| 主树 `rios-sim` | `f7cca5041e5a88b9` |
| 把 `rios-sim/` 拷到另一个临时目录再建 | `0e6e898ce58d3c0e` |

加 `-trimpath` **也没能让两棵树相等**（主树 `5ce12b5fcb8b5205` ／ 冻结树 `7533d5575a9b679e`）。

⇒ **推论三条**：

1. 这个 sha 回答的是「**是不是同一次构建**」，**不是**「**是不是同一份源码**」。
   跨树比较它**没有意义**；同树前后比较它才有意义（本台账第 1-3 行同树，所以可比）。
2. 想钉**源码**身份，要用**源码侧签名**（树 sha ＋ 源码内容签名），不能用 exe sha。
3. ★ **本台账每一行的 sha 只对本行那棵树有效** —— 「同一枚仪器」这句话，
   只在**同一棵树路径**下成立。

`干员 rc=3` 印的是：

> 真实练度·账号名册 **输入MISSING**（0 次折算未量到）⇒ 本轮不给判决；
> 真实练度·深扫 468/468、合成练度·全量宽扫 1324/1324 次折算逐字段一致

`rc=3` 是**仪器缺输入**，与 `rc=1`（判据红）**不是同一个码**。名册在 `.gitignore` 里
（个人数据、不入库）⇒ **闸门在彻底干净的检出上永远不会全绿**，
它的形态是「**25 绿 ＋ 1 具名缺输入（rc=3）**」。

★ 比原来那版好得多：原来「名册不在」会让那一段**静默不跑**、脚本照旧 `rc=0`
并印「468/468 一致」。**三态设计在这里第一次真正救了场**。

补拷名册之后，同一棵树、**同一枚仪器**，26/26 全绿 ⇒ 差的那一环自始至终**只有一个输入文件**。

## 四 · 怎么复现

```powershell
# 1) 建一棵冻结树（detached，**不建任何 junction**）
git worktree add --detach $env:TEMP\gatechk <sha>

# 2) 把输入**复制**进去（不是链接）
Copy-Item data "$env:TEMP\gatechk\data" -Recurse -Force
Copy-Item docs\roster-<uid>.md "$env:TEMP\gatechk\docs\" -Force   # ← 少这一句就是 25/26

# 3) 在那棵树里自己 build，并用**它自己的**仪器
cd "$env:TEMP\gatechk\rios-sim"; go build -o "$env:TEMP\gatechk\out\acceptance\rios-sim-stage3.exe" .
$env:RIOS_SIM_BIN = "$env:TEMP\gatechk\out\acceptance\rios-sim-stage3.exe"

# 4) 跑
cd "$env:TEMP\gatechk"; python -X utf8 tools\check_go_all.py --selfcheck

# 5) 收尾：先删掉拷进去的 data，再摘 worktree（**不用 --force 兜**）
Remove-Item "$env:TEMP\gatechk\data" -Recurse -Force
git worktree remove "$env:TEMP\gatechk"
```

⚠ **绝不要用 junction 建冻结树**：2026-09-24 有人用 `git worktree remove --force`
收尾时**穿透 junction** 删掉了主仓 `data/` 的内容。收尾要先断链接再摘树。
