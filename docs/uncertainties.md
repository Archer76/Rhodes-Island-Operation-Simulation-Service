# 待裁定清单

> 这份文件由 `python tools/uncertainty_audit.py` 生成。
> **每一张表的最后一栏 `裁定` 是给人填的**，重新生成时会被读回续用，不会冲掉。
> 表中其余各栏都是实测数据，请不要改（改了下次生成就没了）。

## 怎么填

1. 在 `裁定` 栏写结论。词表那一节可以直接写 `认成 buff/减伤`、`排除`、`留空`。
2. 写 `|` 会切断表格——要写字面竖线请写成 `\|`。
3. **表格里行序按频次降序**，从高到低裁收益最大；低频长尾可以整段跳过。
4. 一节里若想批量同意，可以在该节第一行写 `以下全部同意建议`，我会按建议逐条落地。
5. 填完告诉我，我读回裁定并接进代码，然后跑全套自检。

## 一、项目级问题（答一句即可）

| 问题（键） | 分歧在哪 | 现状与影响 | 我的建议 | 裁定 |
|---|---|---|---|---|
| 法术伤害有没有 5% 保底 | wiki.gg 的 Damage 页原文说「不论伤害类型，最终伤害至少为攻击力的 5%」；xulai1001/akdata 也实现了；但 wxhwwla/calc-framework 只对物理保底 | 本项目 `battle/damage.py` 现在**无保底**（法术 = ATK×scale×(1−RES/100)）。影响 SR-EX-8 的吓人路灯（RES 99）：有保底是 5%，无保底是 1% | 维持无保底（跟随 calc-framework）／改为有保底（跟随 wiki 与 akdata） | 有保底，5% |
| 攻速下限取多少 | wiki 写 20，akdata 写 10 | 现无夹取。攻速只影响攻击间隔换算，下限越低越容易打出高频 | 取 10 或 20，或明确「不夹取」 | 20 |
| 攻击力取整方式 | 面板取整与伤害结算取整是两回事，原先混在一行问。wiki 明确的是**伤害**里的 FLOOR | **2026-09-17 分两处定案**：面板 = 四舍五入（`operator/stats.py` 默认已由 floor 改为 round）；伤害结算 = floor（`battle/damage.py`，不动）。判据见 `check_db` 的 [4b] 节（红豆 1185/510、怒潮凛冬 2981/1307/473 命中 round；攻击 1307 一栏 floor 给 1306，直接排除），旁证是 prts.wiki 干员页计算器亦用 `Math.round` | 面板 round、伤害 floor，两者各自成立 | ▼ 原裁定「维持不变」只适用于**伤害侧**；面板侧 2026-09-17 改为 round |
| `tile_forbidden` 与 `tile_empty` 哪个对应哪种不可部署地块 | 两者 `(heightType, buildableType, passableMask)` 完全一致，都是 NONE/FLY_ONLY，**字段分不出来** | 现按「不可部署地块」统一处理，不区分。若要区分只能靠名字猜 | 维持不区分 |  |
| 算式认领：属性表扩到哪一层 | 第二节列了全部 169 个候选词。扩得越宽，认错的机会越多 | 当前 `_EXPR_ATTR` 只有 11 项 | **只收战斗结算真正用到的**（阻挡/移速/攻防/攻速/重量/再部署），其余留空 | 只留战斗结算有用部分 |
| 干员侧要不要也接算式 pass | 实测干员侧 10392 条技能描述里，纯常数片段 561 处、**真变量只有 8 处**，且 8 处**全部**是 `速度+0.25/秒` 的 `/秒` 被当除号 | 目前只有敌人侧接了 | **不接**——零收益，只多出 7 种误读。此口径须写进文档，免得日后有人「顺手对称一下」 | 全部接入，本项目之后需要脱离llm，不能保留自然语言 |
| `防御力/法术抗性最终×0` 怎么读 | 斜杠是**并列**（「防御力和法术抗性都归零」），不是除法。两侧不是伤害类型词，现有判据拦不住 | 共 4 处。现产出 `算式 法术抗性最终×0 ⟨变量：法术抗性最终⟩`：字符串忠实、语义读错 | 补判据（如「最终」+乘 0 视为并列）／接受现状 |  |
| 规则抢先导致算式不出一项，是否可接受 | 规则已咬走的区间，算式不再重复出项（这是「不可能重复计数」的保证） | 代价：`攻击力/防御力降低至已处理目标数量/待处理目标总数量×100%` 只留下「降低（幅度未写明）」，而正文其实写了幅度。1 类 | 接受（保证不重复计数）／改成「算式优先于规则」 | 同样的，不保留自然语言，写成算式 |
| 阿米娅技2「影霄·绝影」的『接下来的伤害类型变为真实』管到哪一段 | 描述是「进行 10 次攻击力 220% 的法术斩击（**最后一击**系数加倍且为真实伤害）；……**并且接下来的伤害类型变为真实**」。10 次里只有末击明写「为真实伤害」，那『接下来』究竟指技能剩余时间她自己的**普通攻击**，还是**斩击序列的后几段** | 现按 `_wants_true_damage`（描述含「伤害类型变为真实」）判为**整条技能**的真实——对这条技能**偏强**：会把 10 次斩击全按真实算，而原文只保证末击与『接下来』的那些。真数值差可达数倍 | **请博士裁定**：① 只末击为真实；② 末击起往后全为真实（含技能期普攻）；③ 整条技能全为真实（维持现状） |  |
| 阿米娅(术战者)天赋「青色怒火」的『技能开启期间效果加倍』是谁开技能 | 描述：「在场时所有友方单位的攻击力和防御力+7%，**技能开启期间效果加倍**」。加倍的主语没写明 | 现**未建模**（该天赋整条还没接）——它在场时是全场光环，承接它要新开一条光环路径（现有 `RegenAura` 只做治疗） | **请博士裁定**：① **阿米娅自己**开技能时翻倍（+14%）；② **受益的友方单位**各自开技能时翻倍。两者结果不同且都要写进实现 |  |
| 天赋「情绪吸收」在技能开启期间是否照常回 SP | 正文只写「攻击敌人时额外回复 3 点技力，消灭敌人后额外获得 10 点技力」，**没有**『非技能期间』的限定 | 现按游戏常规实现：**技能开启期间不回**（与既有 `sp_per_attack` 口径一致）。但这一条是**未证实假设**，天赋正文没写。若实际照常回，阿米娅的技力循环会明显更快（技能衔接变紧） | **请博士裁定**：① 技能期间不回（维持现状）；② 技能期间照常回 |  |
| 概率类效果（闪避/暴击）怎么进确定性模拟 | 阿米娅技1「获得 60% 的法术闪避」、赤刃明霄陈天赋2「闪避下一次攻击」都是**概率**效果，而本项目模拟器是**确定性**的（无随机数） | 现**未建模**，闪避率与触发次数都无处安放。同一段文本还出现在「有 X% 概率晕眩敌人」这类控制上 | **请博士裁定**建模口径：① **期望值法**（伤害 ×(1−闪避率)，可复现、但单次交战的波峰波谷被抹平）；② **确定性触发**（每 N 次命中固定闪 1 次）；③ **不建模**并如实标注。我倾向 ①，但这是口径问题不是技术问题 |  |
| 阿米娅技2「影霄·绝影」的击杀叠层：『斩击期间』指多长、结束后留不留 | 黑板 `amiya2_s_2[kill].atk 0.4` / `[kill].magic_resistance 20` / `[kill].max_stack_cnt 3`，描述写「**斩击期间**每击败一个敌人获得攻击力+40%和法术抗性+20（最多叠加 3 次）」 | 现**未建模**。歧义有二：① 该技能 `duration=35s` 但效果是「**立即**进行10 次斩击」，「斩击期间」是指那 10 次斩击的一瞬，还是整个 35 秒？② 叠满的 +120% 攻 / +60 法抗在**技能结束后是否保留**（描述没说） | **请博士裁定**：① 「斩击期间」的范围；② 技能结束后是否清零。两者都影响伤害量级，我不猜 |  |
| 召唤物的「同时部署上限」以哪个数为准 | 同一个召唤者身上有**三个**不同的数，且互相不等：① 天赋黑板的 `cnt`——它是描述里的「可以使用 N 个」（可动用总量，**随精英阶段变**：电弧/令 E0=3→E1=4→E2=5，望 4→5→6）；② 描述文字里的第二个数——电弧/令写「最多同时部署 3 个」（**恒为 3**，不随精英变），望写「最多拥有 5/6/7 枚」；③ token 自己的 `maxDeployCount`——电弧是 3、望是 4/5/6，但在**令/深池/梅尔 身上是 1**（它们明明能同时放 3-5 个，所以这个字段并不总是上限） | **2026-09-18 已按 ② 落地**（`battle/talents.py` 的 `find_summon_allowance`）：优先取「最多同时部署」→ 退而取「最多拥有」→ 文字没写时才回落到 `cnt`，并在返回值里用 `source` 标明出处。**明确不用** `maxDeployCount`——取它会把令错压成只能放 1 个。影响：召唤师同时能站几个（电弧/令现在取 3） | 维持现读法（文字优先、`source` 留痕）／并请顺带定一下：**望该取 6（「可以使用」）还是 7（「最多拥有」）**——现取 7 |  |

## 二、算式认领：属性词表

算式认出来、但**认不出作用在哪个属性**的共 **274 项**，归到 **130 个不同的左词**。当前 `_EXPR_ATTR` 只有 11 项，这张表就是它的候选扩展。

**高频词（出现 ≥2 次，共 216 项 / 72 词）**——建议优先裁这一张。

| 左词（键） | 频次 | 最常见形态 | 我的建议 | 裁定 |
|---|---|---|---|---|
| `法术伤害` | 13 | 法术伤害-80% | 认成 减伤（buff/减伤） |  |
| `百分比` | 10 | (百分比)+0.1 | 留空（占位符） |  |
| `当前移动速度` | 8 | 当前移动速度×800 | 留空（它是**系数**，不是被改的属性——见第一节算式口径） |  |
| `Lv` | 7 | Lv×0 | 留空（等级系数，不是一个属性） |  |
| `场上球员类敌人总数` | 7 | (场上球员类敌人总数+1) | 排除（足球模式，按既定口径不收） |  |
| `x` | 6 | x-4 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `发射` | 6 | 发射-20 | 排除（发射角度，不是属性增减） |  |
| `范围x` | 6 | 范围x-8 | 排除（范围记法） |  |
| `s` | 5 | s/15 | 留空（秒数记法） |  |
| `累计伤害阈值` | 5 | 累计伤害阈值-2200 | 留空（阈值，不是被改的属性） |  |
| `【绒毛屏障】的屏障值` | 4 | 【绒毛屏障】的屏障值-2 | **待定** | 可能是怪物或关卡机制，提问 |
| `使其` | 4 | 使其-2 | 排除（散文碎片） |  |
| `光弹对范围x` | 4 | 光弹对范围x-1 | 排除（同上） |  |
| `对十字范围x` | 4 | 对十字范围x-7 | 排除（同上） |  |
| `对目标所在地块及周围四格x` | 4 | 对目标所在地块及周围四格x-5 | 排除（同上） |  |
| `待处理目标计数` | 4 | 待处理目标计数+1 | 留空（计数，非属性） |  |
| `摩擦力` | 4 | 摩擦力-0.3 | 留空（物理引擎参数，非战斗属性） |  |
| `自身所在地块周围的视野x` | 4 | 自身所在地块周围的视野x-1 | 排除（视野记法） |  |
| `伤害` | 3 | 伤害+20% | 认成 减伤 |  |
| `强制撤退范围x` | 3 | 强制撤退范围x-4 | 排除（同上） |  |
| `然后发射` | 3 | 然后发射-20 | 排除（同上） |  |
| `10×充能层数)` | 2 | (10×充能层数) | 排除（疑似散文碎片/记法，待博士确认） | 给出具体出处 |
| `2+Lv×2)` | 2 | (2+Lv×2) | 排除（疑似散文碎片/记法，待博士确认） | 给出具体出处 |
| `20%×(上述增益的层数)` | 2 | 20%×(上述增益的层数) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `20%×场上同构碎片的数量)` | 2 | (20%×场上同构碎片的数量) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `4%+0.5%×倍率层数)` | 2 | (4%+0.5%×倍率层数) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `50%+10%×场上结晶数量` | 2 | 50%+10%×场上结晶数量 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `s后于目标周围一定范围x` | 2 | s后于目标周围一定范围x-1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `·另一个AMa` | 2 | ·另一个AMa-10 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `与活性源石风暴之间距离不超过其半径` | 2 | 与活性源石风暴之间距离不超过其半径+0.3 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `个` | 2 | 个/3 | **待定** |  |
| `仅攻击范围内存在非敌占L` | 2 | 仅攻击范围内存在非敌占L-44 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `使敌方能量值` | 2 | 使敌方能量值+1 | **待定** |  |
| `使自身周围大范围x` | 2 | 使自身周围大范围x-2 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `充能数` | 2 | (充能数×25) | **待定** |  |
| `切换目标` | 2 | 切换目标/6 | 排除（散文碎片） |  |
| `则计数` | 2 | 则计数+0.2 | 排除（散文碎片） |  |
| `召唤的“寒意”数量` | 2 | 召唤的“寒意”数量+1 | **待定** |  |
| `否则` | 2 | 否则×1.0 | 排除（散文碎片） |  |
| `场上结晶数量` | 2 | 场上结晶数量/5 | 留空（分子，非属性） |  |
| `对主目标周围四格x` | 2 | 对主目标周围四格x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `对大范围x` | 2 | 对大范围x-1 | 排除（同上） |  |
| `对更大范围x` | 2 | 对更大范围x-2 | 排除（同上） |  |
| `对目标及范围x` | 2 | 对目标及范围x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `对目标周围四格x` | 2 | 对目标周围四格x-5 | 排除（同上） |  |
| `对目标所在地块及周围八格x` | 2 | 对目标所在地块及周围八格x-4 | 排除（同上） |  |
| `岁` | 2 | 岁/岁·左爪/岁·右爪/岁·尾攻击力-30% | **待定** |  |
| `并使敌方能量值` | 2 | 并使敌方能量值+1 | **待定** |  |
| `并在影响范围x` | 2 | 并在影响范围x-5 | 排除（散文碎片） |  |
| `当前生命值比例` | 2 | 当前生命值比例-30% | **待定** |  |
| `影响半径` | 2 | 影响半径-0.075 | **待定** |  |
| `待部署区内“斩”的数量` | 2 | (待部署区内“斩”的数量)+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `攻击倍率` | 2 | 攻击倍率-10% | **待定** |  |
| `次` | 2 | 次/2 | **待定** |  |
| `此模式时使所在禁区的预警值` | 2 | 此模式时使所在禁区的预警值+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `每次普通攻击以攻击主目标位置为中心对` | 2 | 每次普通攻击以攻击主目标位置为中心对十字区域x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `清空范围内L` | 2 | 清空范围内L-44 | 排除（散文碎片） |  |
| `的年代印痕x` | 2 | 的年代印痕x-2 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `的近战途径物理伤害并令目标` | 2 | 的近战途径物理伤害并令目标-1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `碰撞半径` | 2 | 碰撞半径-0.05 | **待定** |  |
| `秒生效x` | 2 | 秒生效x-1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `秒的落雷区域x` | 2 | 秒的落雷区域x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `自身所在地块周围八格的视野x` | 2 | 自身所在地块周围八格的视野x-4 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `自身退场并令拾取计数` | 2 | 自身退场并令拾取计数+1 | 排除（散文碎片） |  |
| `范围内的修道院居民sp` | 2 | 范围内的修道院居民sp-20 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `该次攻击的攻击倍率` | 2 | 该次攻击的攻击倍率+20% | **待定** |  |
| `部署后对较大十字范围x` | 2 | 部署后对较大十字范围x-7 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `锁定该L` | 2 | 锁定该L-44 | **待定** |  |
| `附加的寒冷时长` | 2 | 附加的寒冷时长+1 | **待定** |  |
| `随后自身储存数` | 2 | 随后自身储存数+1 | 排除（散文碎片） |  |
| `随后自身拾取数` | 2 | 随后自身拾取数+1 | 排除（散文碎片） |  |
| `额外对以取消伤害的对象为中心周围四格` | 2 | 额外对以取消伤害的对象为中心周围四格x-5 | 排除（疑似散文碎片/记法，待博士确认） | 以上未填写部分先使用你的建议 |

**低频长尾（出现 1 次，共 58 词）**——可整段跳过；想逐条来也留了裁定栏。

| 左词（键） | 频次 | 最常见形态 | 我的建议 | 裁定 |
|---|---|---|---|---|
| `100%+20%×目标当前阻挡的敌人` | 1 | (100%+20%×目标当前阻挡的敌人数量) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `10×汲取体数量)` | 1 | (10×汲取体数量) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `125.0+25×我方能量值` | 1 | 125.0+25×我方能量值 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `125.0+25×敌方能量值` | 1 | 125.0+25×敌方能量值 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `150+50×我方能量值` | 1 | 150+50×我方能量值 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `2+撞击计数)` | 1 | (2+撞击计数) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `25×汲取体数量)` | 1 | (25×汲取体数量) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `3%×间隔数` | 1 | 3%×间隔数 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `3-目标数)` | 1 | (3-目标数) | 排除（疑似散文碎片/记法，待博士确认） |  |
| `50+25×增幅数` | 1 | 50+25×增幅数 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `50×敌方能量值` | 1 | 50×敌方能量值 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `80%+20%×技能次数` | 1 | 80%+20%×技能次数 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `90%+4%×场上结晶数量` | 1 | 90%+4%×场上结晶数量 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `AMa` | 1 | AMa-10 | **待定** |  |
| `·伪装成被占领的L` | 1 | ·伪装成被占领的L-44 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `※优先选择我方单位周围四格x` | 1 | ※优先选择我方单位周围四格x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `※另一个AMa` | 1 | ※另一个AMa-10 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `一次攻击` | 1 | 一次攻击-1000 | **待定** |  |
| `且伤害来源周围x` | 1 | 且伤害来源周围x-4 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `令其退场并使该技能计数` | 1 | 令其退场并使该技能计数+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `令给予方` | 1 | 令给予方+1 | **待定** |  |
| `优先级` | 1 | (优先级+2000) | 认成 buff/技能优先级 |  |
| `伤害时` | 1 | 伤害时-1 | **待定** |  |
| `伤害时取消此伤害并使【主体】` | 1 | 伤害时取消此伤害并使【主体】+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `使【主体】` | 1 | 使【主体】-1 | **待定** |  |
| `假死期间若另一个AMa` | 1 | 假死期间若另一个AMa-10 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `则令该年代的剩余影响步数` | 1 | 则令该年代的剩余影响步数+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `占用关卡敌人上限` | 1 | 占用关卡敌人上限+1 | **待定** |  |
| `另一个AMa` | 1 | 另一个AMa-10 | **待定** |  |
| `增益层数` | 1 | (增益层数×0%) | **待定** |  |
| `实为装置L` | 1 | 实为装置L-44 | **待定** |  |
| `对每个十字范围x` | 1 | 对每个十字范围x-6 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `将大范围x` | 1 | 将大范围x-3 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `层数` | 1 | (层数×300) | **待定** |  |
| `并令毒性雾霾的伤害等级` | 1 | 并令毒性雾霾的伤害等级+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `待部署区中的宿傲数量` | 1 | (待部署区中的宿傲数量+1) | **待定** |  |
| `待部署区中的宿怒数量` | 1 | (待部署区中的宿怒数量+1) | **待定** |  |
| `战斗经过一定时间或该L` | 1 | 战斗经过一定时间或该L-44 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `晕眩` | 1 | 晕眩+1 | **待定** |  |
| `普通攻击对目标及目标周围四格x` | 1 | 普通攻击对目标及目标周围四格x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `最高` | 1 | (最高-50%) | **待定** |  |
| `期间使相同范围x` | 1 | 期间使相同范围x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `期间点亮范围x` | 1 | 期间点亮范围x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `水平` | 1 | (水平/竖直方向)/1.4142 | **待定** |  |
| `治疗场x` | 1 | 治疗场x-1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `物理与法术伤害` | 1 | 物理与法术伤害-(已回收保险柜数×10%) | 认成 减伤 |  |
| `自身【毒性】层数` | 1 | 自身【毒性】层数×50 | **待定** |  |
| `自身占用关卡敌人上限` | 1 | 自身占用关卡敌人上限+1 | **待定** |  |
| `自身占用关卡敌人上限数` | 1 | 自身占用关卡敌人上限数+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `自身将作为该L` | 1 | 自身将作为该L-44 | **待定** |  |
| `自身非移动时改为` | 1 | 自身非移动时改为-35% | **待定** |  |
| `若伪装的L` | 1 | 若伪装的L-44 | **待定** |  |
| `被击倒后晕眩该影响范围x` | 1 | 被击倒后晕眩该影响范围x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `计数` | 1 | 计数-1 | **待定** |  |
| `调查熟练度后自身的撞击计数` | 1 | 调查熟练度后自身的撞击计数+1 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `随后寻找目标周围x` | 1 | 随后寻找目标周围x-4 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `随机选择周围四格x` | 1 | 随机选择周围四格x-5 | 排除（疑似散文碎片/记法，待博士确认） |  |
| `鸭爵` | 1 | 鸭爵/高普尼克/富营养的收割者/R-11 | **待定** |  |

## 三、算式误读：具体句子复查

下面是**具体句子**层面的复查，每条都附原文与当前读法。与第二节不同：第二节裁「词归哪一类」，这里裁「这句话有没有被读错」。

| 原文（节选） | 当前读法 | 问题 | 我的建议 | 裁定 |
|---|---|---|---|---|
| 受到的物理/法术伤害-80% | 算式 pass → `法术伤害-80%`（无属性） | **规则表完全没匹配**——写成「降低80%」能认，写成「-80%」不认。91 处同类，是唯一会**伪装成已解析**的一类 | 补规则字形 `受到…伤害-N%` → 减伤 | 受到的物理伤害与法术伤害都降低80% |
| 受到的物理/法术伤害降低80% | `e_damage_reduce_pct 减伤 -80% (物理/法术)` | 这条是对的 | 保持 |  |
| 受到的物理和法术伤害-60% | 同上（无匹配） | 「和」「与」写法的 `-N%` 也漏 | 与第一条一并补 |  |
| 防御力/法术抗性最终×0 | `算式 法术抗性最终×0 ⟨变量：法术抗性最终⟩` | 斜杠是并列不是除法；现状字符串忠实、语义错 | 4 处，见第一节第 7 问 |  |
| 发射-20°、±0°、+20° | `算式 发射-20`（无属性） | 发射角度，不是属性增减——9 处噪声 | 排除 |  |
| Lv×0 / (百分比)+0.1 | `算式 Lv×0`（无属性） | 公式系数，本来就不是一个属性 | 留空（现状即可） |  |
| (场上球员类敌人总数+1) | `算式 …`（无属性） | 足球模式 | 排除（既定口径不收） |  |

## 四、要实机数据、我取不到的

这些不是口径分歧，是**数据缺口**——只能靠实机录像或您告知。

| 事项 | 为什么取不到 | 现状 | 我的建议 | 裁定 |
|---|---|---|---|---|
| 赤刃明霄陈技3 的剑气速度 | 技能原文没写速度；gamedata 黑板里也没有 | 2026-09-16 由攻略录像实测为 **1.2 格/秒**，已写成默认值（构造参数，可调） | **已定案**，不需要裁定。细扫显示 1.2–1.25 会漏 1 只，而旧默认 4.0 恰好不漏——编出来的数不一定偏保守 |  |
| 敌人攻击动作时长 | gamedata 里没有该字段 | 模拟器假设 0.5s（`enemy_windup`） | 已做敏感性：0–1.0s 结论全同，60s 复现 37 杀 4 漏 |  |
| 干员抬手 prepDuration 与动画帧补正 | gamedata 无对应数据；prts.wiki 的 Module 命名空间持续 403 | **攻击间隔已定案否**（2026-09-16 实机：1.25×100/124 的预测 15.12 帧与实测 15.11 帧吻合到 0.07%）；仍缺的只有**首刀时机**（抬手本身） | 周期不必再接数据；抬手要拿只能实机逐帧 |  |

## 五、数据缺口（量小，可缓）

| 事项 | 缺什么 | 现状 | 我的建议 | 裁定 |
|---|---|---|---|---|
| 量纲未定的 3 个键 | `exp` / `attack@exp` / `sell_card_gold` | 都是装置键、来历未定，`check_formula` 已把上限锁在 3 | 按「只补战斗相关」豁免；删表即红，不会悄悄变多 |  |
| 元素损伤的结算 | 数据侧已认得种类与算式，战斗侧没有模型 | 未建模 | 工程量，非不确定；排在 A1 之后 |  |
| SR-EX-8 的敌人技能 / 飞行不可阻挡 / 反射 / BOSS 双形态重生 | 4 件都未建模 | 四人剑气方案已在现有模型下取胜（38 杀 1 漏，非三星） | 只有要打更难的关才需要 |  |

## 六、填了但还没落地的裁定

生成时有内容、但代码里还没生效的裁定会列在这里（我落地后自动消失）。

「已落地」由 `uncertainty_audit.py` 的 `LANDED` 表判定（人工维护，
记的是这条裁定进了哪段代码）——它不能自动判，但也正因如此，
某条裁定日后被改动悄悄回退时，这里会重新把它标回待落地。

| 节 | 键 | 您填的裁定 | 状态 |
|---|---|---|---|
| — | — | — | 暂无 |


## 七、怀黍离敌人侧机制：存疑读法

这四处都**不是**"不知道怎么写"，而是原文/数据允许多种读法，我按最保守的一种先落地了，
并在代码与自检里标了「⚠ 待裁定」。**改成另一种读法不会报错**，只会让结果不一样——
所以在这里列出来，请博士裁定；每条都写清"我选了哪种、另一种是什么、差多少"。

| # | 事项 | 我选的读法（已落地） | 另一种读法 | 差多少 | 裁定 |
|---|---|---|---|---|---|
| ~~1~~ | ~~敌人被击倒时的田地污染 `+N`，要不要同时抬【最大病害值】~~ | **已裁定（2026-09-17，博士给出 prts.wiki「特殊机制#病害值」原文）**：**两套值都不直接加**——`+N` 先进**【缓存】**，每 0.2s 释放 1 点累加到【最大】，【实际】再每 1s 靠拢【最大】。我原来写的"立刻加【实际】并顺手抬【最大】"以及为它列的两种读法**都是错的**，`POLLUTE_LIFTS_MAX` 已删。 | — | — | **已定案** |
| 2 | `enemy_attribute_mul` 同键两条（全局 ×1.2 + 点名 ×1.5） | **依次相乘 → ×1.8** | 后一条覆盖前一条 → ×1.5 | ex02 四星档那个被点名的敌人血量差 20%。我选相乘是因为 runes 里两条是**各自独立**的条目，没有"覆盖"语义 |  |
| 3 | 明识形态的「被标记者退场时污染」以谁为圆心 | **「祟」自己**所在格 | 退场的那个干员所在格 | 两者通常是相邻格（干员被祟打退场多半就死在它旁边），但对**远程干员**可以差到 3~4 格 |  |
| 4 | 明识形态「可进行远程攻击」的射程 | **只在敌人本来就有射程时才转远程**（祟的 `rangeRadius = -1`，所以仍是近战） | 给一个编出来的默认射程 | 数据里**根本没有**这个半径。编一个数就是"编错了是错"，所以宁可维持近战并在此标出 |  |

另外两条**只登记不使用**的，列在这里以免后来人以为是漏读：

* `Passive_Hit.extra_value = -0.001`（祟）：全库该前缀里唯一一个**没有任何正文提到**的键。
  可能是刻意的极小值占位（让某处乘法不为零）。**不实现**，但保留字段与注释。
* `PassiveM2.value = 100`（祟）：与 `pm2_mark_pollut`（40）并列，来历不明，同样是只读数不使用。

## 八、费用与再部署：新口径下我拿不准的三处

2026-09-18 给模拟器加了两条真实规则（**费用闸门** `cost_mode="strict"`：
付不起就拒收并留痕；**再部署规则** `redeploy_mode="strict"`：同一干员不能
同时在场上两个、离场后要等 `redeploy_time` 秒）。这两条本身有数据支撑，
但下面三处的**取数口径**我只能先选一种，请博士过目。

| # | 事项 | 我选的读法（已落地） | 另一种读法 | 差多少 | 裁定 |
|---|---|---|---|---|---|
| 1 | **再部署时间的天赋/模组缩减** | **不建模**：`OperatorUnit.redeploy_time` 直接取 `operator_attr.respawn_time`（基础值，默认 70s） | 天赋与模组里确有减再部署的黑板（如处决者的「再部署时间 -X 秒」），应当折算进去 | 对带这类天赋的干员，冷却会**偏长**；只影响"同一人二次部署"的合法性判定，三条基线里都没用到（1-7 真作业只有一人） |  |
| 2 | **突袭（challenge）的判据来源** | `stage.options.is_hard_training` | `stage` 表的 `hard_level_id` / `diff_group` 列 | 影响 `stars_of(..., challenge=True)` 是否给出四星。三条基线的该字段都是 `False`，所以当前无差；但换成 `diff_group` 判断可能对别的关有差 |  |
| 3 | **同一帧内的扣费顺序** | 固定为 **干员 → 召唤物 → 装置**，先到先扣，扣完为止 | 实机是"点下去那一刻逐个结算"，顺序由玩家点击决定 | 同一帧内同时落地两手、而池子只够一手时，谁拿到钱会不同。当前只在极端排期下才会咬到 |  |

**这一条不是"待裁定"，是为了防止后人重复踩**：旧 `stars_of` 用
`max(1, 3 - leaks)`，等于凭空造出一个「漏 2 只及以上 = 一星」的档位。
博士 2026-09-18 口述纠正：打赢但漏怪（无论几只，只要命没扣完）就是**二星**，
**没有一星**。已改并加了负向守卫（`all(stars_of(True, n) != 1 ...)`）。
旧实现的自注里写着"这是一条假设"——**自注写着是假设的，就不该当基线用**。

## 九、状态机：寒冷有状态、**没有效果**

圣聆初雪的天赋1 与技2 都要施加【寒冷】。落地时发现：**这个状态的"状态"部分
完全确定，"效果"部分的数值查不到。**

### 已经查实的（有出处）

| 项 | 结论 | 出处 |
|---|---|---|
| 冻结 FROZEN 是什么 | 状态机切入 `FROZEN`：**缴械 + 敌人类单位无法移动** | PRTS「异常效果」页 `AbnormalFlag` 表第 16 行 |
| 寒冷 COLD 是什么 | **攻击速度降低**；在特定条件下转变为冻结 | 同上第 23 行 |
| 寒冷是不是可抵抗状态 | 是（干员→敌人、敌人→干员两个方向各算一条） | PRTS「可抵抗状态」页 |
| 寒冷在各处的**时长** | 天赋1 `cold` 1.5 / 2.0 秒；技1 `cold` 3.5 秒；技2 `talent@cold` 5.0 秒 | `operator_talent` / `skill_level` 黑板 |

### 查不到的：寒冷**降低多少**攻击速度

四路取证，全部走到底：

1. PRTS「异常效果」页只写"攻击速度降低"，并注明「此异常效果相关机制主要由
   **Buff** 实现，并非异常本身的效果」——数值不在异常效果里。
2. PRTS「可抵抗状态」页只列它是可抵抗状态，不给量；同页把
   「烹泉与沏虹死亡后造成的攻击速度降低 Buff」列为**另一条**，说明寒冷的
   降攻速是独立的 Buff，不是那个。
3. 会施加寒冷的敌人（雪怪术师）技能描述里只有时长（10 秒），
   **技能黑板是空 `{}`**——连 `enemy_skill` 表里都没有这个数。
4. 数值只可能在客户端的 `excel/buff_table.json`，而它在两个公开镜像上
   **都是 404**：
   - `raw.githubusercontent.com/Kengxxiao/ArknightsGameData/.../excel/buff_table.json` → 404
   - `map.ark-nights.com/data/excel/buff_table.json` → 404
   （同一路径下的 `skill_table.json` 是 200，所以不是路径写错。）

### 我做了什么、没做什么

**做了**：`EnemyUnit.cold_timer`（剩余时长）与 `EnemyUnit.freeze_timer`（限时
冻结）两个状态，时长从黑板精确取数；限时冻结与「站在满层积雪上」那种位置派生
的冻结**分开两个字段**（后者每帧按位置重算，共用一个字段会被它抹掉）。

**没做**：**不给寒冷加任何攻速折减**——不编数，也不当 0 用。字段注释里写明了
这一点。

**故意没接线的一条**：「受到伤害时使敌人寒冷 X 秒」。它在全库里对应
`op.take()` 的 **9 个调用点**，而寒冷的攻速效果未知 → 挂了**不产生任何行为
差异**，却要散挂 9 处、必然漏一处。按「宁可漏不可错」压后。

### 数值到位后要改的地方（只有两处）

1. `ak_tactic/battle/unit.py` 的 `EnemyUnit.cold_timer`：加一条攻速折减
   （攻速是乘算还是加算要一并定）。
2. 「受伤害时给攻击者上寒冷」：在伤害链路里带上攻击者身份后再接，
   不要在 9 个 `take()` 调用点各写一遍。

## 十、位移：规则与接线都已落地（2026-09-18 起本节不再是欠账）

**规则层**在 `ak_tactic/battle/displace.py`；**接线**在 `EnemyUnit.displaced`
（守卫 `check_battle` 第 `[26]` 节）。原先"没有地方可以落"的症结是位置由
`point_at(route, progress)` 算出、没有「脱离路线」这个概念——解法是加一个**并列**
坐标，而不是改掉 `progress`。

仍然欠的（都不在本批范围内）：

* **弹道时间**没接：现在是瞬时推开，`displace.push_time` 拿到了但没用上；
* **拉动**没接：`attract` / 「诱导」是另一套机制（圣聆初雪技3 用的是它）；
* `force` 没进 `SkillEffects` 的分类表，是从 `blackboard` 直读的；
* 敌人**重量**只在 gamedata 的 `massLevel`，敌人库里没有这一列。

位移的**规则**已经查实并落地（`ak_tactic/battle/displace.py`，出处 PRTS
「推与拉」与「游戏数据基础#力度对应表」）。落不下去的不是数，是**表示**。

### 规则的结论（有出处，可直接用）

- **受力等级 = 力度等级 − 重量等级**（「力度对应表」与目标重量的**差值**）。
- 推力**作用时间瞬间**，等价于给目标一个"受力×1s"的动量；之后是匀减速滑动。
- **弹道类与特效类不同值**（命中帧多一帧以初速度的失衡位移），0 级受力是
  1.6958 格（弹道）对 1.56247 格（特效）。
- 方向力的**特殊修正**：夹角 > 45° **或** 距离 < 0.25 格 → 改判径向力且
  **受力等级 −2**。是**或**，不是且。
- 拉力 0 级及以上是**拉至身前**，不是走固定格数。
- 位移是**连续值**（1.6958 格），不是整数格。

### 落不下去的地方

敌人位置是从**路线进度**算出来的：`ak_tactic/battle/unit.py:828`
`self.position = point_at(self.route, self.progress)`，走分段计划时另有
`_advance_legs`。也就是说——

**"敌人偏离路线"这件事在模型里没有表示。** 把 `position` 改掉，下一帧
`point_at(route, progress)` 会把它算回原处；把 `progress` 改掉，等于"沿路线
后退"，只对**与路线反向的推力**成立，横向推力（把她沿部署方向推出去）根本
表达不了。

要真正接线，得先给敌人加一层"偏离路线的位移"，或者把行进模型换成
2D 位置 + 重新寻路。**这是架构改动，不是补一个字段**，所以我没有把
规则硬塞进现有的 `progress` 里——那样会在横向推的场合静默算错，而
"看起来推动了、结果不对"比"没实现"更难查。

### 什么时候值得动

等真有一份作业**靠位移通关**时再动（地穴推人、推回拆火）。在那之前，
规则层留在库里、逐值有守卫（`check_battle.py` 第 `[20]` 节，27 条），
接线时直接可用。

### 与它无关的一个坑（免得白找）

敌人**重量在 gamedata 里**（`gamedata/enemy.py:507` 的 `weight`，取
`massLevel`），**敌人库 `data/enemydb.sqlite` 里没有这个字段**——表结构里
只有图鉴字段（`page`/`name`/`category`/…），没有任何质量或重量列。
去 enemydb 找会白找一轮。

## 十一、屏障：叠加与衰减的规则已查清，但实现先不做

**规则本身不欠账**（2026-09-18 查证，守卫 `check_battle` 第 `[25]` 节）：

* 屏障**会叠加**，上限**每个技能各自**——varkis技2 `max_hp_ratio 1.0`、
  cairn技2 `shield_max_hp_ratio 0.9`；
* 一次授予**可以超过最大生命的 100%**——gravel技2 `hp_ratio 2.5`（250%）。
  **因此不存在"统一按 100% 封顶"这种写法**；
* **衰减**是另一回事：gravel技2 `duration 10`、morgan技2 `shield_duration 6`、
  cairn技2 `buff_duration 10`。

**欠的是实现**，而本批**不需要**它：十位干员里只有电弧技1 与机械师技2 给屏障，
两者都没有上限键与衰减键、都写「持续至技能结束」、一次激活只授予一次，所以
`OperatorUnit.grant_barrier` 现有的 `max` 与 `sum` **完全等价**。

**什么时候必须动**：那两个技能里出现上限键或衰减键时。守卫 `[25]` 钉着这条，
它会先响。

**另需注意**：`hp_ratio` 这个键**同名反义**（见计划里「同一条判据的坑」），
屏障的判据只能按渲染后的描述取，不能按键名取。

## 十二、撼地者特性的**溅射形状**取不到，天赋1 因此卡在第一格

怒潮凛冬天赋1「汹涌怒火」要落地，前提是先有**特性溅射**：

> 特性（撼地者）：攻击使**目标周围的其他敌人**受到相当于攻击力 **50%** 的
> **群体**物理伤害。
>
> 天赋1：特性溅射造成的物理伤害**提升 24%**（黑板 `damage_scale: 1.24`），
> 且**溅射到的每个高台**对周围四格所有地面敌人造成相当于攻击力 24%
> （潜3 起 27%，黑板 `attack@splash_atk_scale`）的物理伤害。

**已经确定的**：50% 这个底数在特性正文里；`damage_scale` 是天赋的倍率
（50% × 1.24 = 62%）；高台可判（`gamedata.stage.Tile.is_highland()`，
`height == "HIGHLAND"`）；"地面敌人"可判（敌人侧有 `is_flying`）。

**还没确定的**：**"周围"到底是哪几格**。

* 本地库**没有**这个字段——`operator_attr` 的列里没有 splash（列清单见
  `PRAGMA table_info(operator_attr)`），`operator.raw_json` 里也搜不到
  `splash` 字样；
* 干员侧的普攻目前**根本没有溅射实现**：`sim._operators_attack` 里是
  `for target in targets:` 逐个结算，没有"目标之外的敌人"这一路；
  `sim.py` 里带 `splash` 字样的字段（`skill_atk_cross` 等）全在**敌人**侧。

**因此这里是一个"必须先查外部数据源、查不到就不能动"的点**：形状猜错会让
溅射范围整个偏掉，而且**不会报错**——只是伤害数字安静地不对。按纪律记在这里，
先做别的，不停在原地。

**要查的是什么**：撼地者（子职业 `hammer`）特性的溅射覆盖——
是目标格的四邻十字、还是含目标格的 3×3、还是别的形状。
查证时可一并核对：`prts.wiki` 的特性页 / 游戏本体 `character_table` 里
该子职业的 `trait` 字段 **与** `ranges` 表是否给出了溅射掩码。

### 十二之一、2026-09-18 第二轮：**形状键找到了**，问题缩小为"语义待定"

上一版把这条写成"取不到"，**这一步已经推进**：

```
sqlite> SELECT range_id, blackboard FROM operator_trait
         WHERE char_id='char_1051_headb2';
range_id = None
blackboard = {"attack@ability_range_radius": 1.0, "attack@atk_scale_2": 0.5}
```

* **形状由 `attack@ability_range_radius` 给出，值是 1.0**；
* 那个 `0.5` 就是特性溅射的**底数**（`attack@atk_scale_2`），天赋1 的
  `damage_scale: 1.24` 乘在它上面 → **62%**。
* 之前在 `operator_attr` 与 `operator.raw_json` 里翻不到，是因为**找错了表**：
  结构化特性住在 **`operator_trait`**（正文 `override_description`、
  系数 `blackboard`）。全库用这个键的只有 `hammer` 一族 4 位，全是 1.0。

**仍待裁定的是"半径 1"的几何**：是曼哈顿半径 1（目标格 + 上下左右四邻 = 十字 5 格），
还是切比雪夫半径 1（含目标格的 3×3 = 9 格）。**现有资料不足以裁定**：

* 仓库里**已有同一口径的先例**，但那是敌人侧自己命名的字段——
  `EnemyUnit.skill_atk_cross` 的注释写着「溅射十字半径（1 = 目标格 + 上下左右四邻）」
  （`unit.py:767`）。沿用它**是自洽的，但不是游戏数据的证明**；
* 上网只得回一页 2022 年的 PRTS 沙盒残页（菲亚梅塔一条），没有撼地者；
* 特性正文只写「目标周围的其他敌人」，**没有**给出格数——而**同一份数据里
  天赋1 的第二段明写「周围四格」**，说明这套文案在需要时会写死格数；
  特性没写，恰恰说明它不可从正文推。

**结论：暂不实现，也不猜。** 实现时把形状收在一个常量里，并在守卫里钉住，
使"十字 → 3×3"是一处改动。要裁定时最可靠的路径是**实机录屏逐格验证**，
其次是游戏本体的 `ranges`/`tile` 相关表。**顺带记一条判据上的教训**（本轮踩到）：`damage_scale` 这个键**归得了类**
（进 `buff` 桶），因此它会**通过审计的第一道筛子**，而第二道筛子只在第一道
失败的键上跑——于是"这个键战斗层根本没读"这件事，两道键级筛子**都看不见**。
补的第三道筛子是**天赋级**的（天赋名在不在源码里），才把它那一族照出来。

## 十三、焰狐龙梓兰：每一项都卡在**语义**上，不是卡在实现上（2026-09-18 排查）

她是第三批里欠账最多的一位（20 条键）。逐条查过数据源之后，**没有一项是
"不知道怎么做"，全都是"这句话该怎么读"**——所以按纪律留档、不猜。

| 项 | 正文/黑板 | 卡在哪 |
|---|---|---|
| 天赋1 强击瓶专家 | 「部署后**首次**开启技能时，接下来 **50 次**攻击的攻击力提升至 105%」`power_attack_count=50` `power_attack_scale=1.05` | **"50 次"按什么数**：按**出手**（一次攻击动作算 1 次，该次的所有箭矢都吃加成）还是按**箭矢**（技1 一次射 4–5 支、技2 射 3/4/5 支）？两者差 4–5 倍，且**都不会报错**。正文只写「50 次攻击」，没写死。**仓库里没有同类先例可循**（查过：`power_attack` 等计数型攻击加成**一处都没有**），所以也抄不到既有口径 |
| 天赋2 翔虫机动 | 「再部署时间 **-15 秒**；部署至上次部署位置周围时，30 秒内攻击力 +10% 并且可以部署在近战位」`respawn_time=-15` `atk=0.1` `atk_duration=30` `ignore_build_type_target=1` | 「上次部署位置周围」的**半径**未定；且这一段是**部署期**的规则（改再部署、改可部署地块类型），**不属于战斗模型**——它该落在名册/验证层而不是 `battle/` |
| 技1 刚射 | 「4 支 160%/5 支 200% 的箭矢，每支有 **20% 概率**晕眩 2 秒」`stun_prob=0.2` | **离散控场状态不能折期望**（既有裁定，见随机口径那条）。要么标"不建模"，要么走"按期望时长摊"——需要一次裁定，不能自己定 |
| 技2 飞翔瞪射 | 「立刻**起飞**并射击 3 次…之后**降落**」`fly_height=0.4` `fly_duration=0.2` `fly_end_duration=0.2` | **起飞滞留**与「位移的起飞滞留」是同一条留档欠账（本批第③步的另一半）。`fly_*` 的**坐标系语义**（高度是格还是格比例）两库都取不到 |
| 技3 龙之箭 | 「蓄力 3 秒后射出**直线飞行**的贯穿箭矢…每飞行一段距离对周围所有敌人造成…**射程无限**」`dist_interval=0.25` `max_dist=99` `force=2` `knockback_duration=1` `wait_duration=1.5` | 箭矢是**飞行实体**（要沿途逐段结算 + 可被推动的敌人）。`max_dist=99` 是"无限"的占位值，**不能当 99 格用** |

**结论：四项需要的都是"读法裁定"，不是"数据或实现"**。裁定到位后，每一项都
是明确可做的；在此之前动手，只会做出一个**看起来对、数字安静地错**的模型。

**顺带记一条审计的方法边界**（不是漏账，是方法不适用）：第三道筛子按
**天赋名**查，因此**无名天赋一律看不见**。全库无名且带黑板的天赋有
**322 行 / 213 位干员**（焰狐龙梓兰的组2 `attack@damage_scale=0.333`、
组3 `respawn_time=-15` 就是无名的那两个）。不过这一类**并没有漏掉**——
它们的键照样进 `keys_of` 的 `allk`，第一、二道筛子照常管；只是第三道那种
"按名字找检测器"的判法对无名者**无从下手**，属方法边界。

## 十四、**天赋给的属性加成整体没被应用**——但"补上"这件事不能一刀切

2026-09-18 查凯尔希·思衡托时撞见的，**影响面是全库而不是某一位**：

```
计算器给   凯尔希·思衡托 def=257 maxHp=1543 blockCnt=1
天赋要     遗尘守望 {max_hp: 0.25, def: 0.25, block_cnt: 1.0}
```

`blockCnt` 与库里原始值**一模一样**——天赋那三项**一项都没进去**。

**全库天赋黑板里的出现次数**：

| 键 | 次数 |
|---|---|
| `atk` | 416 |
| `def` | 183 |
| `attack_speed` | 170 |
| `max_hp` | 95 |
| `block_cnt` | 41 |
| `respawn_time` | 26 |

其中**只有 `attack_speed` 有通用链路**（`ak_tactic/operator/attack_speed.py`
按黑板键统一读走，实测能天使「快速弹匣」`sources=('天赋「快速弹匣」+12',)`）。
其余五项**没有任何地方读**。

**⚠ 但千万不要照着这张表批量补——会同时引入两类错，而且都不报错：**

1. **重复计算**：星熊「特种作战策略」的 `def: 0.06` 是**发给全体重装的光环**，
   不是自身加成（她自己恰好是重装才吃到）。按"天赋 `def` 键 → 加到自己身上"
   批量补，她会被加**两遍**。
2. **把有条件的当成常驻**：焰狐龙梓兰「翔虫机动」的 `atk: 0.1` 要求
   「部署至**上次部署位置周围**」才生效——常驻补上去，她**整场**白拿 10% 攻击。

所以这张表的正确读法是：**它是一个待分类清单，不是一个待批处理清单**。
每条天赋的属性键都要先归到三类之一——**自身常驻**（如凯尔希「遗尘守望」）、
**发给别人的光环**（如星熊，已在 `TeamAura` 里）、**有条件的**（如翔虫机动，
该由触发条件驱动）——才谈得上接线。

**这一条同时解释了第三道筛子里那批"统计类天赋"报出来的欠账**：它们**不全**是
真欠账（星熊那条已经建了），但也**不全**是误报（凯尔希那条是真的没建）。
**逐条分类之前，任何一种批量改法都是错的。**



