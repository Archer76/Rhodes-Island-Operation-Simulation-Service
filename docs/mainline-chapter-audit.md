# 主线第 3～10 章：跨实现对拍与规格面逐章审计

> **建立者**：RIOS 主线第 3～10 章取数与对拍（`session-4db0d760-9af0-4ba6-8f5d-38044b9df14e`）
> **本文性质**：**手写文档**。文中数字都是**当次读数**，不是会自己更新的水位；
> 引用它们时**必须连仪器（exe 路径＋sha16）、对象（哪几关）与时刻一起引**。
> **逐章追加**：本文件按章推进，每章一节，边做边写。

### 推送台账

> sha 由登记会话（`session-96f8e658…`）在推送后告知，**本文件不自己编 sha**。
> 「时刻」一栏记的是命令 `git show -s --format=%cI <sha>` 读得的**提交时刻**——
> 本机没有「推送时刻」的记录源，所以**记的是哪个时刻就写在值里**，不把两者压成一个值。

| 节点 | 提交 | 提交时刻（`git show` 读得） | 内容 |
| --- | --- | --- | --- |
| 第 3 章 | `6a0bf16` | 2026-09-24 02:08:19 +0800 | 取数 108/108 落缓存 ＋ 第 3 章对拍与规格面读数（逐章审计开篇） |
| 第 4 章 | `a594962` | 2026-09-24 02:16:28 +0800 | 第 4 章 10/10 关、67 只敌人 ＋ 立起本推送台账 |
| 第 5 章 | `3720d75` | 2026-09-24 02:22:54 +0800 | 第 5 章 10/10 关、55 只敌人 ＋ 补第 4 章推送台账 |
| 第 6 章 | `f653db5` | 2026-09-24 02:27:49 +0800 | 第 6 章 15/15 关、76 只敌人 ＋「雪那一栏是零信息量的绿」 |
| 第 7 章 | `b65ac03` | 2026-09-24 02:46:53 +0800 | 第 7 章 16/16 关、84 只敌人 ＋ 记下「尺子被换过」（同一提交链里 `76307f8` 把冻结节点的尺子正式入库） |
| 第 8 章 | `0a81c41` | 2026-09-24 02:49:11 +0800 | 第 8 章 17/17 关、76 只敌人 ＋ 抓到一条「名字不是身份」 |
| 第 9 章 | `fb7510f` | 2026-09-24 02:53:16 +0800 | 第 9 章 17/17 关、86 只敌人 ＋ 第二个 prts 侧未命中 |

---

## 〇 · 一句话

第 3～10 章共 **108 关**，取数 **108/108 落缓存**（逐关核过文件在不在）。
**全 108 关已验**，且**不是逐章拼起来的**：最后用同一把尺子**一次性重跑 108 关**——
`108/108 关逐字段一致`、`593 只敌人逐字段一致`（见 §2.2）。
空计划 `buildspec`：**一百零八关 `mechanisms` 全 []、`unsupported` 0 条、`mechspec.blockers` 全 `<缺席>`**。
**哪几条结论站得住、哪几条只是「我没查」，见 §六 的清账。**

---

## 一 · 口径声明

### 1.1 范围与关数

* **对象**：`main_03-*` … `main_10-*`，即 `data/gamedata/_level_index.json` 里键形如
  `main_NN-XX` 的条目。
* **`#f#` 不算独立关**：索引里另有 `main_NN-XX#f#`（突袭档）条目，**与普通档共用同一个
  `data_path`**，故不计入本轮的关数。
* **现算核对**（python 直接扫索引，不是抄别处的表）：

| 章 | `main_NN-XX` 关数 | `main_NN-XX#f#` 条数（不计） |
| ---: | ---: | ---: |
| 3 | 8 | 8 |
| 4 | 10 | 10 |
| 5 | 10 | 10 |
| 6 | 15 | 13 |
| 7 | 16 | 16 |
| 8 | 17 | 16 |
| 9 | 17 | 17 |
| 10 | 15 | 0 |
| **合计** | **108** | 90 |

★ **本文的关数与别处的口径不同源，不许并列**：另一会话 2026-09-24 查出，
`tools/check_go_all.py` 印的「缓存可达的关卡 **320** 个」**不是关卡数**——
`#f#` 四星档与普通档**共用同一个 `data_path`**，所以 **320 个索引键只对应 159 个不同文件**。
本文的关数按 `main_NN-XX` 键数（**不含 `#f#`**）＝108。
⇒「108 关」「320 键」「159 文件」是**三个口径**，本条只声明它们不同源，**不做任何换算**。

### 1.2 仪器与时刻

| 项 | 值 |
| --- | --- |
| 引擎 | `out/acceptance/rios-sim-stage3.exe` |
| sha16 | `908E2D2EDCE63543` |
| 字节数 | 4,990,464 B |
| mtime | 2026-09-24 02:01:55 |
| 构建 | 当次 `cd rios-sim; go build -o out/acceptance/rios-sim-stage3.exe .`（rc=0，go1.26.0 windows/amd64） |
| 树 | `<仓库根>` |
| 树 HEAD | 第 3 章读数时 `c0800c191fd4e7a5f184248558bb199fcf3b409a`；<br>第 4 章读数时 `6a0bf16d3de38bcfb33883122af60fd51f429b59`；<br>第 5 章读数时 `7c3f5ab511d77dc4e07fefe4dba9de6744e5c084`（★ 不是本文件报的那一笔：本会话报 `a594962`，其间**别的会话**又落了一笔 `7c3f5ab`，所以 HEAD 已前进）；<br>第 6 章读数时 `3720d755e4725abacc138eeb3687ac6ef37a1176`（＝第 5 章那一笔，本文件报的与树 tip 一致）；<br>第 7 章读数时 `f653db53f16940685f3ef3782bae2c209882d378`（＝第 6 章那一笔；已核 `3720d75` 仍是 HEAD 的祖先，`git merge-base --is-ancestor` rc=0）；<br>第 8 章读数时 `b65ac036a5f30681fb3bd4f2bfca76c0c949d4a6`（＝第 7 章那一笔）；<br>第 9 章读数时 `0a81c41fe5615ccdd0b525f6a76ac09c6cdd4ed9`（＝第 8 章那一笔）；<br>第 10 章读数时 `fb7510f1780c55c51a7ac885e45c81a7519ac521`（＝第 9 章那一笔）；<br>**§2.2 那次全书重跑**时 `fb7510f1780c55c51a7ac885e45c81a7519ac521`（同上一行） |
| 工作区 | 受跟踪文件**无改动**；`git status --short` 只列 4 个别人的未跟踪文件（`docs/batch4-enemy-families.md`、`docs/kl-level-families.md`、`tools/opfamily_inventory.py`、`zz_tmp_book.py`）＋另两个（`docs/worklog.md`、`tools/wl.py`） |
| **尺子（对拍脚本）身份** | `tools/check_enemy_go.py`：content sha16 **`0713c5a62faf682a`**（10236 B，工作区干净，最后一次提交 `ec9ceab`）；<br>`tools/check_stage_go.py`：第 3～6 章读数用的是**已提交版**，第 7 章读数用的是**工作区未提交版** content sha16 **`3d4ae10458e076fb`**（12224 B）。★ 见下面那条 |

★ `RIOS_SIM_BIN` **每一趟都显式设**为本表那条路径；不设会静默落到工作树里的旧 exe 上（本仓为此栽过）。
★ **尺子被换过，已按纪律重估**：第 7 章那一趟跑完，输出里多了一行 `已比：1 关（传入 1 关）`——
说明 `tools/check_stage_go.py` 在我做第 3～6 章之后被**另一个会话改过**（`git status` 显示它是
` M`、未提交；改动是加「冻结基线」模式 `RIOS_GOLDEN=check` 与输入批次对账，119 行）。处置两条：
① 记下两个版本的 content sha16（上表）；
② **用换后的尺子把第 3～6 章那 43 关重跑一遍**：`43/43 关逐字段一致`、rc=0，**结论不变**。
（跑前跑后各读一次 sha16，都是 `3d4ae10458e076fb`，证明这把尺子在我这一趟里没被再改。）

### 1.4 尺子身份（谁量的）

| 尺子 | 用在哪几章 | content sha16 | 状态 |
| --- | --- | --- | --- |
| `tools/check_stage_go.py`（**已提交版**） | 第 3、4、5、6 章 | 未记（当时没取） | 事后不可复核 |
| `tools/check_stage_go.py`（**工作区版**） | 第 7 章 ＋ 第 3～6 章重跑 | `3d4ae10458e076fb` | 12224 B，工作区 ` M`（**他人的未提交改动**） |
| `tools/check_stage_go.py`（**已入库版**） | 第 8、9 章 | `3d4ae10458e076fb` | 12224 B，工作区**干净**（`76307f8` 把它正式收编；sha16 与上面那版**逐位相同**） |
| `tools/check_stage_go.py`（**第二个工作区版**） | 第 10 章 ＋ §2.2 全书重跑 | `f92c202f060088b1` | 11164 B，工作区 ` M`（**又一次他人的未提交改动**；比上一版**小了 1060 B**） |
| `tools/check_enemy_go.py`（**工作区版**） | 第 10 章 ＋ §2.2 全书重跑 | `5d6a32b5f195a1ac` | 17996 B，工作区 ` M`（**他人的未提交改动**；比第 3～9 章那版 10236 B **大了 7760 B**） |
| `tools/check_enemy_go.py`（第 3～9 章用过的版本） | 第 3～9 章 | `0713c5a62faf682a` | 10236 B，当时工作区干净 |

★ 第 3～6 章**第一次**跑时没记这把尺子的 sha，那几行的「谁量的」**现在不可复核**；
只有重跑那一趟（第 3～6 章 43/43）带得住尺子身份。按「复现不了就写现在不可复核」登记。

★★ **尺子一共被换过两次，两次都留了痕**（这是本文最该被复用的处置）：
1. 第 6 章之后：`check_stage_go.py` 被换成工作区版（`3d4ae10458e076fb`）⇒ 我用它**把第 3～6 章重跑**（43/43）。
2. 第 9 章之后：**两把尺子同时**被换成新的工作区版（stage `f92c202f060088b1`、enemy `5d6a32b5f195a1ac`）
   ⇒ 我用它们**把全书 108 关一次性重跑**（见 §2.2），而不是继续逐章比。
两次重跑都**跑前跑后各读一次 sha16**（各自相同），证明那一趟里尺子没被再改。

### 1.3 命令与请求形状

```powershell
# 取数（逐关串行；rc 不作唯一判据，另核缓存文件在不在）
python -X utf8 -m ak_tactic stage <levelId>

# 对拍（RIOS_SIM_BIN 显式设）
$env:RIOS_SIM_BIN = "out/acceptance/rios-sim-stage3.exe"
python -X utf8 tools\check_stage_go.py <该章全部 levelId>
python -X utf8 tools\check_enemy_go.py <该章全部 levelId>

# 规格面：空计划 buildspec（走 Go 的一行一行协议）
#   请求体：顶层 cmd 与 level，其余参数放**外层对象 spec** 里
{"id": 1, "cmd": "buildspec", "level": "<levelId>"}
```

★ **取数这一步 rc 不作唯一判据**：`stage` 命令在「敌人有字段缺失」时会**打印末尾崩**
（`NoneType.__format__`，rc=1）而**关卡已经落到缓存**。本轮 108 关**没有一关 rc≠0**，
但仍然**逐关核了缓存文件**（见 §四）。
★ **buildspec 的参数位置是个坑**：把 `plan` 之类放到顶层会被**静默忽略**——
量出来是「机制没生效」，不是「没有机制」。本轮是**空计划**，请求里**根本不带 `spec` 键**。

---

## 二 · 逐章对拍读数

| 章 | 关数 | 缓存命中 | `check_stage_go.py`（地图／路线／出怪） | `check_enemy_go.py`（敌人） | 未移植字段 | rc |
| ---: | ---: | ---: | --- | --- | --- | ---: |
| 3 | 8 | 8/8 | **8/8 关逐字段一致** | **68 只敌人逐字段一致** | **无** | 0 |
| 4 | 10 | 10/10 | **10/10 关逐字段一致** | **67 只敌人逐字段一致** | **无** | 0 |
| 5 | 10 | 10/10 | **10/10 关逐字段一致** | **55 只敌人逐字段一致** | **无** | 0 |
| 6 | 15 | 15/15 | **15/15 关逐字段一致** | **76 只敌人逐字段一致** | **无** | 0 |
| 7 | 16 | 16/16 | **16/16 关逐字段一致** | **84 只敌人逐字段一致** | **无** | 0 |
| 8 | 17 | 17/17 | **17/17 关逐字段一致** | **76 只敌人逐字段一致** | **无** | 0 |
| 9 | 17 | 17/17 | **17/17 关逐字段一致** | **86 只敌人逐字段一致** | **无** | 0 |
| 10 | 15 | 15/15 | **15/15 关逐字段一致** | **81 只敌人逐字段一致** | **无** | 0 |

**正负对照**（判据本身有没有分辨力）：

| 对照 | 命令 | 期望 | 实测 |
| --- | --- | --- | --- |
| 正对照（已知答案） | `check_stage_go.py main_02-01 main_02-02` | 2/2 一致、rc=0 | ✓ 一致、rc=0 |
| 反证（地图键人为改动） | `check_stage_go.py main_03-03 --mutate` | 判红、rc=0 | ✓ 1 处不一致、rc=0 |
| 反证（敌人合成不一致） | `check_enemy_go.py main_03-03 --mutate` | 判红、rc=0 | ✓ 10 只全报、rc=0 |
| 反证（第 4 章同两条） | `check_stage_go.py main_04-04 --mutate`<br>`check_enemy_go.py main_04-04 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 5 只全报（rc=0） |
| 反证（第 5 章同两条） | `check_stage_go.py main_05-06 --mutate`<br>`check_enemy_go.py main_05-06 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 7 只全报（rc=0） |
| 反证（第 6 章同两条） | `check_stage_go.py main_06-06 --mutate`<br>`check_enemy_go.py main_06-06 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 5 只全报（rc=0） |
| 反证（第 7 章同两条） | `check_stage_go.py main_07-03 --mutate`<br>`check_enemy_go.py main_07-03 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 2 只全报（rc=0） |
| 反证（第 8 章同两条） | `check_stage_go.py main_08-05 --mutate`<br>`check_enemy_go.py main_08-05 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 4 只全报（rc=0） |
| 反证（第 9 章同两条） | `check_stage_go.py main_09-14 --mutate`<br>`check_enemy_go.py main_09-14 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 8 只全报（rc=0） |
| 反证（第 10 章同两条） | `check_stage_go.py main_10-04 --mutate`<br>`check_enemy_go.py main_10-04 --mutate` | 判红、rc=0 | ✓ 1 处不一致（rc=0）<br>✓ 6 只全报（rc=0） |

### 2.2 全书**一次性**重跑（不是逐章相加）

**为什么要它**：第 9 章之后**两把尺子同时**被换过（见 §1.4）。逐章那几个数来自**不同版本的尺子**，
拼起来不是一个读数。所以用**最后这两把**把 108 关一次跑完：

| 尺子（content sha16） | 命令 | 关数 | 用时 | rc | 结论 |
| --- | --- | ---: | ---: | ---: | --- |
| `check_stage_go.py` `f92c202f060088b1` | 传 108 个 `main_NN-XX` | 108 | 6.7 s | 0 | **108 / 108 关逐字段一致**（失败行 0 处） |
| `check_enemy_go.py` `5d6a32b5f195a1ac` | 同 108 关 | 108 | 53.2 s | 0 | **593 只敌人逐字段一致**（失败行 0 处） |

**仪器同 §1.2**（`rios-sim-stage3.exe`，sha16 `908E2D2EDCE63543`；`RIOS_SIM_BIN` 显式设）。
跑前与跑后各读一次尺子 sha16，各自**相同**；树 HEAD 见 §1.2。

★ 「593」这个数是**出怪项**数（逐关按 `(id, level)` 去重后相加），**不是**关卡数、也**不是**
不同敌人的种数（去重后更少，见 §五）。三个数不同源，**不许并列**。
★ 这一条的意义：它把「逐章一致」升级成「**同一把尺子下的全量一致**」——
用不同版本的尺子逐章拼出来的 108/108，与一次跑出来的 108/108 **不是同一个强度**。

### 2.1 用时（同一趟顺序执行，不是并行；给整闸性能排查当样本）

**仪器同上**（`rios-sim-stage3.exe`，sha16 `908E2D2EDCE63543`）。
计时用 `[System.Diagnostics.Stopwatch]`，**两条命令一条接一条**跑，避免并行互相污染。

| 命令 | 关数 | 用时 | 折算 |
| --- | ---: | ---: | ---: |
| `check_stage_go.py`（第 3 章，计时对照） | 8 | 0.6 s | 0.075 s/关 |
| `check_enemy_go.py`（第 3 章，计时对照） | 8 | 3.8 s | 0.475 s/关 |
| `check_stage_go.py`（第 6 章） | 15 | 0.9 s | 0.060 s/关 |
| `check_enemy_go.py`（第 6 章） | 15 | 6.3 s | 0.420 s/关 |
| `check_stage_go.py`（第 7 章，**换尺后**） | 16 | 1.2 s | 0.075 s/关 |
| `check_enemy_go.py`（第 7 章） | 16 | 8.2 s | 0.513 s/关 |
| `check_stage_go.py`（第 3～6 章合计，**换尺后**） | 43 | 2.6 s | 0.060 s/关 |
| `check_stage_go.py`（第 8 章，**已入库尺**） | 17 | 1.1 s | 0.065 s/关 |
| `check_enemy_go.py`（第 8 章） | 17 | 8.0 s | 0.471 s/关 |
| `check_stage_go.py`（第 9 章） | 17 | 1.1 s | 0.065 s/关 |
| `check_enemy_go.py`（第 9 章） | 17 | 7.9 s | 0.465 s/关 |
| `check_stage_go.py`（第 10 章，**又一次换尺后**） | 15 | 1.1 s | 0.073 s/关 |
| `check_enemy_go.py`（第 10 章，**又一次换尺后**） | 15 | 8.1 s | 0.540 s/关 |
| `check_stage_go.py`（**全书 108 关，一次性**，见 §2.2） | 108 | 6.7 s | 0.062 s/关 |
| `check_enemy_go.py`（**全书 108 关，一次性**） | 108 | 53.2 s | 0.493 s/关 |

★ **两把尺都随关数近似线性**：8 关→15 关（关数 ×1.875）时，
`check_stage` 0.6→0.9 s（×1.5）、`check_enemy` 3.8→6.3 s（×1.66），**没有超线性**。
⇒ 用本表折算，320 关约 `check_stage` 19 s、`check_enemy` 134 s——与「320 关 stage 0.3 min、
enemy 2.1 min」同量级。**本样本支持「整闸慢不在随关数增长的那一项上」。**
★ 第 7 章两行**换了尺子**（见 §1.4）：`check_stage` 的 0.075 s/关 与换尺后的
第 3～6 章 0.060 s/关相比略高，但仍同一量级、**没有跳变**；`check_enemy` 的 0.513 s/关
与前面的 0.420～0.475 s/关同量级。**到第 7 章为止没有出现用时跳变。**
★ 第 8 章（17 关）**文件明显更大**——十七关缓存合计 **1,103,673 B**（`main_08-17` 单关 113 KB），
比第 7 章的十六关大不少；而用时是 stage 0.065 s/关、enemy 0.471 s/关，
**与前面的量级一样、没有跳变** ⇒ 本样本进一步支持「用时主要挂在**关数**上，
而不是挂在关卡文件大小或出怪条数上」。
★ **第 9～10 章与全书重跑都没有跳变**：stage 0.065／0.073／**0.062** s/关、
enemy 0.465／0.540／**0.493** s/关。全书 108 关那两行是最强的一条：
关数 ×6.35（17→108）而用时 ×6.1／×6.7，**仍是线性**。
⇒ 到第 10 章为止，**八个样本点里没有一次用时跳变**；这支持「整闸慢不在随关数增长的那一项上」。

---

## 三 · 逐章规格面（空计划 `buildspec`）

**收哪几栏**：`mechanisms`（挂了哪些机制）、`unsupported`（理由条数）、
`scanned` 里的 `mechspec.blockers` / `mechspec.env_rune` / `gate_goal_open` / `gate_highland_open`，
另记 `missing_keys` 与 `gated_keys`。

| 章 | 关数 | 挂机制的关 | `mechanisms` | `unsupported`（条数／内容） | `mechspec.blockers` | `mechspec.env_rune` | `gate_goal_open` | `gate_highland_open` |
| ---: | ---: | ---: | --- | --- | --- | --- | --- | --- |
| 3 | 8 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 4 | 10 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 5 | 10 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 6 | 15 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 7 | 16 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 8 | 17 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 9 | 17 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |
| 10 | 15 | 0 | 全 `[]` | 0 条 | 全 `<缺席>` | 全 0 | 全 0 | 全 0 |

### 3.1 三态纪律：`<缺席>` 与 `0` 是两回事

* `mechspec.blockers` **只在田地分支跑起来时才进 `scanned`**（`mechspec.go` 的
  `scanned["blockers"] = blockers` 在 `buildMechFarmland` 里）。
  第 3～6 章四十三关的 `mechspec.farmland` 都是 0 ⇒ 田地分支没跑 ⇒ 这一栏**根本不存在**。
  记成 `0` 会把「这条线没被行使」说成「这条线量到 0」。
* 反过来，`mechspec.env_rune` 是**每条路都写**的（0 或 1），所以它是真读数。
* **第一次出现要在表里看得出来**：到第 6 章为止，**六章的 `mechspec.blockers` 全是 `<缺席>`**
  （`mechspec.farmland` 逐关全 0）。哪天某一关的 `farmland` 非 0 ⇒ 这一栏会**第一次变成真读数**，
  那时它必须与 `<缺席>` 逐关可分辨，不能和现在这种「没行使」混在一栏里。
  ★ 第 6 章的核法不止看值，还**逐关核了键的存在性**：
  `"mechspec.blockers" in scanned` 十五关**全是 `False`**——「不存在」是量出来的，不是从空值推的。
  ★ 第 7 章同样核了键存在性：十六关**全是 `False`**。到第 7 章为止七章一致。
  ★ 第 8 章：十七关**全是 `False`**，`mechspec.farmland` 全 0、`goal_open`／`highland_open` 全 0、
  `missing_keys`／`gated_keys` 全空——**这一章没有任何一栏翻真**。到第 8 章为止八章一致。
  ★ 第 9 章：十七关同样**全是 `False`**，逐栏与第 8 章相同、**没有任何一栏翻真**。九章一致。
  ★ 第 10 章：十五关**全是 `False`**，逐栏相同、**没有任何一栏翻真**。
  ⇒ **全 108 关（八章）从头到尾 `mechspec.blockers` 一次都没出现过**；
  这条跟踪项**以「从未出现」收口**，不是「出现过又被漏掉」。

### 3.2 这一面**看不见什么**（沉默不等于没有）

* **空计划下 `unsupported` 的可见范围**：第 3 章八关与第 4 章十关的
  `unsupported.spawn` **逐关都非 0**（出怪侧那条线确实跑了：第 4 章十关是
  40/24/29/26/16/33/37/19/35/19），但**理由条数恒 0**——
  这不是「这一关没有未建模机制」，而是「**这一局的排程里没有东西触发那几条线**」。
* **装置那一组对照（本面的零分辨力因此是量出来的，不是推的）**：
  按 `docs/mainline-stages.md` §九 的表，**第 3 章 16 行里 4 行带装置、装置条目 6 个**，
  **第 4 章 20 行里 0 行带装置、装置条目 0**，
  **第 5 章 20 行里 6 行带装置、装置条目 34 个**，
  **第 6 章 28 行里 7 行带装置、装置条目 17 个**。
  这四章在本节的每一栏读数**完全一样**（`unsupported` 都是 0 条、`blockers` 都缺席）
  ⇒ **这一面对「有没有装置」零分辨力**，沉默不构成证据；
  而且**装置条目从 0 → 6 → 17 → 34** 也一样看不出来。
* **雪那一栏在空计划下是「零信息量的绿」（第 6 章查出来的）**：雪**不是关卡数据里的机制**，
  它由**干员天赋**在**部署那一刻**创建——`ak_tactic/simgo/spec.py::snow_spec` 逐条读
  `inp.deployments` 的 `find_snow(op.talents)`（源头是 `battle/sim.py:3396`）。
  本轮是**空计划**、`deployments` 为空 ⇒ `snow_spec` 恒返回空 ⇒
  `mechanisms` 里永远不会有 `snow.field`，`snow_fields`／`mechspec.snow_input` **必然 0**。
  ⇒ 这一栏的 0 **不携带信息**，不能用来回答「这一关有没有雪」。
  ★ 第 6 章正是雪主题的关群（雪怪小队／霜星等敌人），它照样报 0。
  ★ 另外核过**原始关卡 JSON**：第 6 章十五关里 7 关出现过 `snow` 字样，
  **全部是 `bgmEvent: "snowmonster"`（BGM 名），不是机制字段**，`environmentSe…` 为 null
  ⇒ 就本章而言，「0」与数据也是一致的——但这属于**另一次取证**，不是那一栏自己给的。
* **装置那条线在 `unported` 里**：Go 侧没有装置层，
  `关卡装置 ×N` 这条线具名列在 `unported`（第 3～6 章四十三关都是同 10 条）。
  ⇒ 本轮这一栏只能当「有没有未建模机制的**字段驱动**下界」，不能当「没有缺口」的证明。
* **行为面**由另一个会话在扫，与本文件分工不同。

第 3～6 章的 `unported`（四十三关逐字相同，10 条）：

```
spawns[].p3r_armed：total_attack 要装置层，Go 没有 ⇒ 恒传 false
spawns: species_provider
spawns: total_attack
unsupported: skill_name
unsupported: snow_talent
unsupported: devices
unsupported: total_attack
unsupported: death_token
unsupported: operator_side
mechspec: snow.field
```

### 3.3 敌人侧闸门线**有几条**、它们的输入**量到多少**（第 7 章查的）

第 7 章引用了爱国者、游击队传令兵这类带机制的敌人，而本节照旧报 0 条 `unsupported`。
**「沉默」必须先证明不是「看不见」**，所以我去把这一面的线数清楚、再把输入量出来：

* **线有几条**：Python 侧 `_enemy_reasons`（`simgo/spec.py:450`）今天只剩 **3 条**——
  `awake_value`（Go 已搬）、`modes`（Go 已搬）、`hp_drain_per_sec`。
  其余机制族（`skill_atk_*` / `passive_pollut` / `reborn_*` / `phit_*` / `pm2_*`）
  **已按设计从闸门表里移除**（`spec.py:465-479` 写明理由：它们已接线进 Go）。
* **输入量到多少**：第 3～10 章 108 关、**593 个出怪项**逐项量：
  `awake_value != 0` 的 **0** 个、`modes` 非空的 **0** 个；
  `hp_drain_per_sec` 这一栏在本范围是**空洞的 0**——`EnemyStats` 上**根本没有这个字段**
  （它是干员字段），所以那不是「量到 0」，是**没法量**。
* ⇒ 本面在这一范围的沉默是「**593 个输入逐项查过、没有触发**」，
  与「看不见」是两回事。**但边界要写清**：本面**不回答**「那些已接线的机制族在 Go 里算得对不对」
  ——那是行为面的事，由另一个会话扫。

★ **我自己的两条假读数（同一条尺子陷阱踩了两次，记下来）**：

1. 第一版计数用「**真值非空即机制在场**」，于是 `reborn_hp_ratio`（数据类默认 **1.0**）与
   `aura_hit_radius`（默认 **0.5**）被报成 **593/593 只在场**——那是**尺子错**，
   正是 `tools/check_enemy_go.py` 文件头写过的那一个坑（它的 `is_default()` 就是为它而写的）。
2. 改用「与数据类默认值比」之后，`reborn_summons`（默认 **空元组 `()`**）被 Go 给的
   **空列表 `[]`** 判成非默认 ⇒ **又是 593/593**。空元组与空列表**语义相同**，`==` 却不等。
3. ⇒ 结论：**「非默认即机制在场」这类计数，本范围我没做出来**，不写进结论。
   上面那三条线的 0 是**直接读原始值**（不经任何默认值比较）得到的，可以引。

---

## 四 · 取不到的关与原因

| 章 | 取不到的关 | 原因 |
| ---: | --- | --- |
| 3 | **无**（8/8 取到） | — |
| 4 | **无**（10/10 取到） | — |
| 5 | **无**（10/10 取到） | — |
| 6 | **无**（15/15 取到） | — |
| 7 | **无**（16/16 取到） | — |
| 8 | **无**（17/17 取到） | — |
| 9 | **无**（17/17 取到） | — |
| 10 | **无**（15/15 取到） | — |

逐关缓存核对（`data/gamedata/map.ark-nights.com/levels/<data_path>`；
字节按关号顺序排，读法与关号一一对应）：

| 章 | 关数 | 逐关字节 |
| ---: | ---: | --- |
| 3 | 8 | 38,894 / 53,256 / 77,855 / 56,547 / 63,086 / 33,309 / 49,001 / 53,942 |
| 4 | 10 | 52,237 / 45,817 / 38,680 / 44,437 / 42,085 / 58,694 / 48,581 / 45,365 / 57,287 / 31,443 |
| 5 | 10 | 40,945 / 81,047 / 44,436 / 44,484 / 71,457 / 83,134 / 60,254 / 83,979 / 43,052 / 58,435 |
| 6 | 15 | 38,327 / 56,551 / 30,768 / 48,257 / 50,298 / 65,504 / 40,911 / 49,814 / 47,373 / 59,924 / 54,069 / 46,302 / 59,088 / 38,764 / 18,665 |
| 7 | 16 | 47,407 / 40,504 / 66,153 / 38,550 / 39,079 / 39,175 / 59,646 / 35,309 / 37,933 / 44,927 / 52,279 / 47,249 / 44,054 / 73,025 / 44,325 / 66,369 |
| 8 | 17 | 45,887 / 37,455 / 83,740 / 52,502 / 89,907 / 64,011 / 50,904 / 81,751 / 70,256 / 61,337 / 41,745 / 44,089 / 70,023 / 51,436 / 70,582 / 74,567 / 113,481（合计 1,103,673 B） |
| 9 | 17 | 49,909 / 45,991 / 85,126 / 56,975 / 86,547 / 60,260 / 65,552 / 35,593 / 54,369 / 46,451 / 54,852 / 45,846 / 33,004 / 40,629 / 57,820 / 43,175 / 53,013（合计 915,112 B） |
| 10 | 15 | 43,935 / 53,396 / 60,240 / 122,415 / 68,126 / 53,211 / 30,275 / 34,950 / 52,013 / 68,711 / 85,496 / 30,488 / 56,287 / 69,516 / 59,385（合计 888,444 B；`main_10-04` 单关 122,415 B 是全范围最大的一份） |

---

## 五 · 每章一行：本关群引用的敌人在两侧数据源上的规模

**两侧是什么**（都是**本地**数据，不是现去网上取）：

* **gamedata 侧**＝`data/gamedata/map.ark-nights.com/levels/enemydata/enemy_database.json`
  （**2154** 条 `Key`）。这是 Python 权威侧 `EnemyLibrary` 的属性库。
* **prts.wiki 侧**＝`data/enemydb.sqlite`，其 `meta.enemy_source` 写明来路是
  **prts.wiki 分类:敌人**；规模 **1807 行敌人页 / 2114 个级别档**。
  ★ 这 1807 行里**去重页名只有 1804 个**（同名多行）⇒ 本节的「命中分母」按**去重名**算，
  两个数**不同源、不许混用**（第 3 章那次先印 1807、第 4 章那次先印 1804，就是这两个口径）。
  匹配按**页面名＝敌人名**。

**口径**：章内引用的敌人取 Go `enemies` 命令逐关的 `refs`，
逐关按 `(id, level)` 去重后**跨关取联合**。

★ **八章合计（全书 108 关取联合去重，不是各行相加）**：**149 个 `(id, level)` 组合**、
**去重 id 144 个**、**去重名字 143 个**；gamedata 侧 **144/144**、
prts 侧 **141/143**（未命中两只：「持盾刀兵」「深池方阵步兵」——正是第 3/4 章与第 9 章各一的那两只）。
⇒ 合计行与各章那两列**不相同源**：各章列是**章内**去重、合计行是**全书**去重，
**不许把 8 个章数相加当合计**（相加＝不同分母硬加）。
★ 同一份数据里「144 个 id」与「143 个名字」**也不相等**——差的那一个是
「斗士塔露拉」的两个 id 同名（见上面第 8 章那条），**名字不是身份**。
★ **本文在这里犯过一次错，留痕**：我**先按印象把这三行写成了 88/87/88**，
然后才去量，实测是 144/143/144（prts 侧 141/143）。**先写结论后补动作**——
这次因为写完立刻去量并改回来了，没留在成品里；记在这里是为了让下一个读的人知道
**这三行是量出来的，不是推的**。

| 章 | 引用（去重 id） | 引用（去重 名字） | gamedata 侧命中 | prts.wiki 侧命中 | 两侧都不命中的 |
| ---: | ---: | ---: | --- | --- | --- |
| 3 | 31（32 个 id＋档 组合） | 31 | **31/31** | **30/31** | prts 侧缺「持盾刀兵」 |
| 4 | 25（27 个 id＋档 组合） | 25 | **25/25** | **24/25** | prts 侧缺「持盾刀兵」 |
| 5 | 24（25 个 id＋档 组合） | 24 | **24/24** | **24/24** | 无（本章不引用「持盾刀兵」） |
| 6 | 28（28 个 id＋档 组合） | 28 | **28/28** | **28/28** | 无 |
| 7 | 28（28 个 id＋档 组合） | 28 | **28/28** | **28/28** | 无 |
| 8 | 25（25 个 id＋档 组合） | **24** | **25/25**（按 id） | **24/24**（按名字） | 无 |
| 9 | 14（14 个 id＋档 组合） | 14 | **14/14**（按 id） | **13/14**（按名字） | prts 侧缺「深池方阵步兵」 |
| 10 | 23（23 个 id＋档 组合） | 23 | **23/23**（按 id） | **23/23**（按名字） | 无 |

★ **第 8 章两栏的分母不相等（25 与 24），原因已取证**：两只**不同的 id 同名**——
`enemy_3002_ftrtal` 与 `enemy_3002_ftrtal_s` 的显示名都是「斗士塔露拉」，
属性也逐项相同（HP 15000 / 攻 600 / 防 350），分别出现在 `main_08-13` 与 `main_08-15`。
⇒ **按名字匹配会把两只压成一只**（本仓「名字不是身份」那条），
所以 gamedata 侧按 **id** 命中 25/25、prts 侧按 **名字** 命中 24/24。
两个数**不同源、不许并列当同一个分母用**。

第 3、4 章的唯一 prts 侧未命中是**同一只**：`enemy_1029_shdsbr`（显示名「持盾刀兵」）。
**查询本身已证明跑成功**（每次都在同一连接里做正对照）：`name='潜伏者'` 命中，
且 `name like '%刀兵%'` 只回「萨卡兹刀兵」、`name like '%持盾%'` 回四条但都不是它
⇒ 是**该名字在 enemydb 里查无此页**，不是尺子没跑。

★ **第 9 章出现第二个 prts 侧未命中**：`enemy_1169_duphlx`（显示名「深池方阵步兵」）。
取证四条（**都在同一连接里**，且带两个正对照）：
`name='深池方阵步兵'` **空**、`page='深池方阵步兵'` **空**（页名与显示名两条都查过）、
`like '%方阵%'` 只回「深池方阵战士」「深池方阵指挥官」、异体字 `%方陣%` 也空；
正对照 `name='深池侦察犬'` 与 `name='蔓德拉'` **都命中**（同章引用的另外两只）。
⇒ 该名字在 enemydb 里**查无此页**。
★★ **但另有一条独立观察，必须分槽写**：字符串「方阵步兵」**出现在「深池方阵战士」页的
`raw_wikitext` 正文里**（`raw_wikitext like '%方阵步兵%'` 命中该页）。
即：**「没有这个页」与「正文提到过这个词」是两件事**，不压成一个值——
前者是本节的读数（按页名匹配没有），后者是**另一次取证**（正文级），
它可能意味着 wiki 用别名收录、也可能只是叙述用词，**本文件不作断言**。
★ 第 5、6、7 章**都不引用这只**（第 5 章是宿主／法术大师／暴鸰那一批，第 6 章是雪怪小队、
霜星、狂暴宿主那一批，第 7 章是游击队／爱国者那一批），所以这三章是**全中**——
这一栏的「缺」是**跟着关卡引用走的**，不是库里的常量。
★ 第 6 章这 28 个名字里含**带全角引号与后缀的那种**（如「霜星，“冬痕”」、「霜星S」），
**逐条都命中了**；这不是巧合，是说明匹配键**没有对引号或非 ASCII 做过滤**。

---

## 六 · 清账：哪些还挂着、各自为什么不闭合

**读法**（三态，不许压成一个值）：

* **闭合**＝本轮已给出读数，且有**第二来源或反证**撑着；
* **可闭合（附补法）**＝本轮没做，但**出口存在**，补法写在栏里；
* **不闭合**＝**没有出口**（不是「我没查」，而是以现有手段闭合不了），写清为什么。

| # | 项 | 状态 | 为什么不闭合 / 补法 |
| ---: | --- | --- | --- |
| 1 | **`#f#` 突袭档 90 条**（第 3～10 章） | **可闭合** | 未纳入。补法：键要用 `stageId`/`hard_level_id` 而不是 `levelId`——突袭与普通档**共用同一个 `data_path`**，难度只住 `LevelEntry.difficulty`，**必须显式下传** `inputs._env_difficulty`，否则「不接线会静默逐位相同」。本轮 108 关**一条都不含突袭**。 |
| 2 | **装置面** | **可闭合** | 空计划 `buildspec` 对装置**零分辨力**（七章装置条目 0／6／34／17／22／90／60，读数**逐栏一样**，见 §3.2）。补法：装置清单不从这一面取，走关卡文件的 `predefines`/`hardPredefines`（`tools/mainline_devices.py` 已有 267 关的产物可当输入）。本轮**未取**「哪几关带装置、各几个」。 |
| 3 | **敌人两侧规模：prts 侧只按名字匹配** | **部分闭合** | 2 个未命中（持盾刀兵、深池方阵步兵）都做了**四条取证＋正对照**（名字／页名／同族 like／异体字）⇒ 这**两个实例**是证住的。但「名字匹配本身会不会造出**假命中/假未命中**」**没有**独立于名字的第二把尺子；补法：接一份敌人在两侧的 **id 级映射**（gamedata `Key` ↔ prts 页），再重跑本栏。 |
| 4 | **树 HEAD 跨章不同源** | **不闭合** | 同一棵树上另有会话在提交（第 5 章读数时 HEAD 已不是第 4 章那一笔）。**这是环境事实，不是缺陷**，没有出口。缓解：每章记 HEAD（§1.2），跨章读数**不构成同一次快照**。 |
| 5 | **雪那一栏的 0** | **可闭合** | 空计划下**必然 0**（`snow_spec` 读 `inp.deployments`，本轮为空）⇒ 0 **不携带信息**。补法：拿一份**带排程**的输入再问一次 `buildspec`，看 `mechanisms` 是否含 `snow.field`；本轮**未做**。 |
| 6 | **尺子被换过两次** | **闭合** | 两次都取了两版 content sha16，并**用换后的尺子重跑**（第一次：第 3～6 章 43/43；第二次：**全书 108 关 108/108＋593 只**，见 §2.2）。这一点已闭合，且读数带得住身份。 |
| 7 | **第 3～6 章第一次跑没记尺子 sha** | **不闭合（已部分补救）** | 那几行的「谁量的」**当时没记、事后不可复核**——这一条**没有出口**，只能这么写。补救：§2.2 用新尺子给出的全量读数**是可复核的那一份**。 |
| 8 | **「非默认即机制在场」计数** | **可闭合** | 本轮**没做出来**（两次被尺子骗，见 §3.3）。补法：用默认值比较时要先把 `()`/`[]` 归一，且只对**确有默认值**的字段判定；做不出就**不写结论**——本轮取的是这一条。 |
| 9 | **装置条目数引的是别人的表** | **未核** | §3.2 里第 5～10 章的装置数引自 `docs/mainline-stages.md` §九（`tools/mainline_devices.py` 的产物），**不是本轮现算**。引用时带了来源，但**我没有复核那份产物**。 |
| 10 | **敌人「机制键数」与「引用敌人数」** | **不作比较** | 两个量不同源（前者是另一会话按敌人定义数的、后者是本文件按出怪引用数的），**不许并列**。这不是缺口，是口径声明。 |

### 6.1 本轮**没有覆盖**的面（具名，不装成 0）

1. **行为面**（这些机制在 Go 里**算得对不对**）：由另一个会话扫，本文件**不回答**。见 §3.2／§3.3。
2. **敌人技能／天赋黑板的语义**：`check_enemy_go.py` 只比**两侧同形**（黑板逐键、技能逐条），
   **不比语义**；「两边都读了同一份、都读错成一样」这种**共错**它看不见。
3. **装置运行期**、**敌人侧已接线机制族的实现正确性**、**突袭档难度**：均未纳入。

---

## 七 · 口径总表（★ 跨行不可比）

本文出现过**十几个「数」**。它们**不是同一个口径的不同写法**，而是**各自一个口径**——
把它们相加、相比、或拿一个去替另一个，都会得出错结论。逐行标出**定义、值、来源**：

| # | 口径 | 定义（怎么数的） | 值 | 来源（谁给的） |
| ---: | --- | --- | ---: | --- |
| 1 | **本文的「关」** | `_level_index.json` 里键形如 `main_NN-XX` 的条目，**不含 `#f#`** | **108** | 现算（本会话） |
| 2 | 其中各章 | 3/4/5/6/7/8/9/10 章 | 8/10/10/15/16/17/17/15 | 现算 |
| 3 | **`#f#` 条数**（不计） | 键 `main_NN-XX#f#` | 90 | 现算 |
| 4 | 总闸的「缓存可达关卡」 | **含 `#f#` 的索引键** | **320** | `tools/check_go_all.py` 输出（另一会话 2026-09-24 查出它不是关卡数） |
| 5 | **不同 `data_path` 文件数** | `#f#` 与普通档**共用**文件，去重后 | **159** | 同上（另一会话） |
| 6 | **出怪项数（全书）** | 逐关按 `(id, level)` 去重后**相加** | **593** | `check_enemy_go.py` 的结论行（§2.2） |
| 7 | 出怪项 `(id, level)` **组合数** | 全书联合**去重** | **149** | 现算 |
| 8 | 不同敌人 **id**（全书） | 同上按 id 去重 | **144** | 现算 |
| 9 | 不同敌人 **名字**（全书） | 同上按显示名去重 | **143** | 现算（比 144 少 1：两只 id「斗士塔露拉」同名） |
| 10 | gamedata 敌人属性库 | `enemy_database.json` 的 `Key` 条数 | **2154** | 现算 |
| 11 | prts 侧敌人页（**行数**） | `enemydb.sqlite` 的 `enemy` 表行数 | **1807** | 现算 |
| 12 | prts 侧去重**页名** | 同上 `distinct name` | **1804** | 现算（与第 11 行**不同源**） |
| 13 | prts 侧**级别档** | `enemy_level` 表行数 | **2114** | 现算 |
| 14 | 装置条目（**引用，未复核**） | 各章「带装置行／装置条目」 | 6／0／34／22／17／90／60 | `docs/mainline-stages.md` §九（`tools/mainline_devices.py` 的产物） |
| 15 | `C_main`（**别文档口径**） | `main_` 行按 `code` 去重 | **267** | `docs/mainline-stages.md` §一 |

**仪器身份**（与上面那些「数」不同源，单独一组）：

| 项 | 值 |
| --- | --- |
| Go 引擎 | `out/acceptance/rios-sim-stage3.exe`，sha16 **`908E2D2EDCE63543`**，4,990,464 B，mtime 2026-09-24 02:01:55 |
| 尺子 `check_stage_go.py` | 用过三版：未记 sha（第 3～6 章首跑）／`3d4ae10458e076fb`（第 7～9 章）／**`f92c202f060088b1`（第 10 章与全书重跑）** |
| 尺子 `check_enemy_go.py` | 用过两版：`0713c5a62faf682a`（第 3～9 章）／**`5d6a32b5f195a1ac`（第 10 章与全书重跑）** |

**三条使用规矩**（本文自己遵守的）：

1. **跨行不可比**：第 1～15 行**不许相加、不许互替**。例：「108 关」与「320 键」与「159 文件」
   是三个口径；「593 出怪项」不是关卡数、也不是敌人种数。
2. **每个读数带三件**：仪器（上表）＋对象（哪几关）＋时刻（§1.2 的 HEAD）。
3. **换了尺子就把读数重估**：本文发生过两次，两次都重跑（§1.4／§2.2）。
