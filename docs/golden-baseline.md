# 冻结基线：逐套状态与主判据

**这份文件不干活，它只登记状态。** 状态本身**现算**，不由本文件声明——
本文件里的任何一个数，都可以用下面那条命令重新算一遍，算不出来就是本文件过期。

## 0 · 主判据（现算，不是登记表）

```
python -X utf8 tools\freeze_baseline.py --status
```

`2026-09-24 02:5x` 实测（仪器 `out\acceptance\rios-sim-stage3.exe`，sha256(16)=`908e2d2edce63543`）：

```
✓ 范围     冻结rc=0   默认档=绿        基线=有  0.2s
✓ 分类     冻结rc=0   默认档=绿        基线=有  0.2s
✓ 生命上限   冻结rc=0   默认档=绿        基线=有  0.1s
✓ 攻击间隔   冻结rc=0   默认档=绿        基线=有  0.1s
★ **冻结模式下跑得通：4 / 27 套**
有基线的 4 套里，两种模式**都给绿**（＝这一套的基线录对了）：4 / 4
```

★ **分母是活的**：本次会话开始时 SUITE 是 26 套，做的过程中另一个会话往
`check_go_all.py` 登记了第 27 套（**敌方机制**），`--status` 当场就跟着变成 27
——**本工具不抄套表、不写死 26**（写死的分母会在别人加一套之后变成假话）。
`--status` 在没全绿时 **rc=1**（所以它是可以接进门的判据，不是一张给人看的表）。
**把这一刀接进验收门不在本会话的产权范围内**（`tools/check_go_all.py` 归上级会话），
本会话只保证它**可接**：一条命令、一个退出码、一个现算的数。

★ 两个数的分母**不同、不可并列**：`4 / 27` 的分母是**全部套**，
`4 / 4` 的分母是**跑过默认档那一腿的套**（＝有基线的套）。没基线的套印 `⊘无基线`——
「没跑」与「跑了但不一致」不许混成一个数。

## 1 · 为什么做这件事

博士 2026-09-24 亲自问：**「为什么现在还在和 python 对拍？不是已经由 go 覆盖了吗」**。
逐套量的实测结果是 **26 套里 24 套现跑 Python 当 oracle**。而 2026-09-19 已经裁定过：

> 引擎切换定案：基线全用 Go、Python 仅作历史参考、**门判据＝Go 自身基线漂移即红**。

这条**没落地**。继续吃 Python 的三条代价：

* (a) 有人改了 `ak_tactic/`，**判据的期望值跟着变** ⇒ 两侧同错也判绿；
* (b) `ak_tactic/` 永远删不掉；
* (c) 它量的是「与旧实现一致」，**不是「对」**。

## 2 · 三种模式与退出码

| `RIOS_GOLDEN` | 语义 | 吃 Python 吗 |
| --- | --- | --- |
| 未设 / `0` / `off` | **现状**：现场调 `ak_tactic` 取期望值（对拍）。迁移期默认档，一个字不改 | 吃 |
| `record` | 调 `ak_tactic` 取期望值，**同时**把 `键 → 值` 收下来（由 `--record` 落盘） | 吃（录的时候本来就要吃） |
| `check` | **只读** `fixtures/golden/<套名>.json`；`ak_tactic` 被封死 | **不吃** |

退出码：判据自己的码不动；**`6` ＝ 通道自己的错**（无基线 / 键缺 / 顶层 import 了
`ak_tactic` / 期望值不可 JSON 化 / **脚本从未绑定通道**）。
`6` 的含义是「**这一套还没转**」，**不是「判据红」**——两者压成一个值会同时毁掉两个判读。

**两条防线**（少一条就会有假绿）：

1. **import 拦截器**：`check` 档下 `import ak_tactic` 立即 ImportError。没转好的取期望值点
   当场响，不会静默走回 Python。
2. **绑定断言**：`check` 档的进程退出时，若这一套**从未** `bind()`，以 rc=6 收场。
   ★ 没有它，一套**一个字没改**的脚本在 check 档下会照常跑 Python 并报绿——
   「未转」被读成「已转」。这是本设计的要害。

因此 `check` 档**必须经 runner** 起进程：
`python tools\freeze_baseline.py --run-script tools\check_xxx.py`。
★ 这条**不是靠文档拦的，是靠 `bind()` 拦的**：直跑时缺 runner 标记
（`RIOS_GOLDEN_RUNNER=1`）⇒ 当场 rc=6 并印出理由。
典型触发面：有人把 `RIOS_GOLDEN=check` 导进环境再跑 `check_go_all.py`（它是**直跑**
子进程的）⇒ 4 套真冻结、23 套照常跑 Python 并报绿，而**一张总表上看不出区别**。
所以这条必须在**代码里**堵死，不能只写在文档里。

五个动作：`--list`（套与基线）／`--record`（录，必须显式）／`--check`（只读对账）／
`--status`（主判据）／`--control`（**每套都要做的控制组**，见 §6）。

## 3 · 逐套状态（27 套 = `tools/check_go_all.py` 的 `SUITE`，**不在这里抄第二份**）

「初判」一栏是**给后续节点排序用的**，**不是结论**——「能冻」的证明只有三条：
`--record` 跑通、`--status` 上那一格变 ✓、`--control` 的 P1 变红。所有初判均标 **未核**。

| # | 套名 | 脚本 | 状态 | 取期望值的那一段（oracle） | 初判（未核） |
| --- | --- | --- | --- | --- | --- |
| 1 | 关卡 | `check_stage_go.py` | **未转** | `gamedata.stage.load_stage` | 乙 |
| 2 | 敌人 | `check_enemy_go.py` | **未转** | `enemy_stats` / `EnemyLibrary` / `load_stage` | 乙 |
| 3 | 干员 | `check_operator_go.py` | **未转**（**归另一个会话，本会话只读不碰**） | `OperatorCalculator` / `TalentBook` / `traits` / `talents` | 乙 |
| 4 | 范围 | `check_range_go.py` | **已转**（293 值；控制组 P1/P2/P2b 成立） | `RangeTable.cells` / `battle.range.footprint` | 乙 · 已核 |
| 5 | 技能 | `check_skill_go.py` | **未转** | `SkillBook` | 乙 |
| 6 | 分类 | `check_classify_go.py` | **已转**（1057 值；控制组 P1/P2/P2b 成立） | `skill._classify` / `_split_variant` | 甲 · 已核 |
| 7 | 生命上限 | `check_profile_go.py` | **已转**（100 值；控制组 P1 成立） | `simgo.skills._max_hp_after_bonus` | 甲 · 已核 |
| 8 | 攻击间隔 | `check_interval_go.py` | **已转**（601 值；控制组 P1 成立；**含第三态**） | `skill.SkillEffects` / `ASPD_MIN` / `MIN_INTERVAL` | 甲 · 已核 |
| 9 | 面板 | `check_panel_go.py` | **未转** | `operator_view.OperatorView` / `SkillEffects` | 乙 |
| 10 | 名册 | `check_roster_go.py` | **未转** | `plan.Roster` | 乙 |
| 11 | 练度 | `check_loadout_go.py` | **未转** | `OperatorCalculator` / `Verifier` / `Plan` / `Roster` | 乙 |
| 12 | 计划 | `check_plan_go.py` | **未转** | `plan.Plan` | 乙 |
| 13 | 关卡静态 | `check_stageenv_go.py` | **未转** | `stage_env` / `load_stage` / `mask_applies` | 乙 |
| 14 | 格表 | `check_cells_go.py` | **未转** | `load_stage` / `spec._find_goals` / `_highland_cells` | 乙 |
| 15 | 部分规格 | `check_specgo_go.py` | **未转** | `build_spec` 一族（`SpecInputs` / `Plan` / `Roster` / `stage_env`） | 乙 |
| 16 | 费用天赋 | `check_costbonus_go.py` | **未转** | `talents.squad_cost_bonus` / `TalentBook` / `Roster` | 甲乙混 |
| 17 | 部署费用 | `check_costof_go.py` | **未转** | `Plan` / `Roster` / `Verifier` / `OperatorCalculator` | 乙 |
| 18 | 寻路 | `check_stagepath_go.py` | **未转** | `eta.leading_wait` / `eta.route_plans` / `load_stage` | 乙 |
| 19 | 闸门 | `check_unsupported_go.py` | **未转** | `spec.unsupported_reasons` 一族 | 乙 |
| 20 | 出怪规格 | `check_spawns_go.py` | **未转** | `parse_stage` / `_route_tables` / `_spawn_spec` / `stage_mul` / `enemy_view` | 乙 |
| 21 | 机制规格 | `check_mechspec_go.py` | **未转** | `build_spec` 一族 | 乙 |
| 22 | 单一入口 | `check_buildspec_go.py` | **未转** | `build_spec` 一族 / `battle.sim.make_total_attack` | 乙 |
| 23 | 干员规格 | `check_operators_go.py` | **未转** | `build_spec` / `talent_finders` / `OperatorCalculator` / `Verifier` | 乙 |
| 24 | 自造规格 | `check_sim_selfspec_go.py` | **未转**（**口径已裁定**，见 §4 第 24 行） | **无 `ak_tactic` import**：判据是 Go 两种入参形式的**差分** | 丙 · 口径已定 |
| 25 | 调用链 | `check_sim_via_python_go.py` | **未转**（**口径已裁定**，见 §4 第 25 行） | `Plan` / `Roster` / `ensure_go_engine` / `Verifier`；「以前」取自 `git show d4ddc4c:` 的**原文** | 丙 · 口径已定 |
| 26 | 命令面 | `check_cli_go.py` | **不适用于冻结**（见 §4 第 26 行） | **被测方就是 Python CLI**（`python -m ak_tactic stage …`，子进程） | 丙 · **具名不转** |
| 27 | 敌方机制 | `check_enemy_mech_go.py` | **未转**（**归第三个会话，本会话只读不碰**） | 不从 Python 取期望值：从数据侧现算键空间、从 Go 源码现算消费面 | 待定（**别人的套**） |

「初判」三类：**甲**＝期望值是纯函数调用（输入可 JSON 化）⇒ 直接可冻；
**乙**＝期望值是「跑一遍 Python 构造/解析/求值」的结果 ⇒ 可冻，但**键必须带输入身份**
（关卡 id、夹具文件 sha、名册、计划），否则「输入换了而基线没换」会变成假绿；
**丙**＝判据的对象是 Python 侧自身的形状（源码 / CLI 行为），冻的只能是**原文或行为**。

## 4 · 未转的套：**具名**理由（逐条，不许写「大致都转了」）

未转的 23 套当前都是同一条**程序给出的**具名理由：
`冻结基线防线：套「<脚本>」从未绑定通道（脚本没调 freeze_baseline.bind()）⇒ 未转，rc=6`
—— 即 **一个字还没改**。这不是敷衍的措辞：它是 rc=6 那条防线**现算**出来的，
改一套就少一行。逐套的**实质**理由（能不能冻、冻什么）分列如下：

* **第 1~23 行（甲/乙类）**：**未核——可冻性尚未逐套验证**。初判只从各自 `ak_tactic`
  的 import 面得出。它们要在各自的节点上被**真录一遍**才算数。
  可能真冻不住的情形只有两类，遇到就具名登记：① 期望值由运行期非确定来源产生
  （时间戳 / 随机 / 文件系统状态）；② 期望值不可 JSON 化。
* **第 24 行 自造规格**：**不吃 Python，但现在也没有冻住的基线**。
  **上级会话已裁定（2026-09-24）**：冻「**判决四数 ＋ `unsupported` 逐位**」——
  差分那一路**保留**（它抓的是另一件事），新增一路「Go 现读 vs 冻结快照」；
  冻的必须是 **Go 自己的产物，不许掺 Python**。**未落地。**
* **第 25 行 调用链**：**上级会话已裁定**：**固化 `git show d4ddc4c:` 的原文**
  （把原文落成入库的夹具），而不是冻 Python 现场输出。**未落地。**
* **第 26 行 命令面**：**不适用于冻结**（**这不等于「未转」**——第三种状态）。
  理由：这套的**被测方就是 Python CLI**，它 spawn `python -m ak_tactic stage …`
  并断言「缺值输入下不崩」；spawn 它**不算违规**（它测的是 Python，
  不是从 Python 取期望值）。它没有「Python 侧的期望值」可冻。
  ⇒ 逐套表上它永远是这一格，不列入「待转」。
* **第 3 行 干员 / 第 27 行 敌方机制**：**归别的会话**（`check_operator_go.py` 是第二个
  会话的、`check_enemy_mech_go.py` 是第三个会话的），本会话**只读、不改**。
  它们的状态由各自的会话推进。

## 5 · 冻结的是**两半**：查询集 ＋ 期望值

`分类` 的键集来自 `SkillBook`、`范围` 的代号集来自 `RangeTable`、`关卡` 的清单来自
`load_stage`——**「问哪些问题」本身就是 Python 侧的产物**。只冻答案不冻问题，check 档
要么响（拦截器让它响），要么更糟：有人「顺手」把查询集改成硬编码的一个子集，
**分母静默变小而全绿**（本仓记过：漏跑一套的症状是全绿，只是那份绿少了一块）。

⇒ 规矩：**凡是决定「问哪些问题」的那一段，也走 `G.expect()`**，键名以 `("query", …)`
开头。实测的分子结构（数字本身就证明两半都被冻住了）：

| 套 | 基线值数 | = 查询集 | ＋ 常量 | ＋ 期望值 |
| --- | --- | --- | --- | --- |
| 范围 | 293 | 1（73 个代号） | 0 | 292 |
| 分类 | 1057 | 1（1056 个键） | 0 | 1056 |
| 攻击间隔 | 601 | 0（网格写死在脚本里） | 1 | 600 |
| 生命上限 | 100 | 0（网格写死在脚本里） | 0 | 100 |

### ★ 类型还原：JSON 会抹掉 `tuple` / `set`

`json` 把 `tuple` 写成 `list`、把 `set` 直接拒掉。于是**冻回来的值与现场那个值的
Python 类型不同**，而判据里常常有 `sorted(map(tuple, g)) != sorted(cells)` 这种比法
——那一比会**恒不相等**（假红），或者反过来恒相等（假绿）。

⇒ 规矩：**还原写在判据侧，而且两种模式走同一条路**。取法是把现场值**先规范化成 JSON
形状**（`[list(c) for c in cells]`），判据侧再统一还原（`tuple()`）。
**只在 `check` 档还原的话，两档就不是同一个判据了。**
`范围` 是这套形状的第一个实例（`cells` 是 `tuple` 列表、`footprint` 是 `set[tuple]`）。

### ★ 第三态：**不适用于冻结**

判据里的东西有三种，别压成两种：

1. **期望值** ⇒ 冻；
2. **查询集 / 判定·计数口径常量** ⇒ 冻。⚠ 两者性质不同：**改常量未必能翻转判决**
   （实测：「攻击间隔」的 `ASPD_MIN` 只进行使计数，`--control` 的 P1 一开始挑到它，
   报了「没红」；把 P1 限定成只挑**期望值**之后才成立）。常量冻住的目的是
   **让行使计数的口径可复现**，以及让 check 档不去 import Python；
3. **Python 侧自身的自检**（`攻击间隔` 里「`spec.ASPD_MIN` 与 `skill.ASPD_MIN` 必须相等」）
   ⇒ **冻不了也不该冻**：冻住之后两侧同源、**恒等**，是一条不可能失败的判据。
   这一态**显式印出来**（`⚠ 不适用于冻结：……（两侧现在同源，恒真、零信息量）`），
   不许静默当作通过——把它读成绿，就是又造了一条「预先被决定的绿」。

## 6 · 控制组（`--control`，**不是可选项**）

```
python -X utf8 tools\freeze_baseline.py --control <套名…>
```

本仓规矩：**控制组没红时工具自己吼出来并 exit 1**（没有反向守卫的绿是零信息量的绿）。
两个探针，极性相反、各堵一种洞：

* **P1 · 改一个期望值 ⇒ 必须判红**。证明「**冻的值真的是 oracle**」，
  而不是「读了个文件、判决照旧看 Go」。
* **P2 · 查询集删一个 ⇒ 冻结档看不见（盲区，读数照印）**，
  **P2b · `--check`（拿现读的查询集对账）必须看见**。
  两条合起来才说明分母缩水有补偿控制、且补偿控制真的管用。

`2026-09-24 02:5x` 实测（4 套全做，rc=0）：

| 套 | P1 挑的键（改坏） | P1 读数 | P2 / P2b |
| --- | --- | --- | --- |
| 生命上限 | `["maxhp",-1.0,0.0,-0.5]` | rc=1，`结论：99 / 100` | ⊘ 无冻结查询集 |
| 范围 | `["range","0-1","Down"]` | rc=1，`结论：291 / 292` | 73→72：冻结档 rc=0 `288 / 288`（看不见）／`--check` 改值 1（看见了） |
| 分类 | `["classify","$attack@attack_range_id"]` | rc=1，`结论：1055 / 1056` | 1056→1055：冻结档 rc=0 `1055 / 1055`／`--check` 改值 1 |
| 攻击间隔 | `["interval",0.2,100.0,-0.1,-30.0]` | rc=1，`结论：599 / 600` | ⊘ 无冻结查询集 |

★ **控制组自己抓到了我的两个 bug**（这是它存在的理由，不是我事后补写的说明）：
① P1 一开始挑字典序最小的键，`攻击间隔` 挑到了 `["consts",…]` ⇒ 报「没红」；
真因是那些常量不进比法 ⇒ P1 改成只挑期望值，并把这一点具名登记；
② `_tamper` 原地改，浅拷贝下污染了内存里的真值 ⇒ P2b 凭空多一条「改值 2」；
改成深拷贝后回到「改值 1」。

⚠ **登记在案的残余风险**：冻结档**自己**看不见分母被人改小（它只问冻住的那批问题）。
Python 还在的时候，锚是 `--check`（现读 vs 冻结，两条来源不同）。
**Python 删掉之后**那条锚就没了，剩下的锚只有：基线的 `n_values` 身份栏、查询集那条值
本身、以及**它在版本控制里的历史**。
⇒ 建议（需要 Go 侧配合，不在本会话产权内）：让 Go CLI 能**列出它认识的代号/关卡**，
则「Go 说有 73 个」↔「冻结的查询集有 73 个」是一条**独立来源**的分母对账。

## 7 · 封死的边界（已知，不假装没有）

**拦截器只盖得住本进程的 import。** 判据若 `subprocess.run([PY, "-m", "ak_tactic", …])`
去取期望值，`sys.meta_path` **看不见它**。与其把这句写成口号，不如把它变成一个数：
`check` 档下 runner 会给 `subprocess.Popen` 打一个探针，把**命令行里提到 `ak_tactic`
的每一次 spawn** 记下来并印在结论旁。实测（未转的「命令面」套）：

```
★ 冻结档下 spawn 过 2 个子进程，其命令行里提到 ak_tactic：
    python -X utf8 -m ak_tactic stage main_02-07
    python -X utf8 -m ak_tactic stage act31side_01
```

⇒ 每转一套都要回答一句：**这一套的取数口是不是全走 `G.expect()`**（spawn 计数为 0）。
被测方本来就是 Python CLI 的套（命令面）不算违规——**它测的是 Python，不是从 Python 取期望值**。

## 8 · 各节点的实测：命令 + rc + 读数

仪器一律 `RIOS_SIM_BIN=out/acceptance/rios-sim-stage3.exe`
（sha256(16)=`908e2d2edce63543`）。

### 节点一 · 基础设施（生命上限）

| # | 命令 | rc | 读数 |
| --- | --- | --- | --- |
| 1 | `--list` | 0 | 全部「无基线」 |
| 2 | `python tools\check_profile_go.py`（默认档） | 0 | `结论：100 / 100 个点逐点一致` |
| 3 | `--record 生命上限` | 0 | 写入 `fixtures/golden/生命上限.json`（100 值） |
| 4 | `--status 生命上限 --show` | 0 | 冻结 rc=0／默认档绿／`取期望值 100 次全部命中`／`ak_tactic 已封死` |
| 5 | `--check 生命上限` | 0 | `现读 ≡ 冻结（100 个值逐键相同）` |
| 6 | 控制组 A：手改 1 个冻值 | **1** | `✗ base=-1 cur=0 pct=-0.5 —— Go=0 Python=1.0`，`99 / 100` |
| 7 | 控制组 B：删 1 个冻键 | **6** | `冻结基线防线：缺键 1 个`（**没有**静默回退 Python） |
| 8 | 控制组 C：check 档跑未转脚本 | **6** | `ImportError: 禁止 import ak_tactic` ＋ `从未绑定通道` |

### 节点二 · 逐套改造（范围 / 分类 / 攻击间隔）

**改前 vs 改后（默认档必须一字不变）**：用 `git show HEAD:tools/<脚本>` 取**改前版**到
`out/`（gitignored、非他人产权）当对照，同一把尺子换对象地跑：

| 套 | 改前（HEAD 版） | 改后（工作区版） | 判 |
| --- | --- | --- | --- |
| 范围 | rc=0 `292 / 292 次逐格一致` | rc=0 `292 / 292 次逐格一致` | 相同 |
| 分类 | rc=0 `1056 / 1056 个键逐字段一致` | rc=0 `1056 / 1056 个键逐字段一致` | 相同 |
| 攻击间隔 | rc=0 `600 / 600 个点逐点一致` | rc=0 `600 / 600 个点逐点一致` | 相同 |

| # | 命令 | rc | 读数 |
| --- | --- | --- | --- |
| 1 | `--record 分类 攻击间隔 范围` | 0 | 1057／601／293 个值 |
| 2 | `--status 分类 攻击间隔 范围 --show` | 0 | 三套：冻结 rc=0／默认档绿／`零次吃 Python`／`ak_tactic 已封死` |
| 3 | `--check 生命上限 范围 分类 攻击间隔` | 0 | 四套 `现读 ≡ 冻结`；并**报出一条真事**：`攻击间隔` 脚本内容在录制后改过 ⇒ 该重录 |
| 4 | `--record 攻击间隔`（重录） | 0 | 覆盖留痕：印旧身份 ＋ `值差异：新增 0 / 消失 0 / 改值 0` |
| 5 | `--control 生命上限 范围 分类 攻击间隔` | 0 | 四套 P1 全红、P2b 全看见 |
| 6 | 三套 `--mutate`（`check_go_all --selfcheck` 那一支） | 0 | 反向守卫全部成立 |
| 7 | 控制组 E①：直跑 `RIOS_GOLDEN=check python tools\check_range_go.py` | **6** | `check 档不许直跑（缺 RIOS_GOLDEN_RUNNER=1）`——**直跑赚不到假绿** |
| 8 | 控制组 E②：已转的套 ＋ 空基线目录（经 runner） | **6** | `套「范围」还没有冻结基线`（是 6，不是 1——通道错不许与判据红同码） |
| 9 | 控制组 B/C（缺键／未转的套） | **6** | `缺键 1 个`／`从未绑定通道` |
| 10 | `--status`（全量） | **1** | `4 / 27`；4 套两档都给绿 |

## 9 · 基线里的身份栏位（不是元数据噪声）

`fixtures/golden/<套名>.json` 每条都记：

| 栏 | 为什么必须有 |
| --- | --- |
| `suite` / `script` / `script_sha256_16` | 脚本改了而基线没重录 ⇒ 对不上，`--check` 报「该重录」（**节点二实测到了一次**） |
| `recorded_at` | 判「这份基线有多旧」 |
| `instrument.sha256_16` | ★ 不记仪器，将来绿红翻转时没人能判是 Go 变了还是仪器换了（见 `docs/baseline-flip-list.md`） |
| `python.version` / `git_head` / `ak_tactic.sig16` / `ak_tactic_dirty` | ★ **「用哪个 Python 版本、哪次 HEAD 录的」是基线的身份**：没有它，将来对不上时没人能判是 Go 变了还是 Python 变了。脏名单单独一栏——**脏的那几行正是期望值的来源** |
| `values` / `keys_readable` / `n_values` | 值 ＋ 人读的键 ＋ 条数（条数在 §6 的残余风险里是锚之一） |

`--record` **必须显式点名**；覆盖已有基线时先打印**被覆盖文件的完整录制身份**，
再把「新增 / 消失 / 同键改值」三栏逐块报出（本仓记过：生成物被静默覆盖，没有历史）。

## 10 · 换基线的当天必须登记

本次迁移是**换尺子**（期望值来源从「Python 现场」换成「冻结文件」）。
按 `docs/baseline-flip-list.md` 的规矩：**换尺子的同一个提交里就要写下哪些条目因此不红了**。
本文件 §3 逐套给出「已转 / 未转 / 不适用于冻结」，**未转的一律具名**——没有第三种写法。
另：本次迁移**不改变任何判据的比较逻辑与结论**，每一套在转好的那一刻，
默认档与冻结档**必须同时绿**（§8 节点二那张「改前 vs 改后」表就是这条的证据）。
