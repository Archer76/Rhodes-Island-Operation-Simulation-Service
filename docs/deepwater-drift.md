# 深水漂移定位：`hsex8_max` 的 83杀/814.0333s 与 48杀/3漏/221.6667s

> 工具：`tools/bisect_deepwater.py`（只读；每个提交一个**临时树**，用完即删，不动共享工作树）
> 计划：`fixtures/hsex8_max.json`　名册：`ak-tactic-head/out/roster_max_modelled.json`
> 两枚留证仪器：`b3e4d6b1565d63b4`（19:08 混合树，不可复现）／`b88b28ce6d0ca14f`（22:13 基线构建，已入库到 `fixtures/golden_go.json` 的身份栏）
> **本节所有读数都是本轮实测**；`sha16` 只证明"是不是那一次构建"，源码身份看 `source_sig`

## 一、枢轴问题的答案：**83杀/1漏/814.0333s 不能从任何已提交状态复现**

PM 2026-09-20 00:02 更正后的口径要求先答这一个可证伪的问题。扫描 `6b1173c..HEAD` 之间**改过
`rios-sim` 的全部 17 笔提交**，逐笔建一枚临时 exe 并跑该计划，三态分类（**"建不了"与"拒跑"
都不是读数，必须与读数分开记**）：

| 提交 | 分类 | 证据 |
| --- | --- | --- |
| `6b1173c` | **拒跑** | `这一关点名了机制 "snow.field"，但本模拟器里没有（有的：[huai_shu_li.farmland]）` |
| `d7d8319` `d01eeb6` `d5eeb60` `8accbb8` `e0e7991` `fb0d165` `6bc4eea` | **建不了**（7 笔） | `sim.go:590/607: op.spec.Shield undefined (type OperatorSpec has no field or method Shield)` |
| `38c04eb` `00056b3` `d0eb770` `475424b` `7251fa3` `021287c` `baf1708` `532ccff` | **建不了**（8 笔） | `mech/snow.go:355: ctx.SetEnemyFrozen undefined`（另：`mech/snow.go:425: owner[f.spec.Index].ATK undefined`） |
| `c2ed574` | **读数** | **48杀 3漏 221.6667s 282276.8**　`sha16=94ac2cfb2c2bb52c`　`tree_sig=9b81f2d5` |

**汇总：建不了 15 / 拒跑 1 / 读数 1（共 17 笔）。**

⇒ **在可复现的提交里，只有 `c2ed574` 既能建又能跑，而它读 48杀/3漏/221.6667s**（Go 侧）。
唯一另一笔建得起来的 `6b1173c` **根本跑不了**——它那枚 exe 里**没有当前规格点名的 `snow.field`**。
**中间 15 笔一律编译不过**（两段半落地的多文件改动：先是 `op.spec.Shield`，后是 `snow.go` 的新
API）。⇒ **与权威实现 Python 一致的读数（83/814），只存在于那个不可复现的 19:08 混合态里**
（`HEAD@19:08` ＋ 五个未提交文件，其中含**更早一版、彼时可编译的 `mech/snow.go`**）。
**照实写，不为了凑出区间而放宽**——这是 PM 口径里明写的那一支。

## 二、机制候选表（每条给"变难/变易、在哪个观测量上、改前改后读数"）

| # | 候选机制 | 让该关**变难/变易** | 观测量与**改前 → 改后** | 证据强度 |
| --- | --- | --- | --- | --- |
| 1 | **`snow.field` 真正生效**（积层/扩散/减速/踏入伤害/**满层冻结**） | **变难** | 击杀 **83 → 48**、漏怪 **1 → 3**、用时 **814.0333 → 221.6667 s**、总伤 **591046.1 → 282276.8**（同一 `spec_sha=464a9dc2`、同一名册、同一计划） | **最高**：两侧**都接受**同一份点名 `snow.field` 的规格，唯一变量是 exe；19:08 那枚**有** `snow.field` 却读 83/814 ⇒ **当时 snow 在场但不改变结果**；`c2ed574` 那枚把 `Ctx.SetEnemyFrozen`／`OpView.ATK` 接上后读 48/221 ⇒ **接线才是行为改变点** |
| 2 | **层数护盾**（`op.spec.Shield` 落地） | 方向**未知** | **无可比读数**：涉及它的 7 笔提交**全部编译不过**，没有任何可跑产物 | **中**：只能证明"这 7 笔不可测"，不能证明它改变/未改变该关读数 |
| 3 | 其余 15 笔（含 `snow.go` 新 API 那 8 笔） | — | **无可比读数**（建不了） | 低（不可测本身是结论） |

⚠ **两个方向都还开着，本文件不下"哪一侧对"**：
* "Go 变难是对的"＝**19:08 那版 snow 没接线**（正是本项目反复出现的"**有实现、零调用点**"同族：
  实现与测试都在、就是没人调），Python 反而是过宽的那一侧；
* "Go 变难是错了"＝**snow 的接线把行为带偏**，Python 才是对的（Python 在 2026-09-19 裁定前是**权威实现**）。
**判"哪一侧对"归博士**；本文件只交"哪一笔/哪个机制改变了读数"这一**事实**。

## 三、可复跑

```bat
python tools\bisect_deepwater.py --scan 6b1173c..HEAD --json out\acceptance\bisect-scan.json
python tools\bisect_deepwater.py --exe out\acceptance\rios-sim-legacy-1908_b3e4d6b1.exe
python tools\bisect_deepwater.py --at c2ed574 --keep
```
本轮落盘（`out/` 易失，结论与仪器身份以本文件为准）：`out/acceptance/bisect-scan.json`、
`out/acceptance/bisect-snow.json`。

## 四、这台仪器链上的两件事实（与上表同等重要）

1. **同一份源码两次构建，`sha16` 不同**：当轮自建 `98fe111bce6c4ff0`、`c2ed574` 的构建
   `94ac2cfb2c2bb52c` / `06b7e0a8b7199ae2` —— 它们的 `tree_sig` **都是 `9b81f2d5`**
   （`c2ed574` 之后 `rios-sim/**/*.go` 再无改动）。⇒ **exe 哈希证明"是不是那次构建"，
   `source_sig` 证明"是哪份源码"**，两者不可互替。
   > ⚠ **2026-09-20 00:35 更正（我发布过的错，撤回）**：本节初版把 **22:13 那枚 `b88b28ce6d0ca14f`**
   > 也算进了"同一份源码"里，写的理由是"它们的 `tree_sig` 都是 `9b81f2d5`"。
   > **那是一个未测的假设被写成了读数的样子**——`tree_sig` 是我**今晚**在当前工作树上算的，
   > 它**证明不了 22:13 那枚 exe 是从哪份源码构建的**。
   > **实测反证**（UI_2b 先报，我从自己落盘的两份 stderr 复核，见 §六 6.4）：
   > 两侧 `HITENEMY` 首行——**A（19:08）6 列／B（`c2ed574`）12 列**；
   > 加列的那一笔是 `baf1708`（提交信息原文「HITENEMY 痕迹加 resbase/resdown/res/idx/ecell 列（冻结 −15 的观测点）」），
   > 而 **22:13 那枚的 `HITENEMY` 只有 6 列**（UI_2b 实测）⇒ **它不含 `baf1708`** ⇒
   > **`b88b28ce6d0ca14f` 与 `06b7e0a8b7199ae2` 不是同一份源码**。
   > 修正后的仪器谱系：**19:08 `b3e4d6b1`**（`6b1173c` 混合树，无 −15）→ **22:13 `b88b28ce`**
   > （**含 `d7d8319` 21:26 的 −15 数值，不含 `baf1708` 的观测列**）→ **`c2ed574` `06b7e0a8`**（两者都有）。
   > ⇒ **"给某个旧 exe 贴 `source_sig`"这件事，除非当时量过，否则一律写「不可得」**（后端2 的
   > `guard_engine_bin.py::LEGACY_INSTRUMENTS` 一直是这么写的：`source_sig=不可得`，**它是对的**）。
2. **规格由当前 Python 侧构建**，所以历史 exe 能不能跑，取决于**它有没有当前规格点名的机制**
   （`6b1173c` 就是这样被挡在门外的）。⇒ 做跨提交对拍时，**"建不了"与"拒跑"必须与"读数"分列**，
   否则会把"没有读数"读成"读数没变"。

## 六、痕迹全键差分（同窗归一）——**"雪整族变了"不成立**

工具 `tools/trace_key_diff.py`（**无条件列全部痕迹键**，不预设任何键名；`trace_kv` 是唯一解析入口）。

**仪器与输入**：A＝`rios-sim-legacy-1908_b3e4d6b1.exe`（sha16 `b3e4d6b1565d63b4`，3594240 B）；
B＝`bisect-c2ed574-9b81f2d5.exe`（sha16 `06b7e0a8b7199ae2`，3599872 B，源码 `source_sig=9b81f2d5`）。
**两枚收到逐字节同一份规格**：`spec_sha=464a9dc2f94b1428c8569a36f3b00d1211b6de2c902b481c5f0fa97cae607564`；
**同一套环境** `RIOS_TRACE=1`。判决：A `83杀/1漏/814.0333s/胜`，B `48杀/3漏/221.6667s/负`。

**规模**：stderr 行 A **72666** / B **18425**；解析出的键 A **137** / B **130**；
**未解析行 A 81 / B 47**——逐条看过，是 Go 打的**中文战报文本**（如
`70.8667 凯尔希·思衡托 治疗 怒潮凛冬 +535（1580/2950）`），**不是**痕迹格式失配，故不影响计数。

### 6.1 ⚠ 先说一个陷阱：**全程总数不可比**

`SNOWTICKT` 全程 **A=24422 / B=6651**，看着像"雪少了 73%"——**除下来两边都是 30.0 次/秒**，
差的是**时长**（814.03 s ↔ 221.67 s）。**同窗归一**（W=221.6667 s ＝ B 的全程，A 只统计 `t<=W`）后：

| 标签（同窗计数，取各自 `.t`） | A | B | Δ |
| --- | --- | --- | --- |
| `SNOWTICKT`（雪层节拍） | **6650** | **6651** | **+1** |
| `DMGENEMY` | 964 | 956 | −8 |
| `HITENEMY` | 94 | 96 | +2 |
| `OPDMG` | 231 | 245 | +14 |
| `ATK` / `ENEMYATK` | 101 | 107 | +6 |
| `SNOWENTRY` | 94 | 96 | +2 |
| `SPLASH` | 547 | 556 | +9 |
| `HITOP` | 87 | 97 | +10 |
| `ENV` | 42 | 40 | −2 |
| `DEPLOYDMG` | 9 | 9 | 0 |
| `BLESSING` | 1 | 1 | 0 |
| **`SNOWSKIP`** | **1531** | **1775** | **+244（+15.9%）** |
| **`OPHEAL`** | **2629** | **2494** | **−135（−5.1%）** |
| **`打`（伤害结算整族 8 键）** | **1171** | **304** | **−867（−74%）** |

⇒ **同窗之内，两边的战斗事件量几乎相同**（多数差在 1–2% 以内）。
**"Go 多了或少了雪这一整族"不成立**：雪的节拍同窗**只差 1 次**。

### 6.2 三类差（全程键集合）

* **只在 A 侧出现**：`PHIT.*`（atk/def/name/res/t/层，80 笔）、`PM2.*`（applied/atk/def/name/res/t/层，2 笔）
  ⇒ A 那一版**多打了两族痕迹**（后一版没有）。**"一侧没这个键"只说明那版没打这行，不说明行为不同。**
* **只在 B 侧出现**：`HITENEMY.ecell` / `.idx` / `.frozen` / `.res` / `.resbase` / `.resdown`
  ⇒ B 的 `HITENEMY` 一行**多了冻结标记与"抗性基准／下调"两列**——与 `frozenResDown`（冻结法抗 −15，
  后端2 定位到 `d7d8319` 21:26 引入）同族。**这是"多了哪一列"，不是"多了多少次"。**
* **两侧都有但同窗仍不同**：110 个键，其中**实质差异只有上表那三行**；其余是新旧痕迹列的**配对差**
  （8 键族各减 867 的那批 `打.*` 是同一个事件的 8 列，不是 8 次不同的事）。

### 6.3 结论（**只到"差在哪"，不判"哪侧对"**）

1. **`SNOWSKIP` +244 ——〔已由后端2 读代码定位，本条从"最值得先看"降为"后果"〕**：
   门控在 `rios-sim/mech/snow.go:333-341`（**施放者不在场** `!alive(f.spec.Index)` ⇒ 这一片雪不积层），
   配套在 `:385-392`（敌人侧同判据，减速/冻结/踏入一起停）；原版对应 `battle/sim.py:1127-1128` 与 `:1146`
   ⇒ **`skip ≠ 漏结算`：这是原版就有的语义，跳过是照抄的**。+244 是**后果**（B 侧施放者不在场的帧更多），**不是原因**。
   ⇒ **"雪不是这段漂移的原因"现有两份独立证据**：后端2 的代码读数（`snow_spec` 只送 7 键、
   `mech/snow.go:331-332` 每帧硬写 `spreadCap=0; dotScale=0.0`（技能关闭形态），两版语义相同）＋ 本节同窗计数（`SNOWTICKT` 6650↔6651）。
2. **`OPHEAL` −135**：B 的治疗次数少 5%。
3. **`打.*` −74% 与其它计数矛盾**（同类事件全在 1–2% 内，唯独它掉 74%）⇒ **它八成是"有条件才打"的痕迹**，
   **不许拿它当行为差**；要读 Go 才知道挂什么条件——**属机制层**。**后端2 明说"我没读、不猜"，我也不猜：这一格现在挂着，谁读谁负责。**
4. 两次运行在**同一 221.67 s 窗口内事件量几乎相同**，而结局一胜一负（814.03 s 胜 ↔ 221.67 s 负）
   ⇒ 分歧**不在"事件总量"上**，而在线索之外的**状态演化**（谁活下来 / 谁被冻 / 抗性怎么变）。
   ⇒ 下一步该看的是**同窗内 `HITENEMY.frozen`、`resdown` 的取值序列**（按 `t` 对齐），而不是计数。
   **原始痕迹已落盘**（`--dump-stderr`）：A 72666 行 / B 18425 行，同一份 `spec_sha=464a9dc2…`。

### 6.4 ★ 第一处**值层**分歧：`t=79.0000`，比值恰为 `0.95/0.80`（抗性 20→5）

同窗内**逐痕迹对齐**（UI_2b 先报，我用自己落盘的两份 stderr 复核）：

* `t=79.0000` 两侧的痕迹**只有一行不同**——在同一发、同一目标、同一出手者的前提下：

| 标签 | A（19:08 `b3e4d6b1`） | B（`c2ed574` `06b7e0a8`） |
| --- | --- | --- |
| `SNOWTICKT t=79.0000` | 片数=1 敌人=32 interval=5.5000 timer=0.4667 格数=8 | **逐字相同** |
| `OPHEAL t=79.0000`（×2 位干员） | want=2.000 got=2.000 hp 同 | **逐字相同** |
| `OPATK t=79.0000 op=圣聆初雪` | pick=`厌肮(6873)`，inrange 8 个目标同 | **逐字相同** |
| `DMGENEMY t=79.0000 enemy=厌肮 idx=6 src=圣聆初雪` | `amount=696.000 dealt=696.000 hp=6176.580` | **`amount=826.500 dealt=826.500 hp=6046.080`** |

* **算术**：`826.500 / 696.000 = 1.18750 = 0.95 / 0.80`。而 `0.80 = 1 − 20%`、`0.95 = 1 − 5%`，
  且 B 侧对**同一只敌人**（`厌肮`）的 `HITENEMY` 明写 `resbase=20.00`
  ⇒ **"抗性被下调 15 点"这个读数与观测**（`frozenResDown = 15.0`）**逐位对得上**。
* ⚠ **定性只能是"候选机制"，不是结论**：`HITENEMY` **不是每发都打**（A 156 行／B 96 行，
  `t=79.0000` 那发**两侧都没有 `HITENEMY` 行**）⇒ 我**读不到**那一瞬的 `resdown` 字段。
  比值相等是**事实**，"这个比值来自冻结 −15"是**推断**（有 `resbase=20.00` 与 `frozenResDown=15.0` 两条支撑）。
* **B 侧 `frozen=true` 与 `resdown=15.00` 各 20 行、同一批**（`HITENEMY` 共 96 行），首行 `t=81.1000`
  `enemy=去蚀 idx=26 resbase=10.00 resdown=15.00 res=-5.00 frozen=true dealt=730.800`
  ⇒ **"冻结 ⇒ 抗性 −15"在 B 侧确实生效**（A 侧无该列，读不到）。
* **给机制层的一句话**：19:08 侧（＝与 Python 参照同读数的那一侧）这一发是 `696.000`，
  **没有 −15**；现代侧是 `826.500`，**有 −15**。⇒ 这就是"漂移"在痕迹上**第一次显现**的坐标与形态。
  **"哪侧对"不在这份文档里**（属博士裁定）——但**"差在哪、从哪一发开始差"现在是可指的**。

### 6.5 两处**不是机制差**的东西（别把它们算进差异点）

1. **袋语义（同帧多行）**：同一 `(标签, t)` **可以有多行**（同帧多段伤害），`(t, enemy, idx, src)`
   **也不是唯一键**。UI_2b 第一版值序列拿 `t` 当行键（`dict.setdefault(t,{}).update(...)`）⇒ **静默合并**，
   得 9 处不同里 **1 处是假点**（真身 `80.507 ↔ 80.507`）、**另漏 3 处**（`94.0/95.8/97.6`）。
   **本工具量出来的重复度**（2026-09-20 00:58 重跑，`--dump-stderr` 同一对）：
   A **51339** 个 `(标签,t)` 组里 **17551** 组同 t 多行（最多 **1171** 行，共 38797 行）；
   B **14694** 组里 **1940** 组（最多 304 行，共 5624 行）。
   ⇒ **逐帧值对必须比"该键下 `dealt` 的多重集"**；本工具按 `(标签,键)` 计数＝袋计数，并在开头打印这一行，
   使"我在做袋比较"**在读数里看得见**。
2. **截断 ≠ 机制差**：`t=95.8000` B 侧 `去蚀 idx=26 dealt=199.460 hp=0.000`，A 侧同敌 `201.540 / hp=102.320`
   ⇒ 现代侧那只**先死了**，数值更小是**截断**（与 `93.0` 的 `0.77466` 同型）⇒ **不许进差异计数**。

**★ `t=94.0000` 那处（UI_2b 报"没对上"的 `1.32759`）：是**索敌差**，不是新机制。**
两侧 `t=94.0` 逐行对齐，**第一处不同在 `OPATK` 上**：
老侧 `block=厌肮(365)|厌肮(8276) pick=厌肮(365)`，现代侧 `block=厌肮(8276) pick=厌肮(8276)`
⇒ **同一时刻、同一位干员，两侧挑的不是同一只敌人** ⇒ 后续 `DMGENEMY`/`SPLASH` 的**袋内容整体不同**，
`1.32759` 只是袋差异的算术后果。
⇒ 定性：它是 **`t=79.0000` 那处分歧之后的量**（§6.4 已证 `t=79` 是全窗第一处值层分歧、且当时只有一行不同）
⇒ **读作"下游候选"**；要严格区分"下游"与"第二个独立分歧"，得逐帧追 HP 链——**本批没做，所以没下这个结论**。

### 6.6 ★★ `t=94.0000` 的定性：**下游**（链完整，三条机械判据全成立）

PM 2026-09-20 01:14 指令：把 `94.0` 定性成「下游」或「第二个独立分歧」，**分不出就写分不出**。
工具：`tools/hp_chain.py`（单点溯源，逐帧追一只敌人的 HP 链）。对象：`厌肮 idx=6`，窗 `[60,100]`。

**链（每一步都是实测读数，不是推断）**：

| 步 | 读数 |
| --- | --- |
| ① 第一处分歧 | `t=79.0000 DMGENEMY`（`src=圣聆初雪`）：A `dealt=696.000 hp=6176.580` ↔ B `826.500 / 6046.080`。**它之前没有任何一侧独有的事件** ⇒ 这就是该实体的分歧起点 |
| ② 同一偏置在传播 | 跳变点只有 5 处：`t=79.0/81.0/83.0/85.0` 每笔 **Δdealt = −130.500**（累计 −522.000），以及 `t=93.0` 的 **+156.840**（见 ④，截断） |
| ③ 传播等式成立 | **排除截断点后**，逐步 `hp_A − hp_B == −累计 Δdealt`（半个打印刻度内）⇒ 41 处值差事件全部是"同一串事件 ＋ 一个偏置"，不是新来源 |
| ④ 归零由累计解释 | 最后一笔共有事件 `t=93.0000`：A `hp=365.160` ↔ B `hp=0.000`，实测差 `+365.160` ＝ `−累计 Δdealt` ✓ ⇒ **B 侧这只在 t=93.0 被打死，A 侧还剩 365.160**。那一笔的 `Δdealt=+156.840` ＝ `365.160 − 208.320` ⇒ **是"刚好打到 0"的夹零（截断），不是机制** |
| ⑤ 索敌随之改变 | `t=94.0000/94.1333/95.0` 的事件**只在 A 侧**（A 还在打这只）⇒ 两侧 `OPATK` 的实例集不同（A `['厌肮','厌肮']` / B `['厌肮']`）⇒ `pick` 不同 ⇒ 后续 `DMGENEMY`/`SPLASH` **袋内容整体不同** |

⇒ **`94.0` 那处 `1.32759`＝第①步的算术传播，不是第二个独立分歧。**
判据是机械的：① 第一处是纯值差且无先行独有事件；② 排除截断后传播等式逐步成立；
③ 该个体在另一侧的归零可由同一累计差解释。**三条同时成立** ⇒ 工具打印「下游（链完整）」。

**★ 工具自己踩的两个假信号（留证）**：
1. **第一版按 `idx` 过滤 `HITENEMY`** ⇒ 老侧**整类** `HITENEMY` 被滤掉（该列**只在现代侧存在**）⇒
   `t=71.5667` 被读成"只在 B 侧"＝**假的"第二个独立分歧候选"**。修法：**只用两侧都带 `idx` 的标签**
   （本次＝`ATK/DMGENEMY/ENV/SNOWENTRY/SNOWSKIP`），并把"跳过了什么"打印出来。
   （同族：`d37af24f`「跨扩列构建比结构差要先取键集交集」——这里"列"就是字段名。）
2. **传播等式的符号写反**（`dh == acc` 应为 `dh == −acc`）⇒ 四条正确的传播链被判成"有别的来源"。
   **判据自身的符号错会伪装成"有新机制"**，与"永远红/永远绿"同族。

**★ 三种"换目标"口径（计数不同，结论不冲突——但结论必须带口径）**：

| 口径 | 判什么 | 本窗计数 | 能否分辨"同名不同个体" |
| --- | --- | --- | --- |
| UI_2b 的：`block=` 实例集／实例数，**只对值差点应用** | 换目标候选 | **2** | 否 |
| 本工具的 A：`block=` 名字实例集，**无条件扫全部 OPATK** | 换目标候选 | **5** | 否 |
| 本工具的 B：`DMGENEMY` 按 `(t, src)` 的 **idx 集合** | 挨打的是不是同一批个体 | **67** | **是** |

* ⚠ **不许比 `pick=`/`block=` 的文本**：那里**带血量**，前面的伤害差会让文本天然不同
  （UI_2b 第一版比文本 ⇒ 22 处误标成"索敌差"）。
* `t=193.0000`（UI_2b 报的那处）**只有口径 B 抓得到**：`圣聆初雪` A 打 `idx=0`、B 打 `idx=47`
  ⇒ 两侧名字实例集**完全相同**、挨打的是**不同的个体** ⇒ 名字级判据沉默。**口径 B 是必要的补充。**

### 6.7 UI_2b 那 3 处"未对上"的独立复核（口径 B：`(t,src)` 的 idx 集合 ＋ 单点溯源）

UI_2b 2026-09-20 01:20 请我用口径 B 复核他全窗袋语义里"真未对上"的 3 处。**我独立跑，不用他的结论**：
`python tools/hp_chain.py --a <A> --b <B> --enemy 除秽 --idx <n> --range <窗>`。

**① `t=129.6333 idx=37`（`src=怒潮凛冬`）⇒ **下游**（链完整，三条机械判据全成立）**
* 首处分歧 **`t=129.0667`**：A `dealt=626.400 hp=1588.420` ↔ B `730.800 / 1484.020` ⇒ **Δdealt = −104.400**（B 多打 104.4）；
* `t=129.6333` 那笔 `Δdealt=+71.060` **是截断**：B `hp=0.000`，`71.060 = 201.540 − 130.480`（夹到剩余血量 130.480）；
* 累计 `−33.340` ＝ 末尾 A 的 `hp=33.340` ⇒ **B 侧这只在 `129.6333` 归零、A 侧还剩 33.340**；`t=131.4333` 起只有 A 侧还在打它；
* ⇒ UI_2b 看到的 `A [201.54, 201.54, 1152.0]` ↔ `B [130.48, 201.54, 1152.0]`：**第三笔在 B 侧被夹成 130.48** ⇒ **下游＋截断**，不是新机制。

**② `t=142.1667 idx=38`／`idx=39`（同刻两只同名实例）⇒ **分不出**（工具机械判出，不是我挑的）**
* **袋不可配对**：同 `(t,标签)` 在该 idx 上 **A 1 行 / B 2 行** ⇒ 按序硬对**不允许**（工具直接判"先解决配对再定性"）；
* 且 **一侧独有的事件 12 组**，最早 `t=131.4333`（A 侧）⇒ `142.1667` 那处**可能**是它的下游、**也可能**是独立，**两种都还站得住**；
* 传播判据不成立（`hp` 实测差 `+783.420` vs 累计 `−455.700`）、消失判据也不成立；
* 已知的两个量：`Δdealt = −455.700`（= `657.240 − 201.540`，**B 侧多一发 455.700**）、且 `455.700` 是**单一常数**（整数倍检查=True）。
* ⇒ **定性：分不出**。要定性需要：**(a) 同 `t` 多行按 `dealt` 多重集配对**（而不是按序），
  **(b) 追 `idx=38` 在 `[131.4333, 142.1667]` 的完整轨迹（含 `SNOWENTRY` 那种生成/消失事件）**。**这两件本批没做。**

### 6.8 值序列逐点分类（`trace_key_diff.py --series`）——UI_2b 要的"未归类逐处列出"

口径（打在输出表头里）：键 = **`(t, idx)`**，同一 `(t,idx)` 的 `dealt` 取**多重集**（同 `t` 多行不合并）；
分类优先级＝**截断 → 袋不可配对 → 因子族 → 未归类**；窗 `[0, 221.6667]`；两侧 dump 为
`stderr-rios-sim-legacy-1908_b3e4d6b1-b3e4d6b1.txt`（72585 事件）与
`stderr-rios-sim-9b81f2d5-98fe111b.txt`（18378 事件）。**只读 dump、不跑 exe；缺 dump 或一侧 0 事件 ⇒ 拒跑 rc=3**
（早先那版会因为拿不到原始行而输出"0 处不同"——**静默空长得像"两侧一致"**，是最坏的一类假绿）。

| 类 | 计数 | 说明 |
| --- | --- | --- |
| 相同 | 511 | |
| 截断 | 14 | 任一侧 `hp<=0`（方向无关） |
| 袋不可配对 | 5 | 行数不同 ⇒ **不许按序硬对**（`95.0 idx=7`、`113.8 idx=35`、`142.1667 idx=38/39`、`198.6 idx=52`） |
| 因子族 | 34 | 两条规则：**总和比**命中因子表；**袋内单成员**差一个因子（其余成员相同会把总和比稀释掉，所以必须第二条） |
| 换目标（一侧无此 idx） | 207 | 一侧根本没记这个 `idx`（多为下游死亡/生成差） |
| **未归类** | **1** | **逐处打印，不给它一个像的名字**（见下） |

**唯一那处未归类（`t=94.0000 idx=7`）**：
A 袋 `[17.577, 17.577, 40.362]` ↔ B 袋 `[17.577, 17.577, 65.1]`。
**可见的算术**：`17.577 = 0.27 × 65.1`、`40.362 = 0.62 × 65.1`（`65.1` 是半径那一档的基数，实测于 `12x` 行）
⇒ **同一底数、两侧记的 scale 不同（0.62 对 1.0）**。**这只是一个"看得见的比例"，不是结论**——
它可能是"该吃 0.62 的那一档打到了"、也可能是记法与档位错配，**要定性得看那一发的 kind/档位**，本批没看。

⚠ 与 UI_2b 报告的 `52 处` **不可直接比**：他的键空间覆盖多个标签，本表只做 `DMGENEMY` 的 `(t,idx)` 袋。
**报数必须带口径**（同一件事已经栽过一次：换目标的 2／5／67）。

## 七、一条输入侧的事故（已可证复原）：名册实物曾随旁支树消失

做上面这轮时 `tools/*` 集体报 `找不到：roster_max_modelled` —— **`ak-tactic-head` 被废弃后，
名册实物（`ak-tactic-head/out/roster_max_modelled.json`）随之消失**（它住在**易失的 `out/`**里，
且**从未入库**：`git log --all --diff-filter=A -- '*roster_max_modelled*'` 为空）。
**基线记录的身份还在**（`fixtures/golden_go.json` 每条都有 `roster_sha16=21d3045f119a5d04`），
实物没了 ⇒ **19 份读数当时已不可复现**。

**复原（可证）**：源导出仍在（`（本机 OperBox 导出文件）`）⇒ 用生成器
`tools/max_roster.py --source <导出> --out out/acceptance/roster_max_modelled.regen.json`
重新生成（20 位，缺席「阿米娅（近卫）」1 位，与生成器自带告警一致），
**sha16 = `21d3045f119a5d04` ＝ 基线记录值，逐位一致** ⇒ **输入可证复原**（"预录制的哈希优于两边互等"）。
已装回 `out/roster_max_modelled.json`（`_find` 可见）。

⚠ **结构问题仍在**：**输入住在易失目录**。PM 2026-09-20 00:21 **已批**两件，落地情况：
① **名册实物已入库 `fixtures/roster_max_modelled.json`**（sha16 `21d3045f119a5d04`，提交 `2b0226c`，
正文含源导出约定与重生成命令；`_find` 是 **fixtures 优先** ⇒ 重生成必须写这个落点）；
② **新增判据 `roster_identity`（门内 `[1c]`）**：本次读数所用名册的 `sha16` 必须等于基线记录值，
**实物缺失判红（不许判跳过）**，且**「不在场」（缺口）与「不对」（结论）分开报**。
反向守卫是**注入式**的（改一字节 ⇒ 红并报"不对"；路径不存在 ⇒ 红并报"不在场"；控制组必须绿）——
实测**控制组当场逮住过我**：第一版记录值读成了水位基线（`None`）⇒ 判据会**永远红**，
**前两层守卫（敏感性、反例）挡不住空判据，是控制组挡住的**。


* **痕迹级取证**：两枚 exe（19:08 `b3e4d6b1` ↔ `c2ed574`/当轮构建）在**同一规格**下，snow 相关痕迹键
  （积雪层数／冻结施加／踏入伤害）的**计数对比**——若 19:08 侧计数为 **0** 而 c2ed574 侧非 0，
  候选 1 就从"最强嫌疑"升级为"**直接证据**"。`trace_kv` 是唯一解析入口（关键记忆 `dbee841c`）。
* 需要读 `rios-sim/mech/snow.go` 细节才能做的归因（**属机制层，不由我深读**）——PM 已定：先把本文件
  （①区间 ＋ diff 清单）发出，那一棒由 PM 决定交给谁。

## 八、还没做（原 §五）
