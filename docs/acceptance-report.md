# ak-tactic 验收报告

- 生成时间：2026-09-20 02:42:05
- 执行者：验收与守卫会话 `session-1a45cfee-9a65-4830-a327-03ac84285bfb`（命令：`python tools/acceptance.py`）
- 判定：**通过 8 / 失败 0 / 未跑 1**（判定项；口径＝`counted ∧ blocking`，与控制台同一集合；未跑另两口径仅供参考：全部 4／计入判定 3），退出码 **2**，总耗时 1.4 分钟
- 测量对象：HEAD `1c822843b920`，脏文件 38 条
- 判定所锚的产品路径身份：`rios-sim/**/*.go` → `source_sig=9b81f2d5`（`ak_tactic/`、`rios-sim/`、`fixtures/` 的内容随 HEAD 走）

> ⚠ **自指·必读（落后一笔是设计如此，不是漏测）**：本报告记的是**测量当时**的 HEAD；报告自身入库必然把 HEAD 推前一笔 ⇒ **本产物恒落后一笔**。判读方法：拿本报告与当前 HEAD 比，**差异若只落在 `tools/`、`docs/`（工具与文档），产品路径逐位未变 ⇒ 该轮判定对当前 HEAD 仍然成立，无须重跑**；**产品路径有差异 ⇒ 必须重跑**。（严格自指的正确做法是改本工具、同时记「测量对象 sha」与「生成物自身 sha」，**不是**用重跑去追。）

## 一、逐项实测

| # | 项 | 状态 | 本轮实测 | 上次基线 | 判据 |
|---|---|---|---|---|---|
| 1 | 仪器自检（Go 二进制） | 通过 | 私有构建 `rios-sim-9b81f2d5.exe`（`98fe111bce6c4ff0`）；共享 exe 落后源码=None | — | 当前源码能构建出 Go 引擎二进制（-buildvcs=false） |
| 2 | 金标准（规格哈希 + 判决四数） | 通过 | 一致 19/19 份（spec_sha 含在内） | 19 | 与 fixtures/golden_go.json 逐项一致（含 spec_sha），且已有条目未被改写 |
| 3 | 名册身份（输入实物 sha16 == 基线记录） | 通过 | 实物 `roster_max_modelled.json`　sha16=21d3045f119a5d04　登记值=21d3045f119a5d04 | — | 名册实物 sha16 ＝ 基线记录值 ⇒ 输入身份成立；⚠ **换了名册必须换基线**（「能不能走到分歧点」会随名册变） |
| 4 | 森空岛名册身份（输入实物 sha16 == 登记值） | 未跑 | 未跑：**输入不在场**（缺口，不计入判定）：`data/skland/roster_*.json` 下没有文件。它是账号导出、不入库 ⇒ 别人机器上不在场是常态。取回：python t | — | ① 实物不在 ⇒ **未跑＋原因**（账号导出不入库，不在场是常态，不计红也不许记成通过）；② 实物在而 sha16 ≠ 登记值 ⇒ **红**（换了输入就是换了口径）；③ 实物在而登记缺失 ⇒ **红**（登记处是我方文件，缺项是缺陷） |
| 5 | 仪器新鲜度：所用 exe 不得早于本树最新 `.go`（exe_staleness） | 通过 | newest_go=refraction.go；newest_go_mtime=2026-09-19 22:46:41；stale_n=0；source_sig=9b81f2d5 | — | 所用 exe 的 mtime **不早于**本树最新 `.go` 源；否则这次的读数**不可归因**（先钉/重建二进制再读红绿）。⚠ `sha16` 只能证明「**是不是那一次构建**」，**不能证明「是哪份源码」**——实测同一份源码两次构建哈希就不同（`98fe111bce6c4ff0` ↔ `06b7e0a8b7199ae2`，两者 `source_sig` 都是 `9b81f2d5`）；⚠ **反例（我自己踩过）**：曾把 22:13 那枚 `b88b28ce6d0ca14f` 也算成「同一份源码」，理由是「它的 `tree_sig` 也是 `9b81f2d5`」——**那是我今晚在当前树上算的，证明不了那枚 exe 从哪份源码构建**。实测它不含 `baf1708`（`HITENEMY` 只有 6 列）⇒ **给旧 exe 贴 `source_sig`，除非当时量过，一律写「不可得」**；源码身份看 `source_sig` |
| 6 | 闸门放行抽查（go_fallbacks） | 通过 | 19 份 go_fallbacks=0，spec_error 0，与基线有差 0 | — | 每份计划 go_runs≥1、go_fallbacks==0、spec_error 为空、与基线四数一致 |
| 7 | 自检 check_battle（**原版引擎自身**的回归套件，已退出产品路径） | 未跑（**不计入判定**） | 未跑：套件没跑完（没有判定行）：rc=1，0 AND description LIKE '%真实伤害%'").fetchall()
        ^^^^^^^^^^^^^^^^^^^ | 816 | **本项不计入判定**（PM 2026-09-19 裁定「只跑不判」）：测的是**原版引擎自身**的回归；⚠ **不替 Go 背书**——原版已退出基线地位（博士 2026-09-19 裁定）。留下只为留痕：原版有没有退化仍需看得见 |
| 8 | 自检 check_verify（验证器，主体＝Go） | 未跑 | 未跑：套件没跑完（没有判定行）：rc=1，:\home\DSH\ak-tactic\data\skland 下没有 roster_*.json。
先跑：python tools/skla | 71 | 套件全过（无失败项），且项数不低于基线 |
| 9 | 从 HEAD 干净解树构建（fresh_checkout_build） | 通过 | guard=False；worktree_rc=0；build_rc=0；test_rc=0 | — | `git worktree add --detach <tmp> HEAD` → `go build ./...` + `go test ./...` 均 rc=0（**判的是提交，不是工作树**） |
| 10 | 闸门盲区审计 | 通过 | 登记 9 条（本轮扫出 2），未登记 0，守卫失效 0，自检过 | 未登记 0 | 未登记=0 且 守卫失效=0，且 --selftest 反向守卫成立 |
| 11 | 闸门能力清单（static） | 通过 | 已读取 10 / 已定义未读 1 / 未送 16（未读：积雪） | 未送 16 / 未读 1 | 未送/已定义未读的条目数进水位；「已定义未读」每条须能对上登记理由 |
| 12 | 对拍入口（已退役·仅登记） | 未跑 | 退役（仅登记） | — | 不作为验收手段（通告 #2 裁定；博士 2026-09-19 19:56 弃用 Python 模拟器） |

## 二、仪器身份（先验仪器，再谈结论）

- 私有构建（本门使用）：`rios-sim-9b81f2d5.exe` sha256 `98fe111bce6c4ff0`，3,599,360 字节
- 共享二进制：sha256 `None`，构建于 None，落后于源码：**None**
- 规则：本门一律用私有构建（`RIOS_SIM_BIN` 指过去），**绝不覆盖共享二进制**——回写共享树会造成假红。

## 三、版本指纹（本轮数字属于哪一版）

| 文件 | sha256 前 16 位 |
|---|---|
| `ak_tactic/simgo/spec.py` | `9736cc5c2a51857b` |
| `ak_tactic/simgo/skills.py` | `af49d9c6cb17fb91` |
| `ak_tactic/simgo/verifier.py` | `804b892767d2e9b3` |
| `ak_tactic/verify.py` | `172b0315c6e99247` |
| `ak_tactic/battle/sim.py` | `d41e8fe6d4dbb040` |
| `ak_tactic/battle/unit.py` | `e2d3a15298db9e6b` |
| `rios-sim/sim.go` | `105b33e998a29444` |
| `rios-sim/wire.go` | `739dd0a0af3fd48c` |
| `rios-sim/skill.go` | `37ad6dfc28a6417d` |
| `rios-sim/mech/mech.go` | `73cc56f641228181` |
| `rios-sim/mech/huai_shu_li.go` | `c70b943ac98d7ff8` |

## 四、水位比对（与上一次报告比）

- 无下降、无变化。

## 五、未跑项与原因（未跑 ≠ 通过）

- **森空岛名册身份（输入实物 sha16 == 登记值）**（已登记，默认不参与判定）：**输入不在场**（缺口，不计入判定）：`data/skland/roster_*.json` 下没有文件。它是账号导出、不入库 ⇒ 别人机器上不在场是常态。取回：python tools/skland.py login <手机号> <验证码> && python tools/skland.py fetch && python tools/roster.py
- **自检 check_battle（**原版引擎自身**的回归套件，已退出产品路径）**：套件没跑完（没有判定行）：rc=1，0 AND description LIKE '%真实伤害%'").fetchall()
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
sqlite3.OperationalError: no such table: skill_level
- **自检 check_verify（验证器，主体＝Go）**：套件没跑完（没有判定行）：rc=1，:\home\DSH\ak-tactic\data\skland 下没有 roster_*.json。
先跑：python tools/skland.py login <手机号> <验证码>
      python tools/skland.py fetch
      python tools/roster.py
- **对拍入口（已退役·仅登记）**（已登记，默认不参与判定）：对拍项退役：不作验收手段、不修 `sweep_hsl_parity` 那条路、不为它写新仪器。「8 关闸门拦下 / 3 关真差」改读**能力清单**（见上一项）。历史水位（不可复算）：怀黍离 20 关 = 9 关四项归零、8 关被闸门拦下、3 关真差根因未定

## 六、独立复核台账（「它说的」vs「我测的」）

## 独立复核台账（「它说的」vs「我测的」）

> 维护者：验收与守卫会话 `session-1a45cfee-9a65-4830-a327-03ac84285bfb`
> 规则：**不复核就不得替它背书**。每当有会话广播「某阶段完成」，独立复跑与该阶段相关的
> 验收项，把「它说的」与「我测的」并列写在这里；不符就点名会话、文件与数字。
> 本文件被 `tools/acceptance.py` 原样嵌进每轮的 `docs/acceptance-report.md`（标题降一级）。

### 格式

```
#### <日期> <会话> <阶段名>
- 它说的：<原话或摘要>（出处：会话广播 / 提交正文 / 进度文件第 N 节）
- 我测的：<命令 + 实测数字>
- 判定：一致 / 不符 / 部分覆盖（写明缺什么）
- 附录：<最小复现或反证>
```

---

#### 2026-09-19 首轮基线（**自测，不是替谁背书**）

- 它说的：无（首轮只建立基线，未替任何会话的阶段结论背书）
- 我测的：金标准 `--check` 17/17 一致（两台仪器：共享 exe `B3E4D6B1…`（19:08 构建）
  与当前源码私有构建 `18D68C1D…`）；`check_battle` 通过 816 项无失败；
  `check_verify` 通过 71 项无失败；闸门盲区审计未登记 0 / 守卫失效 0（登记表 9 条，
  本轮扫出 7 条）；审计反向守卫 `--selftest` 通过；闸门放行抽查 17 份
  `go_fallbacks=0`、`spec_error` 0 条。
- 判定：不适用（基线）
- ⚠ 未验证项：① 共享 `rios-sim.exe` 落后源码（19:08 vs 源码 21:33），本轮 17 份上
  两台仪器结论相同，故未造成假绿，但这条陈旧已在门里做成固定项；② 逐关对拍已退役
  （通告 #2），本门不复算；③ 测量期间工作树被其他会话改动（进度文件、`rios-sim/sim.go`
  等在测量窗口内仍有写入），报告头部会打出指纹差异。

#### 2026-09-19 RIOS后端 提交 `e0e7991`：控制类清单与战栗/沉睡/浮空

- 它说的（提交正文，逐条抄下）：① `gofmt -l control.go control_test.go` → 无输出；
  ② `go vet ./...` → 干净；③ `go test ./...` → `rios-sim` ok、`rios-sim/mech` ok 全绿；
  ④ 8 条守卫全 PASS（`TestAbnormalFlagListsAreExact` 等）；
  ⑤ 「本批全部**未接线**（无调用点）」；⑥ 新增 `rios-sim/control.go` 164 行、
  `control_test.go` 172 行、`docs/mechanics-dictionary.md` +20 行。
- 我测的（全部本轮亲手复算）：
  - `git diff --stat HEAD -- rios-sim/control.go rios-sim/control_test.go` → 空
    ⇒ 我测的文件与那个提交**逐字节同一份**，下面的数字才对得上它。
  - `gofmt -l control.go control_test.go` → **无输出** ✅ 与①一致
  - `go vet ./...` → **退出码 0** ✅ 与②一致
  - `go test ./...` → `ok rios-sim (cached)`、`ok rios-sim/mech (cached)` ——
    ⚠ **是缓存结果**。缓存只在输入未变时命中，所以不算错；但"复核"要的是**重跑**，
    故我用 `-count=1` 强制重跑（见下条）。
  - `go test -count=1 -v -run '<8 条>' .` → **8/8 PASS，`ok rios-sim 0.420s`** ✅ 与④一致
  - 未接线：`control.go` 里的 8 个函数（`blocksAttack` / `blocksAbility` / `blocksMove` /
    `trembleBlocksAttack` / `canDamageSleepingTarget` / `damageVsInvincible` /
    `levitateApplies` / `levitateDuration`）在 `rios-sim/*.go` 与 `rios-sim/mech/*.go`
    中**除 `control*.go` 外零调用点**（机械 grep）✅ 与⑤一致
- 判定：**一致**（五项自述逐项复核通过；唯一修正是"缓存"那一处口径，已用重跑覆盖）
- ⚠ 我**没有**复核的部分（别读成"整条提交都过了"）：判据本身的正确性——即
  `control.go` 里的三张清单是否与 PRTS `异常效果` 页原文逐字相符。我这轮只核了
  "它说的守卫都过、且确实没接线"，没核"守卫的判据抄得对不对"。要核需要原文比对，
  留待该阶段接线时一并做。

#### 2026-09-19 `tools/parity_plan.py`（`4fe187b` 重建入库）——**跑不起来**（最小复现）

- 它说的（通告 #2 第一节）："已从 RIOS终端UI_2 的会话日志按序重放 2 次 write + 3 次
  edit 重建终版（6025 字符、py_compile 通过），入库为 4fe187b"。
- 我测的：
  ```
  python tools\parity_plan.py plan-hsex08 --deploys 4
  → TypeError: GoCapture._run_other_engine() got an unexpected keyword argument 'schedule'
     （tools/parity_plan.py:116 → ak_tactic/verify.py:485）
  ```
  根因：`verify.py:485` 现在传 `schedule=sched, env=env`（round 9/10 加的），
  而重建版的 `GoCapture._run_other_engine(*, sim, plan, stage, deployed, title)`
  是**旧签名**。`import parity_plan` 本身没问题（`compare()` 存在），**一跑就断**。
  ⚠ 这正是进度 3.39 记过的那类："工具的红被读成代码的红"。`golden_go.py` 的
  `SpecCapture` 当时就是因为同一件事改成收 `**kw`（它的注释里写着理由）。
- 判定：**与"可复现证据"的用途不符**——文件在、`py_compile` 过，但**复现不了任何结论**。
  按通告 #2 第二节第 3 条，对拍项已退役、`sweep_hsl_parity` 那条路不修，
  所以这里**只登记不修**。
- ⚠ 提醒（写给出结论的人）：谁要拿 `4fe187b` 当"切换前 17 份四项全归零"的证据，
  得先把上面这个 `TypeError` 修掉、再跑通一次——**否则那段结论目前没有可复现的仪器**。
  我作为只读验收方不动这个文件。

#### 2026-09-19 项目经理通告 #2 落地检查

- 它说的：① 门的判定项改为 golden / check_battle / audit / 20 关 unsupported 能力清单；
  ② 共享 `rios-sim.exe` 任何人不得写回，一律私有构建 + `RIOS_SIM_BIN`；
  ③ `docs/` 与 `out/` 落点批准；④ 验收工件当轮入库。
- 我测的：① 已落地——第四项换成 `tools/gate_inventory.py` 的能力清单（本轮实测
  **已读取 10 / 已定义未读 1（积雪，已登记理由）/ 未送 16**，合计 27 条）；
  `check_verify` **保留**（博士原始交办的第二条自检水位，通告未提，若要去掉是一行改动）；
  ② 已落地——门第 0 项就是私有构建 + `RIOS_SIM_BIN`，共享 exe 只读不写（本轮实测共享
  exe 仍停在 19:08 构建，未被本会话触碰）；③、④ 见本文件同轮的提交。
- 判定：一致。

#### 2026-09-19 「3 关（hs09/hstr02/hsex07）已四项归零」——独立复跑

- 它说的（终端UI_2，通告 #4 三转述）：`plan-hstr02` 7杀0漏430.133333s 28,000.0、
  `plan-hs09` 82杀3漏120.433333s 44,241.0、`plan-hsex07` 42杀3漏121.300000s 50,627.6，
  **两侧逐位相同、unsupported 为空**。
- 我测的（**两台仪器各跑一遍**，私有构建 `b08b28ce…`，`RIOS_SIM_BIN` 指定）：
  - `tools/parity_plan.py`（后端补完另一半后已能跑）：
    `hs09` 82/3/120.433333/44,241.0　`hstr02` 7/0/430.133333/28,000.0　
    `hsex07` 42/3/121.300000/50,627.6 —— **四项全归零，`unsupported` 为空**；
  - `tools/parity_ledger.py`（我自己的工具，独立走 SpecCapture + Simgo + 原版 + `client.compare`）：
    17 关全部 `unsupported` 为空、`go_fallbacks=0`、四项差全 0、漏事件逐笔一致。
- 判定：**一致**。三关确实归零。
- ⚠ **但"归零"不等于"机制咬到了"**（记忆 8a1ec6d6）：三关规格里的 `mechanisms` 只有
  `['huai_shu_li.farmland']`，**没有 devices、没有天桩链、没有敌人侧污染**。若这些关按设计
  本该踩到装置层，那"四项归零"属于**判据覆盖不到该机制**——覆盖不到机制的判据只会沉默、
  不会否证。这一条已单列进台账（`docs/parity-ledger.md` 第五节「机制咬到了吗」），
  **不混进"已归零"**。

#### 2026-09-19 「25 个作业里只剩 1 关有差（act31side_ex08）」——核对不一致，且**根因是她自己的来源**

- 它说的（终端UI_2 第一条广播）：25 作业 / 24 项四项全归零，唯一离群
  `act31side_ex08`：杀 −35、漏 +2、用时 −592.367s、伤 −308,769。
- 我测的（本轮，**本树** `out/plan-hsex08.json`）：两侧 2杀 3漏 46.533333s 8,800.0，
  **四项全归零**（`parity_plan` 与 `parity_ledger` 两台仪器一致）。
- 差异根源（**她随后自己取证撤回，我复核成立**）：那个离群项来自**旁支检出**
  `D:\home\DSH\ak-tactic-head`，`git rev-list --left-right --count 3b348616...<本树>` = `0 78`
  ⇒ 落后 **78 个提交**，夹具 `hsex8_max.json` 写于 09-19 02:16（早于当日全部修复）。
  **同一关名 `act31side_ex08`、两份完全不同的作业**：本树那份是「勿玷对照 **1 人**（只求敌方
  技能出手落地，不求是好解）」46.5 秒，旁支那份是 **8 人满练度**、814 秒。
- 判定：① "25 作业"这个**计数口径**与我的口径不同（我按本树 `out/` 里 17 份计划＝17 关，
  作业数＝关卡数；她那份 `act31side_08` 出现 4 次、`ex08` 3 次，**作业数 ≠ 关卡数**）——
  按通告 #5 一，**门判据取去重后的关卡数**；② "本树只剩 1 关有差"这个读法**已由她本人撤回**，
  撤回成立；③ 她把 `plan_path` 追到旁支树这一步是**对的**，也回收了本项目最贵的教训
  （记忆 8a1ec6d6：无回归 ≠ 已验证）。
- 我补的一件事：**把旁支那份深水夹具搬进本树并按通告 #5 三复跑**（复制后 sha256 逐字节一致：
  `896cbf34de0eef1e` / `afbf9938d98f9722`），得到 `杀 −35 漏 +2 用时 −592.3666s 伤 −308,769.3`
  ——**与她的数字逐位相同**，即那条差异**成立**，且**用当前源码的私有构建就能复现**。
  指纹：原版 83杀/1漏/**814.0333s**/591,046.1 vs Go 48杀/3漏/**221.6667s**/282,276.8；
  **Go 侧 8 名干员全部阵亡（首笔阵亡 t=58.0s）**，随后战斗提前结束——按记忆 10f0641b，
  先问「谁先塌」：这条线的第一个可观测量是**干员阵亡时刻**，不是伤害差。
  `mechanisms=['huai_shu_li.farmland']`、`mech 事件 22 笔 / env 1 笔` ⇒ **机制确有动作**。
- ⚠ 必须带的限定语（通告 #5 四）：**"本树现有用例全绿"不等于"这些关没问题"**；
  本树 17 份浅用例里**没有任何一份**能走到那条长线。

#### 2026-09-19 通法自查：**"声称跑原版却没钉 `engine=`"**（本门链上）

- 要查的缺陷类（终端UI_2 复盘）：09-19 引擎切换后，裸 `Verifier()` 跑的是 **Go**，
  代码却照样打 `[python]` 前缀 ⇒ **自己跟自己比**，不报错、不抛异常，只是安静地什么都不比
  （记忆 ad36418f）。判据**不是**"有没有写 `engine=`"（那样筛出 60 处，太粗），
  而是**「这次的用途是不是要原版」**。
- 我机械核了本门链上与水位相关的每一个调用点（正则：`Verifier(` 且无 `engine=`）：
  - `tools/acceptance.py` —— **裸 `Verifier()` 0 处**（本门不构造 Verifier）
  - `tools/golden_go.py` —— **0 处**（Go 侧走 `SpecCapture`）
  - `tools/parity_ledger.py` —— **0 处**；唯一要原版的地方在 L191，
    **显式钉了 `engine="python"`**（我的深水实测正是走这一行）
  - `tools/check_battle.py` —— 裸 `Verifier()` **24 处，但 `.run(` 调用 0 处**：
    全是 `v.unit()` / `v.talents` / `SkillBook` / `v.stage()` ⇒ **引擎根本不参与**，
    不受切换影响
  - `tools/check_verify.py` —— 裸 `Verifier()` **9 处，其中 6 处真跑引擎**
    （L226/238/250/271/282/295），**未钉**。按用途判：它测的是**验证器自身**，
    而验证器的默认引擎现在就是 Go = 产品 ⇒ **跑 Go 是对的**，不是假绿。
    ⚠ 但要说清后果：那 71 项里写死的锚点（1-7 真实范围 133.0s / 39杀2漏58650、
    怀黍离 05 单干员 201.4s/46杀/0漏/3命）**现在断言的主体是 Go**
    ⇒ "71 项通过"今天只该读成 **「Go 复现了这些锚点」**，**不替原版背书**
    （原版已不在产品路径上）。
- 判定：**本门链上无"声称跑原版却没钉 engine"的基线**（"没找到"也留痕，见上）。
  两处相关但**不是缺陷**的调用点已按用途逐件定性，写在报告第八节。

#### 2026-09-19 深水 `hsex8_max`：差异**成立**（三台仪器一致）

- 它说的（终端UI_2，第二版）：修掉探针的假绿后，Python 侧复现
  `83杀/1漏/814.033333s`、Go 侧 `48杀/3漏/221.666667s`，共 9375 条坐标分开
  （帧记录 880668 / 107688），**到小数第六位一致**。
- 我测的（**早于**这条广播，用我自己的仪器、`engine="python"` 已钉）：
  原版 `83杀/1漏/814.0333s/受伤害 591,046.1/won=True/timed_out=False/死5人/落位72`
  vs Go `48杀/3漏/221.6667s/282,276.8/won=False/死8人/落位64`；
  **两笔第一漏事件相同**（t=96.4 厌肮）。
- 判定：**一致，差异成立**。三台独立仪器（我的 `parity_ledger`、后端补完的
  `parity_plan`、修好后的 `probe_*`）给同一个数。
  补一条我这台独有的旁证：**`won=True` 且 `timed_out=False`** ⇒ 原版那 814 秒是
  **自然打完的一局**，不是被 `max_time` 截断的假象——恰好排掉"仪器截断"那类怀疑。
- ⚠ 方法论留痕：**判据要在修仪器之前写死**（"修好后 Python 侧必须复现 83/1/814.033s"），
  否则修完看到什么数都能叫"对上了"。这一条比结论本身更值钱。
- ⚠ 仍须带的限定语：「本树现有用例全绿」**不等于**「这些关没问题」——能走到这条
  814 秒长线的**只有这一份夹具**，其余大多是几十秒的浅用例。


1. **把仪器的瞬时断裂报成了代码的红**：金标准 17 份同报
   `AttributeError: 'SpecInputs' object has no attribute 'skill_uses'`（`SpecInputs` 收口
   正在改 `spec.py`），我按"有 17 份不一致"的 FAIL 处理；两分钟后再跑即 17/17 全绿。
   ⇒ 已修：**全部计划报同一种异常时先重跑一次再定性**，仍如此记「未跑」并附异常原文。
2. **带 `--update-baseline` 跑红了的一轮**，把 `golden_consistent=None` 写成了新基线
   —— 等于亲手把标准降下来。⇒ 已修：**非全绿的一轮一律拒绝更新基线**（要覆盖须显式
   `--force-baseline`），并重跑一轮绿的重建基线。
   同轮还从水位里**删掉了 `audit_registered_active`**：那是审计输出里的**打印行数**
   （实测 7→2 只因该工具展示口径变了，未登记/守卫失效始终为 0），拿展示细节当水位，
   会把别人的排版改动报成回归。

## 七、水位历史

- 2026-09-19 21:45:51 `fb0d1658cc7d` session-1a45cfee-9a65-4830-a327-03ac84285bfb（验收与守卫）：首轮实测建立基线：金标准17/17、check_battle 816、check_verify 71、审计9登记(扫出7)/0未登记、selftest通过
- 2026-09-19 21:50:41 `b05fee48d328` session-1a45cfee-9a65-4830-a327-03ac84285bfb（验收与守卫）：按项目经理通告#2 落地：第四项换为闸门能力清单（gate_inventory 三态）、对拍项退役仅登记；并记入 e0e7991 独立复核与 parity_plan 跑不起来的发现
- 2026-09-19 21:57:15 `38c04eb4f768` session-1a45cfee-9a65-4830-a327-03ac84285bfb（验收与守卫）：重建基线：上一轮被在途重构的瞬时断裂污染（金标准报 AttributeError、审计撞 activity.py 写锁），两处均为仪器问题；本轮已加「同种异常→重跑一次再定性」与「红轮不许改基线」两条修正
- 2026-09-20 02:32:38 `4c61fe5a4994` session-1a45cfee-9a65-4830-a327-03ac84285bfb（验收与守卫）：水位 17→19 有解释了：判据集扩容——3bab699(22:30) 把判据集收进 fixtures/，用 golden_go --extend 先验已有 17 份逐项一致、再并入新的 2 份（共 19 份），该提交正文自述『17 份的判决四数与 spec_sha 逐项未变，只是判据集长了两份』；基线表 history 里当时没留这笔 ⇒ 报告按规则标『无解释』，规则本身是对的。另：本轮未跑项（check_battle＝库 data/akdb.sqlite 0 字节无表 skill_level；check_verify＝data/skland 无名册）不冲水位，保留上次实测 816/71。

## 八、本门**不覆盖**什么（别把绿读成全绿）

> ⚠ **判据变更（博士 2026-09-19 裁定：基线改用 Go）**：本门第 1 项的参照物本就是 **Go 自身基线**（`fixtures/golden_go.json`），裁定后语义更纯：**红＝Go 自己漂了**。原版数字**退出判据**，任何「与 Python 一致」类判据要么报废、要么改判据，逐列点名见 `docs/parity-ledger.md` 〇之二。

- **逐关对拍**：已按项目经理通告 #2 **退役**（博士 2026-09-19 19:56 弃用Python 模拟器），本门不再把它当验收手段、不为它写新仪器；「8 关闸门拦下 / 3 关真差」改读**闸门能力清单**（第六项）。⚠ 已入库的 `tools/parity_plan.py`（4fe187b）实测**跑不起来**（`GoCapture._run_other_engine` 不接受 `schedule`，见台账）——谁要引它当证据，先自己跑通。
- **判据集里 19 份作业没走到的路径**：金标准只覆盖这些作业上真跑出来的路径；没被走到的分支，它不说话——**覆盖不到机制的判据只会沉默，不会否证**。所以金标准绿 ≠ 所有机制都对。
- **深水限定语**：「本树现有用例全绿」**不等于**「这些关没问题」——能走到长线的只有迁入的 `hsex8_max`（八人满练度），其余大多是几十秒的浅用例。
- **深水差异成立**（`hsex8_max`）：原版 83杀/1漏/814.0333s vs Go 48杀/3漏/221.6667s，三台仪器一致。⚠ 它**不是**本门判定项**之一**（本门只问「Go 与基线一致」，而这条差是**已知且已登记**的），但**深水限定语照带**：「现有用例全绿」≠「这些关没问题」。
- **仪器切换的静默假绿已自查**：本门链上（本文件 / `golden_go.py` / `parity_ledger.py`）**无裸 `Verifier()`**——唯一要原版的地方显式钉了 `engine="python"`。两处**相关但不是缺陷**的留痕见 `docs/acceptance-claims.md`：`check_battle.py` 24 处裸 `Verifier()` 但**零 `.run()`**（引擎不参与）；`check_verify.py` 6 处裸 `Verifier().run()` ⇒ **自 09-19 起它断言的主体是 Go**（它测的是验证器自身，跑 Go 是对的，但那 71 项**不替原版背书**）。
- **保真**（两台引擎一起错）：本门问的是「Go 与基线一致」，**不问**「像不像真游戏」。`UNMODELLED_ENEMY_ABILITIES` 那 5 条属保真课题，不在本门判定内（裁定 `77fce667` 设的门槛只在一致性这一层）。
- **未跑到的自检分支**：`check_battle` / `check_verify` 报的是它们自己声明的项数；项数没降只说明「没变少」，不等于「新加的路径被考到了」。

## 九、怎么用这条门

```
python tools\acceptance.py            # 全套（金标准两趟 + 自检两项 + 审计 + 能力清单）
python tools\acceptance.py --quick    # 省掉「闸门放行抽查」那一趟（退出码记 2）
python tools\acceptance.py --update-baseline --by <会话> --why "<为什么>"
```

⚠ 三条铁律：① 未跑不许写成通过；② 水位降了就是红；③ 比数量前先确认工作树没被别人动过——报告头部的指纹差异段就是干这个的。

