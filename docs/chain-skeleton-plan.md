# `chain.*` 骨架：第一轮预注册（2026-09-20）

> 本文件是 **judge 的预注册**：**判据、方向、红条件先写死，再写代码**。
> 后续若判据要改，**改的是判据的措辞并留痕**，不是让实现去迁就（`7bd9ca65`）。
> 状态：**骨架代码尚未落**（本文件只登记「要判什么、什么情况下必须红」）。

## 一、键族身份（DB 实测，命令可复现）

`chain` 族在库里**只有 3 个键、两种写法**（带 `attack@` 前缀与裸键并存）：

| 键 | 出现处 | 值 | 正文依据 |
| --- | --- | --- | --- |
| `chain.max_target` | 特性（5 位）／技能 `skchr_halo_1` | 3.0（乌啾/明椒等）／4.0（halo 精2、halo 技1） | 「会在 **3** 个友方单位间跳跃」 |
| `chain.atk_scale` | 特性（4 位） | 0.75 | 「每次跳跃治疗量**降低 25%**」 |
| `chain.atk_scale_2` | 天赋「久病良医」×4 档（乌啾） | 1.1 / 1.15 / 1.2 / 1.25 | 「…110%/115%/120%/125%」四档 |

* 持有者（特性）：`char_135_halo`（**对敌人**，法术伤害链）、`char_4071_peper`、`char_4139_papyrs`、`char_4224_turdus`、`char_4179_monstr`（**对友方**，治疗链）。
* 持有者（天赋）：`char_4224_turdus`「久病良医」四档 → `chain.atk_scale_2`。
* 持有者（技能）：`skchr_halo_1`（同时带 `chain.max_target`=4.0 与 `max_target`=2.0 —— **两个不同的键，不许合并读**）。
* 复现：

```powershell
python -m ak_tactic db sql "SELECT char_id, group_index AS gi, cand_index AS ci, name, blackboard FROM operator_talent WHERE blackboard LIKE '%chain%'"
python -m ak_tactic db sql "SELECT char_id, cand_index AS ci, unlock_phase AS ph, blackboard FROM operator_trait WHERE blackboard LIKE '%chain%'"
python -m ak_tactic db sql "SELECT skill_id, level, blackboard FROM skill_level WHERE blackboard LIKE '%chain%' LIMIT 6"
python -m ak_tactic db sql "SELECT char_id, override_description FROM operator_trait WHERE blackboard LIKE '%chain%'"
```

**两条正文原文**（判据的地基：断言从正文写起，不从键名想当然 §`4decd843`）：

* 治疗链（`char_4071_peper`／`char_4139_papyrs`／`char_4224_turdus` 特性）：
  「恢复友方单位生命，且会在 **3** 个友方单位间跳跃，每次跳跃治疗量**降低 25%**」
* 伤害链（`char_135_halo` 特性）：
  「攻击造成法术伤害，且会在 `{attack@chain.max_target}` 个**敌人**间跳跃，每次跳跃**伤害降低 15%** 并造成短暂停顿（精英2后更新）」

⇒ **同一个键族有两种用法**（对友方＝治疗链，对敌人＝伤害链），且**衰减比例不同**（25% vs 15%）。
★ 本轮**只做治疗链这一支**（PM 裁定：第一轮不建第二族）；伤害链那支**登记不建**。

## 二、Go 侧现状（取证范围写在结论里）

* 命令：`grep -r chain rios-sim`（含 `*_test.go`）⇒ **0 命中**。
* ⇒ 「Go 侧没有 `chain` 的消费点」这句话的**取证范围＝整个 `rios-sim` 树（29 个 .go：15 生产 + 14 测试）**；
  超范围（Python 侧、其它树）**未核**（`1865d35a`：取证范围不许窄于结论范围）。

## 三、骨架形状（最小接线，先 dormant）

1. 新机制文件 `rios-sim/mech/chain.go`：实现 `mech.Mechanism`（`ID()` ＝ `chain`），
   配置经 `mech.RegisterFactory`／`wire.MechConfig` 传入（`rios-sim/wire.go:73`）。
2. **只在配置存在时动作** ⇒ 对既有基线**零影响**；自证方式＝golden 基线逐项不变 + `grep` 自证
   （`09516544`：新代码先 dormant 并 grep 自证零变化）。
3. 本轮**不做**：不改 `simgo/skills.py` 白名单，不碰任何现有机制文件。

## 四、★ 本轮只交**一条键的判据**：`chain.atk_scale`

**断言**（方向）：治疗链上，第 2 跳的治疗量 = **0.75 ×** 第 1 跳（**严格递减**，且首跳＝满量）。

**判据三件套**：

* **计数可见**：判据必须打印**逐跳治疗量序列**（不是只打印一个数）——只比总数会让「衰减发生在哪一跳」不可见。
* **合成反例（可证伪的红条件，先写死）**：
  * （a）把衰减系数当**每次乘基准**（每跳都 ×0.75）⇒ 第 3 跳 = 0.75 ≠ 预期 ⇒ **必须红**；
  * （b）把衰减**丢掉**（每跳 = 1.0）⇒ 第 2 跳 = 1.0 ⇒ **必须红**；
  * （c）把衰减方向弄反（越跳越高）⇒ **必须红**。
* **反向守卫**：把 `chain.atk_scale` 置 1.0（等价于无衰减）时判据**必须红**，且退出码非 0。

## 五、★ 明确「未定」的一项（不许拿猜测顶）

第 3 跳的治疗量是 **0.5625（等比 0.75²）** 还是 **0.5（等差 1−0.25×2）**——**语料只给了 `0.75` 一个数，两者在 n=2 完全同值、无法分辨**。

* 正文写「**每次**跳跃…降低 25%」，字面更像**逐次相对衰减（等比）**，但**不足以下结论**。
* 因此：**本轮判据只断言「方向 + 第 2 跳 = 0.75」**；第 3 跳记 **未定**，
  要判它必须另找证据（跨源对账／实战逐跳读数），**不许**由 0.75 与「25%」推出来当事实。

## 六、下一步（第一轮范围）

1. 落 `rios-sim/mech/chain.go` 骨架 + 上述判据（Go 单测）；
2. 跑 golden 基线自证「零变化」，跑新判据自证「能红」；
3. 交 PM；**骨架通过 ⇒ 再补另两个键（`chain.max_target`、`chain.atk_scale_2`）；不通过 ⇒ 早停**（PM 原话）。

---

## 七、★ 第 3 跳的出口：可证伪实验（PM 裁定 ① 时要求补，`msg-mu8ykj51-fe`）

> **「记未定」必须连「什么时候能判、判它需要什么读数」一起记** —— 否则它会烂成坑。
> 下面按 `cbfbb50a` 的格式：**观察与推断分两栏，推断必须配一个可证伪的实验**。

### 7.1 观察栏（已有事实，不掺推断）

| 观察 | 值 | 来源 |
| --- | --- | --- |
| `chain.atk_scale` | 0.75（4 位干员特性共用） | DB：`operator_trait.blackboard` |
| `chain.max_target` | 3.0（乌啾/明椒/papyrs 特性） | DB：`operator_trait.blackboard` |
| 正文 | 「…在 **3** 个友方单位间跳跃，每次跳跃治疗量**降低 25%**」 | DB：`operator_trait.override_description` |
| 第 1／2 跳 | 满量、0.75×满量（**两读法在此重合**） | 本节 7.3 的分岔点分析 |
| 第 3 跳 | **三个候选：等比 0.5625 ／ 等差 0.5 ／ 每跳倍率 0.75 —— 未定** | 三个候选值都能由现有语料「解释」，故现有语料分不开（见 7.2 表：n=2 三行同值） |

### 7.2 ★★ 前置未定项**及其判据**（**比第 3 跳那条更靠前，必须先读这一节**）

> **将来拿到第 3 跳读数的人，第一眼看这一节**：它回答**「我该在哪个点上读」**
> 与**「为什么那个点能区分」**——**以及这两句话各自凭什么**。

#### 7.2.1 前置未定项：`0.75` 是哪种东西，还没定

> **「0.75」这个数本身是哪种东西，还没定** —— 它可能是
> **① 衰减系数（降到 75%）**，也可能是 **③ 每跳倍率（每跳都是 0.75 × 首跳）**。
>
> **这一条之所以「前置」**：`n=2` 的观测值在 **三种**读法下**完全同值**（都是 `0.75 base`）——
> 所以 §四 那条断言（第 2 跳 = scale × 首跳）**数值上在三读法下都成立**。
> ★ **它既是「敢断言第 2 跳」的理由，也正是「它区分不出读法」的理由：同一件事的两面。**
>
> ⇒ **在 ③ 被排除之前，`n=3` 的判决条件是三联的（0.5625 ／ 0.5 ／ 0.75）**，
> 而不是「二选一 + 一个『都不是』的兜底」。

| 读法 | 一句话 | n=1 | n=2 | **n=3** | n=4 |
| --- | --- | --- | --- | --- | --- |
| ① 等比衰减（0.75 是衰减系数） | 每跳是上一跳的 75% | base | 0.75 base | **0.5625 base** | 0.421875 base |
| ② 等差衰减（每跳减 25% 首跳） | 每跳少 25% 首跳 | base | 0.75 base | **0.5 base** | 0.25 base |
| ③ **每跳倍率**（0.75 是倍率本身） | 每跳都是首跳的 75% | base | 0.75 base | **0.75 base** | 0.75 base |

★ **表里 n=2 那一列三行全同**——这就是「前置未定」的全部内容：**n=2 对区分读法零信息量**，
而 §四 的断言在这三行下都真。★ **分岔点仍然只有一个：n=3**（三值互不相等），
**但「n=3 该判成哪个」在 ③ 未被排除前不可能是二值的。**

★ **「某读法为真」时，要分开问它对「数值」与「解释」各有什么影响**（PM 通则 `msg-mu900bls-ft`）：
③ 为真时第 2 跳**仍是** `0.75 base` ⇒ §四 断言的**数值照样成立**，
被推翻的只是「**降低 25%**」这个**解释**（同一个数、两种含义）。
**「数值断言照样成立」本身是有信息量的结论**——它把不确定性的范围缩到了 n≥3。

#### 7.2.2 判据之一：**盲区必须被证明是盲区**（不是免责声明）

**「本判据抓不到候选③」这句话本身也要有判据**——否则它可能是**假盲区**（其实能区分，只是判据没做 ⇒ 那是**漏网**）。
所以 `chain_test.go` 里加了一条断言：**候选③ 与本实现在 n=2 必须同值**（实测 `75 == 75` ⇒ rc=0）。
* 若**不同值** ⇒ 判据本可区分它 ⇒ **「已知盲区」的说法不成立**，测试会红（真漏网）。
* ★ 它同时防住两种假：**假盲区**（能区分却说抓不到）与**假区分**（声称两读法不同、实测同值 ⇒ 判决零信息量）。
* 同族：`ecf7b3f7`（反向守卫必须真换策略）——那条说「你以为换了策略，其实没有」，
  这条说「你以为存在盲区，必须先证明它真存在」。两条合起来＝**关于判据能力的断言，本身也要有判据**。

★★ **2026-09-20 勘误（验收顶回、PM 提成通则 `msg-mu903tfp-g0`）：上面那一版写法「会红，但不独立」。**

* 验收的顶回（它亲自在**副本**里变异，产品树未动）：把候选③ 第 2 跳 `75→80` ⇒ **同一个变异红了两条**
  （`chain_test.go` 的盲区用例 ＋ 那条新断言）。**原因**：两者**共享同一个 `blind` 数组**，
  而「盲区用例绿」＋「实现过判据」在算术上就推出 `blind[1] == seq[1]`
  ⇒ **新断言被前两条蕴含**（同族 `d8706dc5`：判据被算术必然满足＝零信息量）。
* 它还指出比对对象错：**字面量 `75`** 比 **实现算出的 `100×0.75`**，两个数由**同一对常量**决定
  ⇒ 判词里那句「若不同值 ⇒ 该判据本可区分它」是**推断**，不是**测到的东西**。
* **改法（已落地）**：候选③ 做成**真读器**（生成器 `candSeqPerJump`／`candSeqGeom`／`candSeqArith`），
  断言改成**生成器之间**的比较（n=2 三读法同值 ＋ n=3 两两不等），并与 `blind` 字面量**解耦**；
  原来「盲区本体＋理由」的三句话是同一件事的三个面，**合成一条断言**（否则一个变异红三行）。
* ★★ **判据（PM 提成通则）：「一个改动红几条」就是独立性的判据。**
  红一条 ⇒ 独立信息；红两条 ⇒ 至少一条多余（**你无法从「红了两条」知道是哪条在起作用**）；
  红三条以上 ⇒ 大概率是同一判据的多份副本。
* **实测矩阵**（`tools/chain_judge_independence.py`，可重跑；副本在 `out/`，产品树只读）：

  | 最小变异 | 红了哪几条 | 结论 |
  | --- | --- | --- |
  | M1 实现 n=2 写错（×1.05） | 断言 1（实现过判据） | 红 1 条 ✓ |
  | M2 候选③ 的 n=2 写错（×1.05） | 断言 5（读法判别器） | 红 1 条 ✓ |
  | M3 候选③ 的 n=3 变成与②同值 | 断言 6（分岔点） | 红 1 条 ✓ |
  | M4 实现对 scale 不敏感（恒满量） | 断言 1（实现过判据） | 红 1 条 ✓ |

  ⇒ 每条断言**各有自己的一份量**，没有一条是另一条的副本（脚本 rc=0 即此意；某行红 ≠1 条它判失败）。
* ★ 顺带把 n=3 的分岔**从散文变成读数**：三读法 n=3 实测 `56.25 ／ 50 ／ 75`（两两不等），
  故「分岔点只有 n=3」不再是主张，而是这三个数。

★★★ **2026-09-20 追加（PM 派单 ②、验收预注册 G1–G5）：矩阵是真的，缺的是通道。**

* 验收的 **P2 探针**：在同块内加一条**表外**判词、与表内一条**同时红** ⇒ 真实 `go test` **红 2 行**，
  而旧版脚本报「红 1 条 ✓」、**rc=0**——**红行被归类器丢掉，且不留痕**。
  P1（判词改名）会响、P2（多一条认不出）**不响**，差别就在：**覆盖内的照常红、覆盖外多红了一条
  ⇒ 计数仍然正确 ⇒ 不响**（`red_assertions()` 只数它认得的那些）。
* 改法（两条都做了）：① 归类**按行号认语句种类**——`-v` 下 `t.Logf` 与 `t.Errorf` 形状完全一样，
  只有**行号**能分（行号指向发出该消息的语句）；**表外判词 ⇒ 具名打印 + rc=1**，
  行号落在任何已知调用区间之外 ⇒ 同样判失败（**fail closed**）。
  ② 覆盖面**显式印出**：表外 8 条逐条具名，**理由由结构给出**（所属测试函数 ＋ 本轮变异不经过它）；
  另 2 条只作 `bad` 的文案、**结构上不能独立红**（随断言5 那一行一起打印）。
* ★ **判据**（写进脚本注释与产物）：**「认不出」必须自己会红，不能靠「恰好没有第二条」。**
* **G1–G5 全部落成脚本内的检查**（不只是纪律）：G1 双向（P1／P2 两个控制组必须 rc≠0 **且具名**）、
  G3 锚点两两不同、G4 四个变异仍各恰红 1 条（**不许放宽**）、G5 产品树逐文件 SHA256 跑前跑后相同。
  P3（锚点找不到）⇒ 当场退出，且**矩阵先算后印** ⇒ 失败不留一份看着正常的矩阵。
* ★★ **口径对账（两套分母不许相加）**：本脚本按**调用处**数 ＝ **13**（表内 5／表外 8）；
  验收 P4 的正则 `t\.(?:Errorf|Fatalf)\(` 数 ＝ **17 ＝ 13 ＋ 4**，那 4 条是 **`fmt.Errorf`**
  ——正则**子串命中**了它（`fm`＋`t.Errorf`），正是 helper `assertDecayFirstTwo` 的返回文案
  （验收自己判为「实际已覆盖」）。加词边界 `\bt\.` 即只数 13 ⇒ **修后两个口径合成一个，分母消失**。
  ★ 通则：「两个口径给出两个数时，先问能否变成一个口径，能就消掉分母」（`61d85582`）。
* ★ **字符级更正（我上一笔的错）**：`5686f9a` 的提交信息写「GBK 打不出 `⇒`」，
  **真正先卡住的是 `✓`(U+2713)**；类别对（GBK 会让判据假红）、字符差一个。
  并且**把这一类变成检查**：脚本入口断言 stdout 编得动 `✓`/`⇒`，编不动即大声失败并写明
  「这是**环境**不是判据红」。★ 这条检查自己也踩过一次假红：验收探针用
  `contextlib.redirect_stdout(StringIO)`，而 `StringIO` **没有** `encoding` 属性 ⇒ 被读成 ascii
  ⇒ **每一次被重定向的调用都假红**；修法＝「无编码约束的 str 流 ⇒ 这条检查不适用，直接放行」。
* ★ 控制组**不适用**的情形也要落成结构：验收的 P1 树**已经**把判词改名成「读法判别器不通过：」，
  我的 P1 控制组再找「读法判别器红：」必然 0 命中 ⇒ 记为 **⊘ 不适用（外部树已施加该改动）**，
  并且**不算通过、也不许因此变绿**（rc 仍红）；只有「锚点不在且目标改动也不在」才当场退出。

★★★★ **2026-09-20 追加二（PM 派 G6 ＋验收加派 P7b）：这一族有三扇门，修一扇关不掉另一扇。**

* **门①「别的文件」**（G6）：`RED_RE` 把文件名写死 `chain_test.go`，而运行范围是**整个包**
  （`go test ./mech/ -run Chain`）⇒ 新增 `mech/zz_probe_test.go`（基线绿、M1 红）时，
  **真实 `rc=1`、两文件皆红，我的脚本 `rc=0` 全绿**。
  ⇒ 正规式放宽为 `^\s+(\S+_test\.go):(\d+):` **按文件**取种类；**非 `chain_test.go` 的红一律具名 +
  rc≠0**（fail closed）。判据落到一句话并写进脚本：**红行的取证范围必须 ≥ 运行范围。**
  ★ **独立复现（我这边自己搭树，不引用验收的结论）**：同一棵 G6 树上，从 `65c1d83` 取的**改前**
  脚本 `rc=0` 全绿（M1 行写「认不出 0 条」），工作区改后 `rc=1` 且具名 `zz_probe_test.go`。
* **门②「行的种类」**（P7b）：`t.Logf(…); t.Errorf(…)` **同行**时，按「行内第一个调用」标种类
  ⇒ 整行算「日志」⇒ 同行的红被 `continue` 丢掉；**真实 `rc=1`、脚本 `rc=0` 四行全 ✓**。
  ★ **G6 的修法关不掉这一条**——G6 放宽的是**文件名**，这里丢的是**行的种类**。
  ⇒ 种类按**集合**取、**「判词」优先于「日志」**（fail-closed 方向：认成判词最多多报一条未认出）。
* **门③「配平外溢」**（验收的否证记录，实测**不成立**但性质必须留痕）：曾猜「`Logf` 消息含未配平的
  ASCII 左括号 ⇒ 跨度外溢 ⇒ 后续行被标成日志」。**实测外溢确实发生，但那个 `t.Errorf(` 自己的调用点
  更晚出现、把它自己那行标回「判词」**——因为旧写法 `kinds[k] = kind` 是**后写覆盖前写**。
  ⇒ ★ 旧实现的**性质＝标记顺序相关、后者胜**；**门② 正是这条性质的反面**。**不许把「依赖顺序」当地基用。**
  ⇒ 本版**故意去掉顺序依赖**：种类取集合 + 「判词」优先，且配平改为在**抠掉字面量**后的代码上算
  （消息里的括号不该参与配平），并加结构自校验**「任一调用的跨度不许跨过函数边界」**（跨过 ⇒ 当场 rc=1）。
* **门①②③同一族**（`1865d35a`「取证范围不许窄于结论范围」的三种形态）；**根因是同一个**：
  **「红行总数」没有第二个来源** —— `known + unknown` 只与常量 `1` 比 ⇒ 「只红一条」与「只归出一条」
  读数相同。真修法是拿**独立的失败集合**对账（验收给的候选：`go test -json` 的失败测试集合），
  **PM 已登记为设计级改动，本轮不做**（我只补了免费的那半：`good` 里加 `rc != 0`——
  ★ 但它**关不掉静默**：P7b 那次 `rc` 本来就是 1）。
* ★ 顺手补的三笔（PM 转派）：**G5 哈希范围写全树**（7 → **34 个文件**，范围 ≥ 结论，不必再逐个声明
  「其余为什么不可能被写」）；**脚本自带 git 证据**（三态：干净／脏／**本树不在 git 索引里 ⇒
  不适用**——验收的探针把 `SRC` 指向 `out/` 的拷贝树，那里的 `git status` **必然**空，读成「干净」
  就是假绿）；**印「本轮零变异经过的表内断言」**（对称于表外的 ⊘ 栏：今天 **3/5**，
  断言2「真实参数必须拒绝」、断言3「合成反例被抓」零变异经过；**只印不判**，硬判据已由 PM 登记）。
* 控制组：P7 与 G6 都做成**脚本内**控制组（期望脚本红且具名）⇒ 今天 **4/4 变异 + 4/4 控制组**。


#### 7.2.3 判据之二：本节引用的**行号与计数一律现算**（附现场证据）

★ **本节的数值（0.5625／0.5／0.75）是算式，不会漂**；但**凡引用「某文件第几行」的地方一律现算**，
因为**行号会漂**——这不是理论担心，是**两次运行之间实拍到的**：
验收 07:05 独立算 `chain.go:34,70`／`:33,67,111,114`，**不到半小时后重生成变成 `:35,81`／`:34,78,124,127`**
（我在两次之间又改过那个文件）。
⇒ **本节的正文因此不抄任何行号**（抄了就会与现算的那份打架）；被排除文件的**清单、首次入库 sha 与时间、
逐 cand 行号**统一在 `docs/kind-mech-map.md` 的**「被排除文件清单」**一节现算印出。
★ 通则：**同一份产物里会漂的量只能有一个现算来源**，手写副本迟早与它对不上。

★★ **2026-09-20 验收的三条收紧（`msg-mu903mk4-fz`／PM `msg-mu903tfp-g0`），已落地：**

1. **两份读数各自钉一个 sha，不是矛盾**：验收 07:05 的读数对应 **`5d2b4cf`**（`chain.go:34,70`／`:33,67,111,114`），
   v4 的读数对应 **`ae95a11`**（`:35,81`／`:34,78,124,127`）。★ 它的判词照抄：
   **「两份行号都不是错的，各自钉在一个 sha 上；变的是文件，不是尺子。」**
   （它逐版本复算一致；`git diff --stat 5d2b4cf ae95a11 -- rios-sim/mech/chain.go` ＝ **+21/−8** ⇒「我又改过」成立。）
2. **划界规则从「自述」改成「起点 sha ＋ 命令」**：起点写死 `136e617^`，命令
   `git log --diff-filter=A --name-only 136e617^..HEAD -- '*.go'`，集合 ＝ 它的输出 ∩ Go 索引（生产 .go）。
   验收换两个起点各算一次得**同一集合**；手工枚举降级为历史说明。
3. **节标题不许比规则宽**：那一节只列**计入该列口径**的文件，并逐条印出区间内新增的**全部** `.go`
   （`chain.go` 计入、`chain_test.go` 不计入，理由由尺子给出＝不在 Go 索引里），
   避免读者把「清单」当成**文件全集**（`18f53299`：划边界要声明「我在哪停」）。

### 7.3 ★ 可证伪实验（等读数一到就能判，不用再想一遍）

| 项 | 内容 |
| --- | --- |
| **要什么读数** | **第 3 跳的实际治疗量**（与第 1 跳同一次施法内、同一目标序位） |
| **在哪个观测点上分岔** | **只看 n=3 一跳即可**（三读法在该跳互不相等，见 7.2 表）；**n=2 对区分读法零信息量**；**n=4 只用于确认斜率是否继续**（0.421875 ／ 0.25 ／ 0.75） |
| **读数从哪来** | ① **跨源**：能给出逐跳治疗量的第三方数据（现有的 DB 只有 `0.75` 一个数，**不够**）；② **实战**：实机线解锁后打一场带乌啾/明椒的关卡，抓逐跳治疗量痕迹（**现在拿不到**：实机线卡在同步弹窗）。⇒ **现有数据不够**，这一点必须写死，否则下一个人会以为「再查一次库就行」 |
| **判成 ① 等比的条件** | 第 3 跳 ＝ **0.5625 × 第 1 跳**（容差按痕迹打印刻度定，不许先写死） |
| **判成 ② 等差的条件** | 第 3 跳 ＝ **0.5 × 第 1 跳** |
| **判成 ③ 每跳倍率的条件** | 第 3 跳 ＝ **0.75 × 第 1 跳**（＝与第 2 跳同值） |
| **三个都不成立时** | ⇒ **第四种读法**（此时 §四 断言的**数值**也可能失效，必须回头重估） |
| **落地位置** | 只需改 `rios-sim/mech/chain.go` 的 `chainJumpScale`（现在的 `n ≥ 3 ⇒ ErrJumpUndetermined` 换成按读法计算），**判据与规格不许动** |

★ **第三个候选（③）是本实验最有价值的部分**：它说明「0.75」**可能不是衰减系数**而是**每跳倍率**——
**在它被排除之前，连「该在哪一跳读」这件事都不算定案**。
（读数拿不到是**等待**，读法没定是**分析**——前者等时间，后者现在就能想清楚，两件事要分开写。）

★★ **自我更正（留痕，不改旧话）**：本节上一版在这里写过「③ 会让 `n=2` 的断言也变成错的」。
**这句不准确**：③ 为真时第 2 跳**仍然是** `0.75 base` ⇒ §四 那条断言的**数值**照样成立，
废掉的是「**降低 25%**」这个**解释**（同一个数、两种含义）。
**结论不变、表述收紧**（同 `4decd843` 那类：一次乘 vs 乘一次，同一个短语两种读法）。

---

## 八、落地记录（2026-09-20，第一轮骨架已落）

* 代码：`rios-sim/mech/chain.go`（机制骨架：`ChainSpec`、`ErrJumpUndetermined`、`chainJumpScale`、`chainHealJumps`、工厂拒跑）
  ＋ `rios-sim/mech/chain_test.go`（本判据的可执行形态）。
* **判据结果（rc=0）**：逐跳序列 `1 跳 100 → 2 跳 75`；`max_target=3` ⇒ **拒绝**并给出
  `ErrJumpUndetermined`（**未定不猜**）；**合成反例 4/4 被抓**（丢掉衰减／方向弄反／把「降低 25%」
  读成「降到 25%」／序列不足）；坏规格 **5/5 拒跑**＋真规格放行。
* **已知盲区（显式登记为绿）**：「每跳都从基准乘」（第 3 跳 = 0.75）**本判据抓不到**——
  第 3 跳未定，故判据按裁定不覆盖它。这段会红，从而提醒后来者同步改本文档。
* **dormant 自证（两条）**：① `go test ./mech/`（现有田地/病害 golden）**仍 rc=0**；
  ② `python tools/check_simgo.py` **仍 rc=0、通过 27 项**（与改前基线逐项相同）。
* **`ping` 的机制名单**（上一版这里写的是「未实测」——**已实测，故改掉**）：`check_simgo.py`
  的守卫直接印出本二进制编译进来的机制名单 ＝ **`['chain', 'huai_shu_li.farmland', 'snow.field']`**
  ⇒ **`chain` 确实出现在名单里**（这是「注册成功」的正面证据）；同一次运行里另外三条机制层守卫仍绿：
  **点名一个没有的机制 ⇒ 拒跑**（不是静默忽略）、**名单空着时照常跑**（空层对判决的影响必须为零）、
  **有 unsupported 就拒跑**。★ 行为不受影响的理由仍是：**没有任何规格点名 `chain`**
  （`git grep -F '"chain"'` ＝ 0 处，且 `mech.Load` 只实例化被点名的机制）。
* ★ 上一版把「机制名单多一项」写未实测、把「无规格点名」写已核 —— **这个分栏是对的**：
  两者一个要构建二进制、一个只要读代码，**不该因为前者没做就把后者也含糊掉**。
  本轮补上实测后，两条都有读数了。

### 8.1 ★ 预注册勘误（§四 的「反向守卫」那条）

原文写：「把 `chain.atk_scale` 置 **1.0**（等价于无衰减）时判据必须红」。
**这条写法是错的**：若配置里 `scale` 就是 1.0，那么第 2 跳 ＝ 1.0 倍**本来就是对的**，
判据**必然绿**——那是「被已知条件预先决定的绿」（`fc4263c1`），**零信息量**。
⇒ **改成**：「**实现对 `scale` 不敏感**（配置给 0.75，实现恒返回满量）时判据**必须红**」。
测试里对应的反例是「丢掉衰减（第 2 跳仍满量）」这一条，**已实测能红**。
**判据的措辞改了、判据本身没有放宽**（改的是我写错的守卫表述，不是守卫的严格度）。

