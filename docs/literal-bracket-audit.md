# 第七类「无人读」候选：含方括号的键（口径、三条读数与守卫）

**手写文档**（不是生成物）。写它的会话：RIOS后端2（`session-37b2c3e2-6993-4a98-a7d2-5f09d62088a2`，别名「后端2」；本条 id 由 `de_session action=me` 现取，不凭印象）。
依据：PM 派单（本轮任务书，含方括号的键；对应两条 dtodo，id 为 8 位十六进制、**不是**提交也不是记忆 id ⇒ 本文不给它们贴命名空间标签），沿用 `docs/literal-dot-fix.md` 的甲／乙／丙先例。

**身份（缺一即「身份未知」）**

| 项 | 值 |
| --- | --- |
| 尺子 `tools/audit_coverage.py` | blob `1e51f8fcde4118247c2398d3ac009006cd74f9c8`／归一(LF) sha16 `467ca70b4e18e6aa`／1092 行／57807 字节 |
| 冻结副本（本文全部读数的对象） | `out/backend2-b1/frozen3/audit_coverage.py`，与入库字节**逐字节相同** |
| 所属树（量测时刻） | 提交 `12f35f7`；工作区与入库归一后**相同** ⇒ `source_dirty` ＝ 干净 |
| 库 `data/akdb.sqlite` | sha16 `30e08f0da83b2ca9`（mtime 2026-09-20 03:44:30） |
| 范围表 `data/ranges.json` | sha16 `63ea8a90a89ce24c`（mtime 2026-09-20 04:08:01） |
| 名册 `docs/roster-<uid>.md` | sha16 `d8a80e8f4784e331` |
| 量测之后树又前进 | 提交 `bb7353d` → 提交 `9d63b05`；★ 已核：`tools/audit_coverage.py`、`ak_tactic/`、名册在这两笔之间**都未变**（`git diff --stat`）⇒ 本文读数对象仍成立 |

> 全部读数可用 `out/backend2-b1/bracket_measure.py`、`bracket_denoms.py`、`bracket_guards.py` 重跑；
> 逐键明细与频次表在 `out/backend2-b1/bracket-measure.json`、`bracket-denoms.json`。

---

## 一、先接受更正：那条登记**规则为真、实例为真、因果写错了**

分三槽写（记忆 `de4c9be4`：合成一句「我错了」会把真的规则一起扔掉）。

| 槽 | 内容 | 判定 |
| --- | --- | --- |
| **规则** | `_LITERAL` 的字符类是 `[A-Za-z_@$][A-Za-z0-9_@$.]*`，**不含 `[` / `]`** ⇒ 含方括号的**整串**提不出来 | ★ **真**（读源码即可；本轮 213 种键里「规则①全键字面量命中 ＝ **0**」是**结构上必然**，不是量出来的巧合） |
| **实例** | 名册 431 位的键种里含方括号的 **213** 种，其中 **114** 种被 `is_read()` 判「有人读」 | ★ **真**（我独立跑出 213／114，与 PM 的和逐格相同，见 §六对账） |
| **因果** | 登记写的是「提不出来 ⇒ **恒判无人读**」 | ★ **错**。`is_read()` 有第②条：键含 `[X]` 时拿 **`X` 自己**去 `lits` 里找（`_VARIANT_RE` 取**第一个**方括号）⇒ 它们**不恒判无人读**，213 种里 114 种被判「有人读」 |

**真正的毛病换了位置**：判它的**唯一**通道是**变体名**，而当变体名是**通用词**（`self` / `atk` / `cost` / `def` / `enemy`）时，
「这个字面量出现过」与「读了这个键」是**两件事**。
⇒ ★ **不是「提不出来」，是「拿一个通用词当了整串键的证据」。**

这与 `docs/literal-dot-fix.md` §四第 2 条（修尺子反而把这 5 键从「无人读」栏划掉 ⇒ **假清账**）是**同一族**：
两处都是「**出现过 ≠ 被消费**」（记忆 `1bd38acb`；记忆 `66b8e5e0`）。

**量级（报洞必须带）**：213 种里有 **114 种（53.5%）**被判「有人读」，而其中只有 **10 种**有真的选择语境
⇒ ★ **104 种（48.8%）的「有人读」是变体名撞上**。

---

## 二、第三条读数：把第②条拆成两个来源（两个数，不许并成一个）

| 口径 | 判别式（原文） | 读数 |
| --- | --- | --- |
| **宽**（尺子现在的写法） | `is_read()` 第②条：`variant_of(key) in lits` | **114** 种 |
| **窄**（本轮新增） | 变体名出现在**变体选择语境**里（见下） | **10** 种 |
| **差** | 宽认、窄不认 ⇒ **假清账候选** | **104** 种 |

**窄口径的判别式（写清，可复算）**

1. **读侧三形态**（全部来自实测的真实消费点，不是猜的）：
   * `variants.get("kill")`——`ak_tactic/battle/unit.py:853,873`、`ak_tactic/frontend/operator_view.py:238,264`；
   * `effects.with_variant("second")`——`ak_tactic/battle/sim.py:2455`（怒潮凛冬技2 的 `headb2_s_2[second]`，已接线）；
   * `variants.get(_ADD_COST_PERIOD)`——`ak_tactic/operator/skill.py:406`；★ **参数是常量**，不是字面量。
2. **名字的两个来源**：同行**字符串字面量**，或同行**引用的模块级字符串常量**（第 3 条的形态要求解析常量——`out/backend2-b1/bracket_fixture/synth_case.py` 的格3 就是这条的敏感性证据）。
3. **写侧排除**：`variants.setdefault(` / `variant_units` / `variants[...] =` 出现在这些行上**不算**有人读——**生产者不是消费者**（同「名字表不是消费者」）。
4. ★ **必须声明扫描范围**：本轮＝`ak_tactic/**/*.py` **85 个文件**（选择语境命中 **11** 处、涉及名字 **5** 个）。
   范围之外一律判「没人读」——这条不是免责声明，是**判据的一部分**（见 §四·负控制）。

**另一条范围取证**：`*.go` 里搜 `variant` **0 命中**（命令：`git grep -n -i variant -- '*.go'`），
且 `ak_tactic/simgo/skills.py:152` 把「技能变体」列为**不能交给 Go** 的理由之一
⇒ ★ 这些键目前**只有 Python 侧消费路径**。

**三态（只在一处判，三处同口径）**

| 态 | 定义 | 读数 |
| --- | --- | --- |
| `真有人读` | 宽认 **且** 变体名出现在选择语境 | **10** |
| `仅变体名撞上` | 宽认 **但** 变体名没在任何选择语境出现 | **104** |
| `无人读` | 宽不认 | **99** |

★ 三处同口径（明细／括注／汇总）由**程序断言**保证，不是靠人对齐：
「真 ＋ 仅 ＝ 规则②命中 114」且「无人读 ＝ 规则②③都不中 99」且「合计 ＝ 213」⇒ 本轮 **通过**。
明细＝`bracket-measure.json` 的 213 行逐键；括注＝汇总行的同一组数；汇总＝末行。

### 三态逐键（`真有人读`，全 10 种）

| 键 | 变体 | 变体名字面量出现行数 | 需它的干员数 |
| --- | --- | --- | --- |
| `amiya2_s_2[kill].atk` | `kill` | 6 | 1 |
| `amiya2_s_2[kill].magic_resistance` | `kill` | 6 | 1 |
| `amiya2_s_2[kill].max_stack_cnt` | `kill` | 6 | 1 |
| `amiya_t_1[kill].sp` | `kill` | 6 | 1 |
| `closur_s_2[add_cost_period].cost` | `add_cost_period` | 2 | 1 |
| `closur_s_2[add_cost_period].interval` | `add_cost_period` | 2 | 1 |
| `closur_s_3[add_cost_period].cost` | `add_cost_period` | 2 | 1 |
| `closur_s_3[add_cost_period].interval` | `add_cost_period` | 2 | 1 |
| `headb2_s_2[second].atk` | `second` | 4 | 1 |
| `headb2_s_2[second].def` | `second` | 4 | 1 |

### `仅变体名撞上` 按变体名归并（104 种键，前 13）

| 变体名 | 键数 | 字面量出现行数 | 通用词（≥10 行） |
| --- | --- | --- | --- |
| `self` | 10 | 116 | ★ |
| `cost` | 15 | 30 | ★ |
| `b` | 11 | 4 | |
| `debuff` | 7 | 18 | ★ |
| `heal` | 7 | 13 | ★ |
| `ally` | 6 | 24 | ★ |
| `enemy` | 5 | 76 | ★ |
| `atk` | 5 | 62 | ★ |
| `dot` | 5 | 6 | |
| `a` | 4 | 7 | |
| `damage` | 3 | 34 | ★ |
| `def` | 3 | 34 | ★ |
| `magic` | 3 | 2 | |

（全表见 `out/backend2-b1/bracket-digest.md`；`无人读` 99 种里 `bonus`／`overload_start`／`peak_1`／`period`／`weak` 各 4 种，它们的变体名在源码里**一次都没出现过**。）

### 真实实例（PM 提供线索，我用**同一把尺子**复跑，不引用其转述）

线索是「同一天赋的两个分支被分到两栏」。复跑（`out/backend2-b1/bracket_pm_case.py`，rc=0）：

| 键 | 宽 | 窄 | 三态 | `gap_class` |
| --- | --- | --- | --- | --- |
| `huang_t_1[heal].hp_ratio` | **True** | False | `仅变体名撞上` | 1（真没建模） |
| `huang_t_1[lock].duration` | False | False | `无人读` | 1 |
| `huang_t_1[lock].min_hp_ratio` | False | False | `无人读` | 1 |

* 干员＝煌（`char_017_huang`）；`heal` **在** `lits` 里、**不在**选择器集合里；`lock` 连 `lits` 都不在。
* 频次核对：`heal` 字面量 **13 行 / 2 文件**（`ak_tactic/formula.py`、`ak_tactic/mechanics.py`）⇒ 与 PM 报的 13/2 **一致 ✓**。
* 与主量测 JSON 逐条对账：3/3 条都在 213 种里且三态**逐条一致 ✓**。

**答 PM 的三问**：
① 并排判词见上表（实测值，不是从描述推的）；
② ★ **窄口径把它翻出来了**（判 `仅变体名撞上`，不是「有人读」）⇒ 窄口径在这条实例上**够窄**、有分辨力；
③ ⇒ **它应当进报表列**（§七 的丙列 `variant_only`）：这一类正是「宽的判词与窄的判词相反」的那 104 种里的一个。
★ **一个推论（标明是推论）**：按**窄口径**，`heal` 与 `lock` **两分支都没人读** ⇒ 「两分支分栏」是**宽的产物**，不是这一族的分栏缺陷；
真正的状态是**这个天赋整体没接线**。要把它定成结论，还需一次独立取证（谁消费 `huang_t_1` 天赋），本文只到「窄口径判它没人读」为止。

---

## 三、守卫三件（缺一不可）

### ① 敏感性：四格夹具，**真跑**（不是 py_compile）

夹具 `out/backend2-b1/bracket_fixture/synth_case.py` 只有四格，格与格之间**只差「那一点东西」**：

| 格 | 内容 | 窄口径必须 | 实测 |
| --- | --- | --- | --- |
| 格1 | `_SYNTH_ALONE = "synthvariant"`（孤立字面量） | **不认** | 不认 ✓ |
| 格2 | `eff.with_variant("synthwired")` | **认** | 认 ✓ |
| 格3 | `_SYNTH_CONST = "synthconst"` ＋ `variants.get(_SYNTH_CONST)` | **认**（这条证明「解析常量」是必需项） | 认 ✓ |
| 格4 | `eff.variants.setdefault("synthwriter", {})` | **不认**（写侧不是消费者） | 不认 ✓ |

并先断言**夹具真被扫到**（1 个 `.py`）——否则「都不在」不可解释（记忆 `4791107f`：新来源要有「装进来了没有」的判据）。

### ② 控制组：同形状的键，只差「有没有选择语境」，**读数必须不同**

| 合成键 | 宽 | 窄 | 三态 |
| --- | --- | --- | --- |
| `synth_s_1[synthvariant].atk` | True | False | `仅变体名撞上` |
| `synth_s_1[synthwired].atk` | True | True | `真有人读` |

★ 两个键**形状相同、宽口径读数相同**，只有「选择语境」这一处在变 ⇒ 三态**不同**才算变量生效（记忆 `06192463`；记忆 `39642040`）。

### ③ 反向守卫：真接线的**不许**被窄口径判成没人读；桶**不许**是死的

* 三个已知真接线的名字——`kill`（4 键）、`second`（2 键）、`add_cost_period`（4 键）——**全部**判 `真有人读`；
  扫描器侧也各自在集合里、并印出处行（`battle/unit.py:853,873` 等）。任一被判「没人读」即 **rc≠0**（假红）。
* 另一侧（记忆 `09d1b503`：「不许什么都不吞」）：窄口径相对宽口径**收掉了 104 种** ⇒ 桶是活的，不是死桶。

**守卫脚本退出码**：`out/backend2-b1/bracket_guards.py` ⇒ **rc=0**（三件全过）。

### ④ ★ 我第一版的两处红（都是**我的尺子**侧，不是判据侧）

1. **合成反例第一次是红的**：我把**真树**的选择器集合喂给夹具里的键 ⇒ `synth_s_1[synthwired].atk` 被判 `仅变体名撞上`，
   于是**控制组同值**、脚本 rc=1。★ 红得对，但**因不是判据错，是范围错**：窄口径的读数**依赖扫描范围**。
   修法＝把夹具范围并进判据 ＋ 把「范围声明」写成判据的一部分；并**保留这条负控制**（同一把键用真树集合判就是「没人读」）。
   这是「取证范围不许窄于结论范围」（记忆 `1865d35a`）落在**判据自身**上的形态。
2. **频次那格印出「≥10 行 0 种」**：我先把名字集合收成「只出现在选择语境的名字」，于是 `self` / `atk` / `cost` 这些通用词
   **根本不在统计范围里**。★ 这是「0 命中只有两种含义」（记忆 `0752e3c2`）的第二种：**尺子问错了**，不是对象没有。
   修法＝频次的分母改成「该变体名在源码里以字符串字面量出现的行数」，**不加名字过滤**。

---

## 四、频次那一格：与 PM 的 66 **逐格相同**，但通用词代理**低估**

修正后（85 个 `.py`、30 个变体名）：

| 口径 | ≥10 行的名字数 | 覆盖键数 |
| --- | --- | --- |
| ① 字符串字面量行 | **13** | **66** ★ |
| ② 裸词行 | 24 | — |

★ **13 个 ≥10 行的变体名覆盖 66 键** ⇒ 与 PM 报的「≥10 行 **66 种**」**逐格相同**。
口径澄清（这是两个不同的量，差点被我读成不一致）：**PM 数的是键数，我第一版数的是名字数、且被过滤**。

★★ **但「≥10 行」是可疑度的代理，不是判据**：窄口径说这 114 键里有 **104** 键没有真选择语境——
其中 **38 键**的变体名出现 **<10 行**（`b` 4 行、`d` 3 行、`first` 1 行、`once` 1 行、`limit` 1 行…）。
⇒ 用「通用词」当筛子会**漏掉 38/104 ＝ 36.5%**。**判据是窄口径；「≥10 行」只能当排序用的先验。**

---

## 五、分母对账（第四问：992 与 1594／761 各自是什么）

**每个数一行写清定义；跨行不可比。**

| 数 | 定义（一句话） | 身份／出处 |
| --- | --- | --- |
| **992** | 名册 **431 位**的**键种**：技能 lv7／m3 ＋ 天赋 ＋ 特性**三来源并集去重** | 本轮实测（尺子 sha16 `467ca70b4e18e6aa`） |
| 3702 | 名册 **(干员,键) 去重对** | 本轮 |
| 4437 | **(干员,来源,键) 出现次**（同一键跨来源重复计） | 本轮 |
| 730 | **技能层**键种；★ 把 3728 个 (技能,等级,专精) 组合全走一遍**仍是 730** ⇒ 技能层键种**不随档位变** | 本轮 |
| 2543 | 全档位 **(干员,键) 去重对** | 本轮 |
| 600 / 771 | 第二道筛子**「无人读」**的 键种 / 需求对 | 本轮（调尺子自己的 `scan_ops` ＋ `classify_gaps`） |
| **594 / 761** | **第 1 类**（＝无人读键里 `gap_class` 五类**都不成立**者）的 键种 / 需求对 | 本轮；★ 与 `docs/literal-dot-fix.md` 的旧值 **594／761 逐格相同（差 +0）** |
| 1803 / 94 | **敌人侧**（`data/enemydb.sqlite` 的 `enemy_level.blackboard_raw` **解析后**）键种 / 含方括号键种 | 本轮；★ **口径未对齐**（敌人键带 `N.` 前缀、且未走 `$`／别名那几条）⇒ **未核**，只当线索 |
| **1594** | ★ **任何候选口径都不命中** | ★ **查无出处** |

**能归一个吗？不能。** 992 与 594／761 **不是同一个量**：
一个是「有多少种键」，另一个是「缺什么、且**该做**的」（中间隔着第一道筛子与五类分层）。
⇒ 按记忆 `6dd6739c`：**分开写，并显式标「跨行不可比」**。
★ 顺带一个正面结果：594／761 在**跨了若干笔提交之后仍然 +0** ⇒ 这两个分母**对这段区间的树位移不敏感**（这与「分母随对象每笔都在换」相反，值得记一句）。

**1594 的取证范围（写全称结论必须写范围）**：
在 `<仓库根>` **全树**（含 `out/`、`docs/`、`data/`）与 `%DSH_HOME%\.dsh\memories` **全轨**按字面量 `1594` 检索，
命中全部是无关数值（`docs/batch2-plan.md:35` 的**血量** 1594→2800、几份 gamedata JSON 的数值）。
⇒ ★ **它不是本仓任何现行口径的读数**；我逐个量过的候选（992／3702／4437／730／2543／600／771／594／761／996＝992＋4／2795＝992＋1803）**没有一个是 1594**。
⇒ 处置：**引用它的人应补出处**；在补出处之前，**不许拿它和 992 并列**。

---

## 六、仪器侧：选择器集合里有**定义处**假阳性（量级 0，但要写下）

选择器名字集合 **5** 个：`SkillEffects`、`replace`、`add_cost_period`、`kill`、`second`。
前两个的出处行落在 **`def with_variant(self, variant: str, *, mode: str = "replace") -> "SkillEffects":`** 这一行的**定义处**
⇒ **定义处被当成了消费处**（与「名字表被当有人读」同族）。
★ **量级**：键侧用到的名字只有 `kill` / `second` / `add_cost_period` ⇒ **对键态的影响 ＝ 0 种**（名字集合脏了、读数没脏）。
修法＝扫描时排除 `def` / `class` 行（一行代码）；本轮**没改工具**，只登记。

---

## 七、结论三句

1. 含方括号的键**不恒判无人读**（114／213 判「有人读」）；但其中 **104 种是假清账**（变体名撞上、无真选择语境），**真有人读的只有 10 种**。
2. 尺子**没有**「提不出来 ⇒ 全红」的效应（第②条接住了）；真正的洞是**判据太宽**：**用一个通用词当了整串键的证据**。
3. 这不是键的性质，是**判据的性质**（与第六类同形：修完应当**消失**，而不是登记）——但**修哪一层是裁定**，不是执行者定。

**待裁定（选项 ＋ 代价，照 `docs/literal-dot-fix.md` 的先例）**

* **甲「加排除」**：把 `is_read()` 第②条窄化（变体名必须在选择语境里出现；写侧不算；带范围声明）。
  代价：≈30 行 ＋ 一次重跑（≈6 分钟）。★ **读数会动**：全库「无人读」栏会**多回 104 种**，
  且**会动 A 名单**（覆盖收益只用第 1 类；这 104 种现在被算「有人读」⇒ 不计入缺口，收窄后就会计入）⇒ 落地甲必须重跑 `--select` 并逐位比对名单。
* **乙「不加」**：接受这 104 种被算「有人读」。代价 0；但「无人读」这一列在**含方括号的键上基本不可信**（114 里只有 10 真），引用时必须同时带这个例外。
* **丙「只改报表」**：`is_read` 不动，报表加一列 `variant_only`（＝宽认、窄不认），三态可分。
  代价：≈15 行（复用本轮 `out/backend2-b1/bracket_common.py` 的扫描器）＋重跑一次。
* ★ **我的建议＝丙**（与提交 `af1e8a6` 的处置同一口径：先让**三态可分**，不动已入库判据的语义），
  并把「**扫描范围**」写成那一列的**必备字段**（§三④ 那条红就是缺它造成的）。
* ★ **一条与主线相关的提醒**（不是反对，是代价）：那 10 种真有人读里，`second` 的**唯一**消费点在 `ak_tactic/battle/sim.py`，
  `kill` 的消费点之一在 `ak_tactic/battle/unit.py`；而 `battle/` 正在「能否被 Go 完全替代」的待验证清单上
  ⇒ 若 `battle/` 摘除，`headb2_s_2[second]` 会**由「真有人读」变成真的没人读**，本判据的读数会跟着变。`add_cost_period` 住在 `operator/skill.py`，与此无关。

---

## 八、复现命令（逐条可跑）

★ **仪器身份**（按 content sha16 认）：`out/` 被 `.gitignore:39` 忽略 ⇒ 仪器**只在磁盘上、不入库**
（「留痕的家」放哪按 PM 2026-09-20 的口径归博士定；本节不为此改造仓库结构）：

* `freeze_ruler7.py` ⇒ sha16 `1f3354fdb417cc20`
* `bracket_common.py` ⇒ sha16 `48a1b9108a7092b1`
* `bracket_measure.py` ⇒ sha16 `efd83ea6af1111b9`
* `bracket_denoms.py` ⇒ sha16 `a2c8b34fde6f408c`
* `bracket_guards.py` ⇒ sha16 `ed7b99ebeada1098`
* `bracket_pm_case.py` ⇒ sha16 `dca9311ba5627a8e`
* `bracket_digest.py` ⇒ sha16 `9efb893ee2f7c17e`

```powershell
cd <仓库根>
$env:PYTHONIOENCODING='utf-8'
python out/backend2-b1/freeze_ruler7.py        # 冻尺子＋两侧身份
python out/backend2-b1/bracket_measure.py      # ①主量测（≈6 min）
python out/backend2-b1/bracket_denoms.py       # ②频次重数＋第1类分母（≈6 min）
python out/backend2-b1/bracket_guards.py       # ③守卫三件（须 rc=0）
python out/backend2-b1/bracket_digest.py       # ④文档用摘要
git grep -n -i variant -- '*.go'                                 # ⑤Go 侧范围取证（0 命中）
```
