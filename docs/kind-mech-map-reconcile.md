# kind ↔ Go 落点表 · **对账**（验收方，独立复算）

> 对账对象：`docs/kind-mech-map.md`（**生成物**）＋ 生成器 `tools/kind_mech_map.py`，提交 **`83a11a4`**（后端2 出表）。
> 本文是**手写**的对账报告（不是生成物，不会被重生成冲掉）。
> 纪律：**出表方不自证，对账方不看它的数**——下面每个数都是我在本树重跑/重算得到的。
> **分工**：表由后端2 出（能改表的一方），对账由验收出（不改表的一方，`83825a62`）。

## 一、可复现性（先行，不然后面全是转述）

| 检查 | 我跑的 | 结果 |
|---|---|---|
| 反向守卫 | `python tools/kind_mech_map.py --check` | **rc=0**（控制组锚从索自身取、命中最多词 `omitempty` 107 处）✓ |
| 生成 | `python tools/kind_mech_map.py --md … --json …` | **rc=0** ✓ |
| 与它交的产物比 | 我重生成 vs `docs/kind-mech-map.md` | **逐字只差一行**——生成时树 HEAD（它 `83a11a4`／我 `cefa1b7`）⇒ 同口径可复现 ✓ |

尺子自检：`rios-sim` 下 29 个 `.go` = **15 生产 + 14 测试**，与它的读数一致；生产整词表我独立取到 **1490** 个（它报 1508，差 18 ⇒ 见 §四未核）。

## 二、列的独立复算（口径：**cands＝Python 落点字段名**，不是 kind 名）

★ 我第一遍按 **kind 名**在 Go 里整词匹配，得「有落点 10」，与它的 3 对不上。**不是它错，是我换了键空间**（`5383209a`）。按它的口径（`cands` 字段名）复算 ⇒ **一致**：

| 态 | 我的复算 | 它的读数 | 判 |
|---|---|---|---|
| 有落点 | **3**（`damage`、`ep_damage`、`targets`） | 3 | **一致** ✓ |
| 未核 | **20** | 20 | **一致** ✓ |
| 无落点 | **0**（未填） | 0 | **一致** ✓ |

kind 种数：我从 `ak_tactic/formula.py` 的 `RULES[*].kind` 独立取到 **23** 种，逐名与它相同 ✓。

## 三、★ 顶回去的两处（这是本次对账的产出）

### 3.1 「有落点 3」是 **kind 计数**，不同字段只有 **2** 个

`damage` 与 `ep_damage` **共用同一个 cand `damage_type`**，hit 明细（三条）**逐条相同**：

```
rios-sim/mech/huai_shu_li.go:369  DamageType string          `json:"damage_type"`
rios-sim/wire.go:165               DamageType     string      `json:"damage_type"`
rios-sim/wire.go:268               DamageType string  `json:"damage_type"`
```

⇒ 表里应显式标注「共用同一 cand」，或加一列 `distinct_cand`；否则读者会把 3 读成「三个独立落点」。
另外：**`damage_type` 是否同时也是 `ep_damage`（元素损伤）的落点，未核**——两把机制共用一个字段名不等于共用一个语义（同族 `180eab0c`：**一个字面量同时清两个键**）。
> ★ **本条已自纠（见 §七）**：「distinct 字段 = **2**」**是错的**，正解 **3**（`damage_type`、`atk_scale`、`max_target`）。
> **错在我自己**：我打印时只取了 `hits[:3]`，而 v1 的 `damage` 行有 **4** 条 hit，`atk_scale` 排在第 4 —— **是我的截断把它藏了**，与它主表的显示无关。

### 3.2 ★ `regen` 那一格**不是「未核」，是「cand 名错」**

它把 `regen` 放进「未核②（有字段名但 Go 0 命中）」，cand 是 **`heal_scale`**（与 `heal` 共用）。而 Go 生产侧**有真落点**，只是**名字不同**：

```
rios-sim/wire.go:206     RegenAura *RegenAuraSpec `json:"regen_aura,omitempty"`
rios-sim/wire.go:80      type RegenAuraSpec struct {
rios-sim/sim.go:2377     func regenAuraTick(ops []*operator, dt float64) {
rios-sim/sim.go:347      regenPerSec float64
rios-sim/mech/huai_shu_li.go:1169  func (fs *Farmland) RegenPerSecond(x, y int) float64 {
```

取证范围：`rios-sim/**/*.go` **排除 `_test.go`**（15 个生产文件），读法：**整词大小写敏感 + 全小写化两种**。
⇒ 处置：这一格的 `reason` 应从「Go 0 命中」改为「**cand 名与 Go 落点名不一致**」（已见一例）。**零命中是尺子的读数，不是机制的事实**——否则读者会把 0 读成「没有落点」。

## 四、它点名要我顶的三处 · 判决

1. **通用词命中** ⇒ **这一栏是干净的**：三处命中全部是**真字段**（struct 字段 + `json:` tag，`wire.go:165/268/271`、`mech/huai_shu_li.go:369`），没有取到注释或无关同名词。
2. **大小写/驼峰尺子** ⇒ **诊断列本身有洞**：`regen` 的同族词在 Go 里是 `Regen*`/`regen*`（§3.2 五行），而它按 `heal_scale` 派生的驼峰候选自然命中不到 ⇒ **诊断「该命中却没命中」这一自检成立**。判别式：**靠名字匹配永远抓不到「换了名字的落点」**（与上一轮「名字命中≠语义等价」是对偶面）。
3. **「未核」≠「无落点」** ⇒ **同意，并守住**：本轮「无落点」我**一个都没填**。我给的范围只能证明「**没有同名字段**」，证明不了「**没有机制**」（§3.2 就是反例：机制在，名字不在）。要判「无落点」，必须逐条读机制并把取证范围写上。

## 五、本对账**未覆盖**的部分（不许读宽）

* 15 个「Python 侧无可抽字段名」的 kind：我**未逐条核**其落点假设，只核了 kind 计数与态分布；
* 表内 `rules` 列与 `formula.py` 的对应关系：**未逐条核**；
* 第②桶里除 `regen` 外的 4 格（`control`、`ep_burst`、`ep_heal`、`heal`）：我只验了它们的 cand **整词 0、子串 0**（即「连子串都不存在」），**未**逐条读机制判「有/无落点」。

## 六、1490 vs 1508 **已定位**（PM 点名「不许悬着」）

**结论：差 18 个词，全部是「只在行尾注释里出现」的词。** 口径差**只有一处**：它的 `go_index()` 只跳过**以 `//` 开头的整行**；我另外剥掉**行尾注释**（`line.split("//")[0]`）。

**两个数不可混用**（同族 `6dd6739c`：两套分母的数不许并列）：

| 量 | 值 | 含义 |
|---|---|---|
| 全局差集 | **18** | 只在注释里出现过的词（它 1508 − 我 1490） |
| 逐行差集 | **52** | 至少有一处命中落在注释里的词（可能别处也在代码里） |

18 个词逐一带原文行（节选）：`STUN`@`control.go:28`、`UNABLE_ACTION`@`control.go:29`、`FROZEN`@`control.go:31`、`FROZEN` 一族共 11 个状态名、`INVINCIBLE`@`stealth.go:38`、`valve`@`mech/huai_shu_li.go:140`、`vs`@`element.go:118`、`N`@`sim.go:401`。

★ **对本次判决零影响**（逐 cand 复算，两种口径并列）：

| cand | 严格口径命中 | 含注释口径命中 | 判 |
|---|---|---|---|
| `damage_type` | **4** | 4 | 一致 |
| `max_target` | **1** | 1 | 一致 |
| `atk_scale` | **1** | 1 | 一致 |

且**表里 12 个 cand 与「仅注释词」的交集为空** ⇒ 那 3 格「有落点」**没有一处是注释撑起来的**。

★ **但风险是结构性的，不能因为这次没踩到就放过**：注释里的名字会被算成落点（`1bd38acb`：注释被当证据会让结论反转）。**建议**：`go_index()` 补一行剥行尾注释——本表恰好没踩到，**不等于判据守得住**。

取证范围：`rios-sim/**/*.go` 排除 `_test.go`（15 个生产文件），整词、大小写敏感，两种口径都算过。

## 七、对账 **v2**（表 `ce6a3ad`）

**收下的（我独立复算过）**：

* `regen` 改判 ✓：v1/v2 逐 kind 对拍，**23 格里只有 `regen` 一格变态**（未核→待换尺子），其余 22 格不变 —— 与它的声明一致；
* `1490` ✓：剥行尾注释后词表 1508→1490，与我一致；它把「两口径逐行判决相同」做成了**守卫第③条**（实测：剥=1490／不剥=1508、态不同的 kind=**无**）——★ **这比我的「逐 cand 复算」更强**（我核了 3 个 cand，它核了 23 行）；
* 四条守卫我重跑 **rc=0** ✓（敏感性、控制组、两口径、新档可行使）；
* 「distinct 字段 = 3」✓ —— **这一条是我错**，见 §3.1 自纠。

★ **顶回三处（都不是判据崩，是计数口径）**：

1. ★ **「3 处命中」是 3 条同一行**：v2 命中表里 `atk_scale` 出现 **3 次**、`max_target` 出现 **3 次**，**三行逐字相同**（都是 `wire.go:273` / `wire.go:271`）。原因：cand 抽取按 `formula.py` 行**逐条出候选项**（`atk_scale＠1669·out`、`＠1670·out`、`＠1671·note` 是同名三条），命中表又**按候选项展开** ⇒ 同一个 Go 行被数了三遍。
   **用它的索引量**：`atk_scale` **1 处**、`max_target` **1 处**、`damage_type` **4 处**（`mech/huai_shu_li.go:369`、`wire.go:165/268/432`）。
   ⇒ 命中表要**按 `文件:行` 去重后再计数**；否则「处数」会随 Python 侧抽取次数变化（0～N 倍）。★ 与它 v1 的幽灵 kind **同源**：**都是按「抽取次数」计数，不是按「对象」计数**。
2. ★ **共用表漏一个、重一个**：实际共用字段 **2** 个 —— `damage_type`（`damage`/`ep_damage`）与 **`heal_scale`（`heal`/`regen`）**；v2 主表印 **1**，且把 `damage_type` 那一行**印了两遍**（`ep_damage、damage` 与 `damage、ep_damage`）。
   ⇒ **漏掉的正是 regen 那条缺陷的本体**：共用表若一开始就列 `heal_scale`，「regen→heal_scale」当场就会显形。**共用表不是附注，是这类缺陷的探测器。**
3. **v2 对我「净字段 2」的归因只对了一半**：v2 产物里写着「v1 主表每行只显前 2 处命中，把 `atk_scale` 藏了」——**我的 2 与它主表的显示无关**：我读的是它的 **JSON**，v1 的 `damage` 行有 **4** 条 hit，`atk_scale` 排第 4，是**我打印时 `hits[:3]` 自己截掉的**。
   ⇒ **「2」与「3 处」都不是「位置数」**——两边的截断方向不同（它的显示截断／我的打印截断），后果相同。建议四个量分开写：**kind 计数 3 / distinct 字段 3 / 共用字段 2 / 去重后命中处数（`damage_type` 4、`atk_scale` 1、`max_target` 1）**。
   > ★ **本条署名已更正，见 §8.1**：那句话的**源头**是 PM 的推断，v2 只是**写入**了它。

**未核**：它自报的「幽灵 kind（`enemy` 被当成 kind、`max_target` 算成与 `enemy` 共用）」——`"enemy"` 在 **v1 与 v2 的交付产物里各出现 0 次**（`enemy` 字面只出现在 `enemy_control` 内）⇒ **修前/修后在产物上都无法核**（可能只存在于中间运行）。当前状态与它的描述自洽（幽灵 0），但「修了什么」我**不当既成事实**。
> ★ **本条已于 §8.2 销掉**：它把中间态做成了可重跑脚本，我亲自跑过（rc=0）。

## 八、对账 **v3**（表 `b3c1cee`）＋ 一条署名更正

### 8.1 ★ §七 第 3 条的**署名更正**（PM 认，我跟着改）

**事实（我核了产物）**：那句话**确实写在 v2 的产物里** —— `out/acceptance/kind-mech-map.v2.md:17`（我留存的复跑副本，mtime 06:27:45）：
> …**不是谁错**——v1 主表每行只显前 2 处命中，把 `atk_scale`＠…

**但源头不是它**：PM 自认那是**它在广播里替后端2 做的推断、且没标「这是我推的」**。
⇒ **我的引用「它写」——文本上没错，署名槽不全**：我只署了「写入产物的一方」，**没署「归因的源头」**。三方归属应为：

| 槽 | 是谁 |
|---|---|
| 归因的**源头** | PM 的广播推断（**未标来源**） |
| **写入产物** | 后端2 v2 表第 17 行（引用了该推断，未标引用） |
| **我方引用** | 我按产物署名归给后端2（漏了源头一栏） |

后端2 v3 已把该行改成「**两边各错一半**」（我的 `2` 是我自己 `hits[:3]` 截断；它的 `3` 是按抽取次数重计）。
★ **通则**：**「产物里写着」与「这是谁说的」是两个槽**；转述别人的归因必须标来源，否则它就从「某人的说法」变成「双方默认的事实」（`2d6f0f2d`）。

### 8.2 v3 的复核（我重跑）

* 四条守卫 **rc=0** ✓（敏感性／控制组／两口径逐行相同／新档可行使）
* ★ **幽灵 kind：「未核」→「已核」** —— 我**亲自跑** `tools/phantom_kind_evidence.py`：**v1（`83a11a4`）认到 9 个名字，其中不是 kind 的 `['enemy']`**（来源行 `ak_tactic/formula.py:1684 elif t.kind == "targets" and t.op == "enemy":`）；**v2 认到 8 个、幽灵 0**；**rc=0**。
  ⇒ **它把「消失的中间态」变成了随时可复现的取证**，比我当时记「产物里查不到 ⇒ 未核」更进一步。**这条未核我销掉。**
* ★ **命中处数两个口径我都独立复算，逐项一致**：

| 口径 | `atk_scale` | `damage_type` | `max_target` | 合计 |
|---|---|---|---|---|
| 含本轮新增（现树） | **3** | 4 | **5** | **12** |
| 不含本轮新增 | **1** | 4 | **1** | **6** ＝ 我先前独立量的 **1/4/1** ✓ |

  chain.go 新增明细：`atk_scale` ← `rios-sim/mech/chain.go:34,70`；`max_target` ← `:33,67,111,114`。
  ⇒ ★ **自指成立**：它本轮的骨架文件 `rios-sim/mech/chain.go` **自带 `json:"atk_scale"`／`json:"max_target"`**，同一把尺子把**骨架算成了落点**（`35c2d66d②`）。**这不是表错，是「树变了」**——我量的 `1/4/1` **只在没有该文件的树上成立**（该文件于 `5d2b4cf` 入库，mtime 06:32:47）。
  ★ **建议补一格**：把「不含本轮新增文件」那一列的**被排除文件清单**印出来；否则「本轮新增」会随轮次漂移，下一个人不知道到底排除了什么（表里已有树 HEAD，但 HEAD 每轮前进）。
* 它自陈的那支全库引号检查（2743 处／216 文件）：**它自己写明「没抽样定性、只当逐文件门」** ⇒ 我**未复算、未定性**，如实记为「自陈未定性」，不当作结论引用。

**遗留**：§五 的 15 个「Python 侧无可抽字段名」仍未逐条核（**声明，非漏**）。

## 九、对账 **v4**（表 `ae95a11`）· PM 点名的四处

### 9.1 ① 「本轮新增文件清单」——sha/时间对，**边界可验但需起点 sha**

* **`rios-sim/mech/chain.go` 首次入库 = `136e617`、2026-09-20 06:32:18** —— 我独立跑 `git log --diff-filter=A` **逐字相同** ✓
* ★ **同一次提交还新增了一个 `.go`：`rios-sim/mech/chain_test.go`**（`git show --name-status --diff-filter=A 136e617` 给出两行）。清单里没有它。
  ⇒ **按它自己写的划界规则**（「本轮新增**且会被同一把尺子算成落点**的文件」）**排除 `_test.go` 是对的**（尺子本来就跳过 `_test.go`），`6` 不受影响 ✓。
  ⇒ 但**节标题比划界规则宽**：「本轮新增文件清单」会让读者以为这是**文件全集**（`18f53299`：标题要么写成规则，要么别用全称）。**建议补一行**：「同提交还新增 `chain_test.go`，按划界规则不计」。
* ★ **边界能不能从 git 验**：**能，但要给它一个起点 sha**。我换两个起点各算一次，得到**同一集合**：
  ```
  git log 83a11a4..HEAD  --diff-filter=A --name-only -- rios-sim/**/*.go  ⇒ chain.go、chain_test.go
  git log 136e617^..HEAD --diff-filter=A --name-only -- rios-sim/**/*.go  ⇒ chain.go、chain_test.go
  ```
  ⇒ 现写法「手工枚举的路径集合（**不是 tag、不是起点 sha**）」是**自陈式**的：读者无法独立判定清单是否完整（本次它恰好对）。**建议把划界规则改成「起点 sha ＋ 该命令」**，手工枚举降为历史说明——这一步与上一轮「印被排除清单」是同一件事：**把分母消掉。**

### 9.2 ② 行号漂移 —— **成立，两组数各自钉在一个 sha 上**

| 读数 | 对应版本 | `atk_scale` | `max_target` |
|---|---|---|---|
| 我 07:05 独立算 | **`5d2b4cf`**（06:33:15） | `:34`、`:70` | `:33`、`:67`、`:111`、`:114` |
| 它 v4 现算 | **`ae95a11`**（07:07:45） | `:35`、`:81` | `:34`、`:78`、`:124`、`:127` |

* 两个版本的行号**逐项复算一致** ✓（`git show <sha>:rios-sim/mech/chain.go` 逐行取）
* `git diff --stat 5d2b4cf ae95a11 -- rios-sim/mech/chain.go` = **+21 / −8** ⇒ 「我随后又改过那个文件」**成立** ✓
* ⇒ ★ **这就是「行号不能写死」的现场证据**：**两份行号都不是错的，各自钉在一个 sha 上；变的那个量是文件，不是尺子**（同族 `ccfbc875`／`85eb2fb8`：比之前先写死两个 sha）。

### 9.3 ③ 「盲区必须被证明是盲区」——**真在跑、会红，但它不独立**

* **真在跑** ✓：基线 `go test ./mech/ -run Chain -v` **rc=0**，输出里第 112 行有它自己的日志：
  > `✓ 盲区有理由：候选 ③ 与本实现在 n=2 **同值**（75）⇒ 第 2 跳对区分读法零信息量；分岔点只有 n=3 一处（0.5625 ／ 0.5 ／ 0.75）`
* **会红** ✓：在**副本**（`out/acceptance/mut_chain/`，产品树未动）里把候选 ③ 的第 2 跳 `75 → 80`，**rc=1**，第 109 行正是它自己的判词：
  > `候选 ③ 的第 2 跳 80 与本实现的 75 不同 ⇒ 判据本该能区分它，「已知盲区」的说法不成立（这是真漏网，不是边界）`
  ⇒ **不是「必然绿」**（`fc4263c1` 的形态被排除）。
* ★ **但独立性不成立——这是我要顶回的一处**：**同一个变异红了两条**：
  * `chain_test.go:100` 已知盲区用例应当通过…实际红了（差 5）
  * `chain_test.go:109` 上面那条新断言
  原因是**它俩共享同一个 `blind` 数组**：盲区用例要求 `blind[1] == 100×0.75`（判据期望值），而实现过判据要求 `seq[1] == 100×0.75` ⇒ **两条绿就推出 `blind[1] == seq[1]`** ⇒ **新断言在算术上被前两条蕴含**，`d8706dc5` 的形态（判据被算术必然满足 ⇒ 零信息量）。
  ⇒ **它测的是「字面量 75」与「实现算出的 100×0.75」**，而这两个数**由同一对常量（`chainBase`、`chainScale`）决定**；判词里那句「若不同值 ⇒ 该判据本可区分它」**是推断，不是它测到的东西**。
  ⇒ ★ **真正的盲区本体是它的上一条**（候选 ③ 的**形态** `[100,75,75]` 能过本判据）——**那条才是实测**。
  ⇒ **建议（构造性）**：把候选 ③ 做成**一个真读法**（生成器 `candSeq(n) = 每跳 × 基准`），新断言改成**两个生成器**在 n=2 的输出比较；并把 `blind` 与候选序列**解耦**，使**最小变异只红一条**——**「一个改动红几条」本身就是这条断言独立性的判据**（`b53e0d7f`）。

### 9.4 ④ 三方槽 —— 与 §8.1 **一致** ✓（源头＝PM 推断／写入＝它 v2／引用＝我；无需再取证）

### 9.5 本轮**未复跑**（按 PM 交代，如实标注）

`check_simgo.py` 的机制名单与通过 27 项、表守卫 rc=0、引号检查（它的文件）0 处 —— **未复跑**；我**跑过**的是 `go test ./mech/ -run Chain -v`（rc=0）与其副本变异档（rc=1）。

**遗留**：§五 的 15 个「Python 侧无可抽字段名」仍未逐条核（**声明，非漏**）。

## 十、对账 **v5**（`5686f9a`）· PM 派单四项 ＋ 表数字

工具：`tools/acceptance_chain_indep_probe.py`（**我的**探针，直接 import 后端2 的脚本、只注入 `SRC`／`WORK`／`MUTATIONS`，**不复制、不改原件**）；读数落在 `out/acceptance/probe*.log`。产品树**只读**，一切变异在 `out/acceptance/probe/` 内。

### 10.1 ★★ ① 矩阵真跑了吗 —— **真跑** ✓

* 我自己跑 `python tools/chain_judge_independence.py`：**rc=0**，四行与我复算的一致（M1→断言1、M2→断言5、M3→断言6、M4→断言1，各 1 条）。
* **产品树没被动过**：跑前／跑后 `rios-sim/mech/*.go`＋`go.mod` **逐文件 SHA256 未变**、`git status --short -- rios-sim` **空** ✓。
* **变异确实落在副本里**（否则「产品树没变」只证明没被改）：跑完后 `out/backend2-b1/indep/rios-sim/mech/chain.go:109,111` 留有 **M4 的痕迹 `return 1.0, nil`**，且每次变异 `rc=1` ⇒ 变异到达了目标那行 ✓。
* ★ 知情项：`stage()` 每个变异前 `rmtree` 副本 ⇒ 副本**只保留最后一次变异**，中途态不可回看（不是缺陷，但复查者要知道）。

### 10.2 ★★★ ② 「按判词前缀归类」可靠吗 —— **一半可靠，且失败的那一半是静默的** ✗

判别式（PM 给的）：**「归类器认不出来」必须是可观察的失败。** 我造了两种形态：

| 探针 | 现场 | 脚本表现 | 判定 |
|---|---|---|---|
| **P1 判词被改名** | 把「读法判别器红：」改成「读法判别器不通过：」，其余不动 | M2 一行报 **红 0 条 ✗**、**rc=1** | **可观察** ✓ |
| **P2 表外判词 ＋ 表内同时红** | 在断言5 的同一块里加一条**判词不在表内**的断言（`future 新断言：…`），与 M2 同条件触发 | 真实 `go test` **红 2 行**（`chain_test.go:179 future…`、`:183 读法判别器红：`），脚本报 **「红 1 条 ✓」、rc=0** | ★ **静默吞掉** ✗ |

* ⇒ **同一份输出，红了两行，矩阵照样漂亮、退出码照样 0**——正是 PM 担心的形态。
* **覆盖量是量过的**（P4）：`chain_test.go` 里 `Errorf/Fatalf` 判词 **17 条**，`ASSERTIONS` 表只认 **5 条**，**认不出 12 条**。其中 4 条是 helper 的返回文案（只出现在被包装的 `实现未过判据：…` 行里 ⇒ 实际已覆盖）；**真正能独立红的表外判词 ≈8 条**（`TestChainSpec_RefusesBadConfig` 的 6 条 ＋ 2 条 `Fatalf`）。
* **现状是否已触发**：**没有**——表里 4 个变异各自只红 1 条覆盖内断言 ⇒ 这一笔的矩阵是**真的**。**缺的是通道**，不是当前的读数。
* **修法（构造性）**：归类器要**同时输出「没认出来的红行」**，并让 **未认出 ≥1 ⇒ rc=1**（具名列出）；或者把 `ASSERTIONS` 改成**从 `chain_test.go` 抽取全部判词**再归类。判据就是这句：**「认不出」必须自己会红，不能靠「恰好没有第二条」。**

### 10.3 ★★ ③ `expect` 自校验 —— **当场退出、且不印矩阵** ✓

把 M1 的锚点改成找不到的串（只改第一处）⇒ 脚本输出 `[SystemExit] ✗ 变异锚点在 mech/chain.go 里出现 0 次（应为 1）⇒ 变异没到达目标那行，读数无效`、**rc=1**，且**没有印出「矩阵」与「判定：」** ✓ ⇒ 不会拿一份看似正常的矩阵糊弄人（同族 `bd07345f`／`7eb5adf9`）。

* ★ 顺手量到一处**易踩**：`MUTATIONS` 里 **M1 与 M4 的 `old` 串逐字相同**（`"\tcase 2:\n\t\treturn scale, nil"`，各自 1 处命中，故不违反自校验）。但改锚点时**只改一处就会留下另一处**（我探针第一版就踩了：断言 `count==1` 直接失败）。建议 M4 的锚点写长一点以区分，或加一条「`MUTATIONS` 内锚点两两不同」的自校验。

### 10.4 ★ ④ n=3 三读数 —— 独立复算**一致** ✓

`① 等比 = 100×0.75² = 56.25`／`② 等差 = 100×(1−2×0.25) = 50.0`／`③ 每跳 = 100×0.75 = 75.0`，**两两不等** ✓；与 `docs/chain-skeleton-plan.md` §7.2 一致（文中 `0.5625`／`0.5` 是比率，③ 的「每跳都 ×0.75」⇒ 第 3 跳 0.75 ⇒ 值 75）✓。

### 10.5 ★ 顺手：`go_index` 加 `__filelist__` 后表数字**未变** ✓

* `kind_mech_map.py --check` **rc=0**、重生成 **rc=0**；五项逐项复算：**有落点 kind 3**／**distinct cand 3**／**命中处数 12**／**不含本轮新增 6＝1/4/1**／**共用字段 1／2**／**待换尺子 1** —— 与它报的**逐项相同** ✓。
* `__filelist__` 在 JSON 产物里出现 **0 次** ⇒ 元数据键没有漏进词表或命中；`_n_words()` 的效果＝这五项与上一版**逐位一致** ✓。
* **环境那条我复现了** ✓：不带 `PYTHONIOENCODING` 跑同一脚本 ⇒ **rc=1**、`UnicodeEncodeError: 'gbk' codec can't encode character '\u2713'`；带上 ⇒ **rc=0**。⇒ 「中途 rc=1 是环境不是判据红」成立。★ 一处字符级更正：先打不出的是 **`✓`（U+2713）**，它信里写的是 `⇒`——**类别对，字符别差一个**（同族 `9cd94fde`：红/绿先问被测对象还是仪器）。

### 10.6 ★ 自我披露：我这台探针也犯过同一个错

探针 v1 把 `SRC` 与 `WORK` 指到同一棵树，而 `stage()` 第一件事是 `rmtree(WORK)` ⇒ **它先删掉了自己的源码**，报 `FileNotFoundError`。v2 又把 `ROOT` 算深两层（副本比原件深两层），把树指到不存在的路径；v2 的「真实对照」还从**产品树**（而非探针树）拷了一份，于是那条对照**少算了一半**。
⇒ **三次都不是判据错，是仪器错**；它们之所以没变成假读数，只因为**都大声失败了**（异常而非空输出）。**这条要记在我自己头上**：`06c2d722` 说「红/绿都先问被测对象还是仪器」，我这次是**仪器先错了两轮**才问对。

**遗留**：§五 的 15 个「Python 侧无可抽字段名」仍未逐条核（**声明，非漏**）。

## 十一、**预注册**：复核 §10.2 那条静默通道被消掉没有（下次提交用）

> 写这一节的时机＝**在看到修改之前**。目的：先钉死「消掉」长什么样，免得事后各说各的（同族 `e009a70b`：预注册守卫自己也要被判据证）。

**对象**：`tools/chain_judge_independence.py` 的归类器 `red_assertions()` ／ `ASSERTIONS` 表（下一次提交）。
**命令**：`python tools/acceptance_chain_indep_probe.py`（我的探针，直接 import 它的脚本）＋ `python tools/chain_judge_independence.py`（它的原档）。

| # | 判据 | **红**长什么样 | **绿**长什么样 |
|---|---|---|---|
| **G1** | ★ **双向判据**：P2 形态（表外判词 ＋ 表内同时红）必须被当失败 | **现状**：真实 `go test` 红 2 行，脚本报「红 1 条 ✓」、**rc=0** ⇒ **现在必红** | 脚本 **rc≠0**，且在输出里**具名列出**那条没认出的红行（不是只改 rc 不提名字） |
| **G2** | 归类表覆盖量必须**可解释**：`chain_test.go` 里 `Errorf/Fatalf` 判词 17 条，表内现在只认 5 条 | 仍只认 5 条且**没有任何说明** ⇒ 红 | 要么表覆盖全部可独立红的判词，**要么**显式印出「哪些不覆盖、为什么可以不覆盖」（如 helper 返回文案只出现在被包装的行里） |
| **G3** | `MUTATIONS` 内**锚点两两不同**（现 M1 与 M4 的 `old` 串逐字相同） | 仍逐字相同 ⇒ 红（改一处锚点会留下另一处） | 锚点互异、或加一条自校验挡住 |
| **G4** | **不许放宽**：原 4 个变异仍各自**恰好红 1 条** | 出现红 0 条或 ≥2 条 ⇒ 红 | 4 行全 ✓、`rc=0` |
| **G5** | 产品树**只读**（新归类逻辑不得把变异写回产品树） | `git status --short -- rios-sim` 非空、或跑前后 `SHA256` 变 ⇒ 红 | 哈希逐文件相同、`git status` 空 |

* ★ **G1 是双向的**，这正是它的价值：**现在跑它必然红**（我已在 §10.2 实测 rc=0），修好后必须转绿。**一条判据如果现在和修好后都绿，那它什么都没测。**
* 声明：我**不会**因为 G2 是「说明题」就放宽 G1；两条独立记。
* 复跑时会把本轮 `out/acceptance/probe*.log` 与新读数**并列**留档，不覆盖。

## 十二、对账 **v6**（`e9ef3a7`）· 按 §十一 预注册逐条复验 **G1..G5 全部成立** ＋ 一个**新入口**

**被测身份**（复验前钉住，因工作区可能仍脏）：`tools/chain_judge_independence.py` 最后一笔 = **`e9ef3a7` 07:30:42**、工作区 `sha256[:16] = B3B1CFFB95A33E15`。
**仪器**：`tools/acceptance_chain_indep_probe.py`（我这份，**直接 import 它那一份**，只注入 `SRC`／`WORK`／`MUTATIONS`）＋ 独立对照＝**手工施加变异后用 `go test`（不带 `-v`）按我给定的判词原文数针**。读数 `out/acceptance/probe-v9.log`、`indep-v6.log`。

| # | 判据 | 本轮读数 | 判 |
|---|---|---|---|
| **G1** | 双向：预注册形态必须**当场红且具名** | 我的树（表外判词＋表内同红）⇒ 它 **rc=1**、`✗ M2 …认不出 1 条`、具名 `chain_test.go:179: future 新断言…（表外判词 ⇒ 归类器认不出）`；我的独立对照：rc=1、针命中 **`读法判别器红：`1 ＋ `future 新断言`1 ＝ 真实红 2 条** | **✓ 成立** |
| **G1′** | 它两个控制组**都没覆盖**的分支：**行号不在任何已知调用区间** | 间接接收者（`tt := t` ⇒ `CALL_RE` 的 `\bt\.` 认不出该行）⇒ 它 **rc=1**、具名 `chain_test.go:180: P5 间接接收者…（行号不在任何已知调用区间内 ⇒ 认不出）`；对照：rc=1、针 **2 条**（`读法判别器红：`＋`P5 间接接收者`） | **✓ 成立**（fail-closed 两个方向都在） |
| **G2** | 覆盖量须**讲得通** | 它按**调用处**印 **13 ＝ 表内 5 ＋ 表外 8**，8 条逐条具名＋结构理由，另 2 条 `bad` 文案注明「不自己发消息 ⇒ 结构上不能独立红」 | **✓ 成立** |
| **G3** | 锚点两两不同 | M1／M4 已写长带上上下文；我**注入重复锚点当反向守卫** ⇒ 它 `rc=1`、`✗ G3 自校验失败（改锚点会留下另一处）：变异之间旧串重复（2 处）` | **✓ 成立（反向守卫真会红）** |
| **G4** | **不许放宽** | 4/4 各恰红 **1** 条表内断言、**0** 条认不出、`rc=0`；我的三棵树各自也都读到 4/4 | **✓ 成立** |
| **G5** | 产品树只读 | **我自己算**（不靠它脚本内打印）：7 个文件跑前跑后哈希**无差异** | **✓ 成立** |
| **P3** | 锚点坏必须当场退出且**不印矩阵** | `[SystemExit] ✗ 变异锚点在 mech/chain.go 里出现 0 次…`；`含「矩阵」False`／`含「判定：」False` ⇒ 已是**结构保证**（矩阵先算后印） | **✓ 成立** |
| ★ **一处「不适用」** | 它的 P1 控制组在我那棵**已改名**的树上 0 命中 | 它记 `⊘ 不适用（外部树已施加该改动）⇒ 本树上通道证明不完整`，**rc 仍红、不许因此变绿** | **✓ 处置正确** |

### 12.1 ★ 我的尺子被它纠正了：17 是**尺子产物**，真值 13

它报的机理我**复算并确认**：我的 P4 正则 `t\.(?:Errorf|Fatalf)\(` **子串命中 `fmt.Errorf`**（`fm` ＋ `t.Errorf`）⇒ 多出 **4** 条，正是 `chain_test.go` 第 **46／49／53／56** 行的 helper 返回文案。加词边界 `\bt\.` 后 **＝13**，与它按调用处数的 13 一致 ⇒ **两个口径合成一个，分母消失**（`61d85582`：能合成一个口径就先消掉分母）。

* ★ **要更正的话**：我 §10.2 写的「17 条／认不出 12 条」**是尺子给的数，不是被测对象的数**；真值 **13／8**。
* ★ **但我的结论没塌**：我当时说「真正能独立红的表外判词 **≈8**」——那是**用手减掉 4 条**得到的，与现在量出来的 8 **一致**。**结论对、数字错，错的在尺子**（`9edb4bdd` 同族）；我已把 `\bt\.` 修进探针，并在 §十一 的 G2 里把「17」更正为「13」。
* 它这次**没动我的仪器、只报机理**，分寸是对的。

### 12.2 ★★ 新发现（G6，建议登记为下一条）：**同包另一个 `_test.go` 里的红，它看不见**

`RED_RE = ^\s+chain_test\.go:(\d+):\s?(.*)$` —— **文件名写死**；而它跑的是 `go test ./mech/ -run Chain`，**这是整个包**。我造了一棵树：产品文件 ＋ 新增 `mech/zz_probe_test.go`（`TestChainForeignProbe`，基线下**绿**、M1 下**红**）。

* 它：**`rc=0`、全绿**、M1 行写「表内红 1 条、**认不出 0 条**」、判定「通道与矩阵都成立」。
* 我的独立对照（手工 M1）：**rc=1**、针命中 `实现未过判据：`1 ＋ **`P6 外文件的红`1**、出现过的测试文件 **`['chain_test.go', 'zz_probe_test.go']`**。
* ⇒ **同一份运行，两个文件都红了，矩阵照样全绿**——**与 §10.2 那条同一族的另一个入口**：上一条是「**认不出的判词**不计入」，这一条是「**别的文件里的红**不计入」，本体都是**取证范围窄于结论范围**（`1865d35a`）。
* **今天是潜伏的**：`mech/` 里能匹配 `-run Chain` 的测试只有 `chain_test.go`（另一份 `farmland_golden_test.go` 的测试名不含 Chain）。**但构造可达**（上面就是我构造的），且**任何未来新增的同包 Chain 测试会立刻踩上**。
* **修法（便宜）**：`RED_RE` 放宽为 `^\s+(\S+_test\.go):(\d+):` 并按**文件**取 kinds；或对**任何非 `chain_test.go`** 的红行直接判「未认出 ⇒ 具名 ＋ rc=1」（fail closed）。判据一句话：**红行的取证范围必须 ≥ 运行范围**（`-run` 跑几个文件，就得看几个文件）。

### 12.3 我本轮自己的仪器错（第三次同型）

第一版对照我**在没施加变异的树上**跑 `go test`，量到的自然是日志；修成「手工施加变异」后，又不带 `-v` ⇒ **失败的测试会把它的全部 `t.Logf` dump 出来**，形状与 `Errorf` 相同，于是数出「真实红行 13／14 条」这种**假读数**。
⇒ 定式：**对照读数要按「我给定的判词原文」数针，并报出出现过哪些 `_test.go` 文件**——后者不依赖任何行号分类。**这正是它用行号分辨 Logf 与 Errorf 的同一个理由**（`06c2d722`：红/绿/空都至少要有两个来源；这里我的第二个来源是针，不是行数）。

**遗留**：§五 的 15 个「Python 侧无可抽字段名」仍未逐条核（**声明，非漏**）。

## 十三、整族重读（同一根轴：**取证范围 vs 运行范围／结论范围**）· 五面扫描 ＋ **第三处**

**被测身份**：`tools/chain_judge_independence.py` **`e9ef3a7`**，我把它**冻结**成只读副本 `out/acceptance/pinned/chain_indep.e9ef3a7.py`——`git cat-file blob` 原始字节、`sha256[:16] = b3b1cffb95a33e15`，**与我审计起点记的工作区值逐字节相同** ✓；探针经 `CHAIN_INDEP_PIN` 只读这一份（后端2 当时正在改该文件，工作区已到 476 行／`A1A5731445A147EA`）。读数 `out/acceptance/probe-p7b.log`。

**同一族已两次**：§10.2「**认不出的判词**不计入」、§12 G6「**别的文件里的红**不计入」。⇒ 按同族纪律（`00205485`）**整族重读**，找第三处。

| 面 | 读数 | 三态出口 |
|---|---|---|
| **1 `-run Chain` 的实际匹配范围** | 量了：`rios-sim` 下 **15 个** `_test.go`、**89 个**测试函数；**匹配 `Chain` 的只有 `mech/chain_test.go` 的 2 个**（`TestChainJudge_AtkScale_FirstTwoJumps`／`TestChainSpec_RefusesBadConfig`）。脚本读 **1** 个文件 | **⊘ 不适用**（今天没有第二样本；但**这就是 G6 的家**，已派后端2） |
| **2 每条断言的「归属」：「一条都没红」vs「不在视野」** | 表内 **5** 条断言，4 个变异**只经过 3 条**（断言1／5／6）；**断言2「真实参数必须拒绝」、断言3「合成反例被抓」本轮零变异经过**——而脚本对**表内**没有这一态（**表外** 8 条反倒有理由栏） | **成立**（不对称：表外有「为什么不覆盖」，表内没有「本轮没经过谁」） |
| **3 `MUTATIONS` 施加范围 vs 判据看范围** | M1／M4 改 `mech/chain.go`、M2／M3 改 `mech/chain_test.go`；判据只看 `chain_test.go`；`go test ./mech/` 覆盖整包。查「改 A 看 B」是否真发生：全仓 grep `"chain"`／`chainMech`／`mech.New`／`Register` 在**根包 0 命中**、根包测试里 `mech.` **0 命中** | **⊘ 不成立缺口**（改动目标是小写标识符 ⇒ 可见性封在 `mech` 包内、包外零消费点；取证范围＝`rios-sim` 全树 `.go`，用上述四模式） |
| **4 `go test` 的 rc 进没进判据** | `rc` 全脚本只用在 **2** 处：基线 `L321 if rc != 0 or known or unknown`、控制组 `L343 good = bool(unknown) and rc != 0`。★ **变异行 `L331 good = (len(known)==1 and not unknown and known[0]==expect)` —— `rc` 完全没参与** | **成立**（见下 13.1） |
| **5 G5 的范围：7 个文件是谁定的** | `tree_hashes()` = `rios-sim/go.mod` ＋ `rios-sim/mech/*.go` ＝ **7**；而 `rios-sim` 下共 **34** 个文件 ⇒ 覆盖面 **7／34**。★ 脚本里 **`git` 零命中** ⇒ 脚本**自己没有**全路径证据（我上轮用的 `git status` 是我另做的外部证据，不是它的） | **成立（有保留）**（见下 13.2） |

### 13.1 ★★★ 第三处（成立）：**同行两个调用，整行被判「日志」⇒ 同行的 Errorf 红被静默丢弃**

构造（`probe/p7`）：在被认判词同一块里插一行 `if … { t.Logf("P7b 同行日志"); t.Errorf("P7b 同行被吞的红：%v", …) }`。

* **它**：**`rc=0`、四行全 ✓、认不出 0 条**、判定「通道与矩阵都成立」。
* **我的独立对照**（手工 M2）：**`rc=1`、针命中 `读法判别器红：`1 ＋ `P7b 同行被吞的红`1 ＝ 真实红 2 条**。
* **机理**：`CALL_RE.search` 取**行内第一个**匹配 ⇒ 该行 kind＝「日志」；`classify` 里 `if kind == "日志": continue` 这一支 ⇒ **同一行的 Errorf 红被当作日志丢掉**。**这一条 G6 的修法关不掉**——G6 放宽的是**文件名**，这里丢的是**行的种类**。
* **修法**：① `kinds` 一行存**多个**种类（`set`），分类时**该行出现过错词就按判词算**（fail-closed 优先于「日志」否决）；② 或让「日志」这一否决**只作用于同时不含任何判词字样的行**。

### 13.2 另两条成立项的修法

* **面2**：把**表内**也印出「本轮零变异经过的断言」并给理由（对称于表外的 ⊘ 栏）；更强的写法是把它做成显式判据——**「每个表内断言至少被一个变异经过」，否则红色登记**（今天会是 2/5 ⇒ 红）。
* **面4**：`good` 补一条 `rc != 0`（免费、严格更强）。★ 但要说清它**关不掉静默**：P7b 那次的 `rc` 本来就是 1。**真正缺的是「红行总数」的第二个来源**——现在 `known＋unknown` 只与常量 `1` 比，**「只红一条」与「只归出一条」读数相同**（`e764fd7f` 家族）。可用的第二来源：`go test -json` 的**失败测试集合**对账 `scan_calls` 的 `funcs` 归属（能抓 G6 那类跨文件红）；至于**同一函数内**少归出一条，只能靠与基线输出做差集当**上界**。
* **面5**：把哈希范围写成 `rios-sim` **全树 34 个**（成本近乎为零），或显式声明「其余 27 个为什么不可能被写」——注意那是**当前代码的性质**，不是**结论的范围**。

### 13.3 否证记录（**⊘ 也要写**）：括号外溢那条**不成立**

我先构造的是「`t.Logf` 消息里含未配平的 ASCII 左括号 ⇒ 跨度外溢 ⇒ 后续行被标成日志」。**实测不成立**：外溢确实发生了，但**我那个 `t.Errorf(` 自己的调用点在同一遍扫描里更晚出现、把它自己那一行重新标回「判词」**（`kinds[k] = kind` 是**后写覆盖前写**）⇒ 被救回来了；那一轮它的行是**表外判词**（具名 + rc=1）。
⇒ **性质记录**：`scan_calls` 的标记**顺序相关**、**后者胜**，且每个调用点都会覆盖自己首行（多行调用的整段也被自己的跨度覆盖）。这条性质使「外溢」单独不致命，**但也意味着「谁最后写的」决定了种类**——P7b 就是这条性质的反面。

### 13.4 我本轮自己的仪器错（第四次）

① 把 P7 的**报告行**插到了 `show` 定义**之前** ⇒ `NameError`（构造对了、报告崩了）；② 头一次看 P7 现场时，**我自己的 grep 把真读数滤掉了**（过滤词里没有「✗ M2」「认不出」），差点把「被吞」读成「没触发」。
⇒ 都是仪器，不是判据；**都大声失败了**，所以没变成假读数。

**遗留**：§五 的 15 个「Python 侧无可抽字段名」仍未逐条核（**声明，非漏**）。

## 十四、复验 `5b0a143`（G6 ＋ P7b ＋ 面5 ＋ `rc != 0` ＋ 印零经过断言）：**双向全部成立**；★ 并给出一条新判据

**被测身份**：`git cat-file blob 5b0a143:tools/chain_judge_independence.py` ⇒ 冻结 `out/acceptance/pinned/chain_indep.5b0a143.py`，`sha256[:16] = ce583302829c3b33`、**604 行**（与它自报逐项一致 ✓）。读数：我 `out/acceptance/probe-v10.log`、它 `out/acceptance/indep-v7.log`。

| 面 | 改前 `e9ef3a7`（我实测过） | 改后 `5b0a143` | 双向 |
|---|---|---|---|
| **F** 同包另一个 `_test.go` 的红 | rc=0、全绿、静默 | **rc=1、具名** | ✓ |
| **P7** 同行 Logf+Errorf | rc=0、全绿、静默 | **rc=1、具名** | ✓ |
| C／D／E 表外判词·行号区间外 | rc=1 具名 | rc=1 具名 | 未回退 ✓ |
| G3 反向守卫（我注入重复锚点） | rc=1 | rc=1 | ✓ |
| H 锚点坏 | `SystemExit`、不印矩阵 | 同（`含「矩阵」False`） | ✓ |
| I 我的尺子 | 13 ＝ 5 ＋ 8 | 同 | ✓ |
| J **我自算**哈希 | 7 文件无差异 | 7 文件无差异（它自报已改全树 34） | ✓ |
| G4 不许放宽 | 4/4 各恰 1 条 | 4/4 各恰 1 条、0 认不出 | ✓ |

它另自报：**零变异经过的表内断言 3/5 已印**（断言2／断言3，只印不判）、G5 全树 34、git 三态（干净／脏／本树不在索引 ⇒ 不适用）。

### 14.1 ★★ 新判据：**P7 控制组的绿，可以被一条「恒在的 benign unknown」满足**

它的 P7 控制组自报：

```
✓ P7 同行 Logf+Errorf：rc=1、表内红 1 条、认不出 2 条
   ↳ chain_test.go:178: P7 同行日志（表外判词 ⇒ 归类器认不出）
   ↳ chain_test.go:178: P7 同行被吞的红：78.75（表外判词 ⇒ 归类器认不出）
```

它种下 **1 条**判词、却产生 **2 条** unknown —— 其中「P7 同行日志」是**日志文本**（它自己提问的那一条）。

⇒ 而控制组的判据是 `bool(unknown) and rc != 0`：**只要那条恒在的日志文本在，这条控制组就必然 ✓**。**若哪天「同行被吞的红」回归成不被认出，它照样绿。** ⇒ **零信息量的绿**（`fc4263c1`／`ecf7b3f7`）。
⇒ **本体＝控制组的取证范围（unknown 集合里任意一条）宽于结论范围（本控制组种下的那条被认出）**——**同族第四次**，与 §10.2／§12（G6）／§13.1（P7b）同一根轴。

**修法（三态，可执行）**：

1. **控制组自带 needle**：`good = rc != 0 and any(种下的原文 in u for u in unknown)`，并单独印「本控制组种下的那条在不在未知里」（这正是我 §十一 预注册 G1 写的「**具名列出**」，当时只用在判据上、没用在控制组上）；
2. 同行双种类派生的那条 unknown **单列一栏 ⊘ 未定性**（同一行两种调用 ⇒ 凭行号分不出输出文本的来历），**声明它不参与控制组判定**；
3. 若保留它的 rc 影响（**方向是对的，不要退回**——同行两调用必须按判词算），则控制组判据必须与它**解耦**。

⇒ 关键：**「未知」这一栏现在混了两种东西**（真红但表外的判词 vs 同行派生的日志文本）＝**两种不同的东西压成一个值**（`1b6ee8e9`）。

### 14.2 未验项（**不许算通过**）

它新加的结构自校验 **`spans_stay_inside_funcs`**（任一调用的跨度不许跨过函数边界，跨过即 rc=1）——**本轮我没有构造，故记「未验」**，不写「通过」。若 PM 要，我按 §13.3 那条性质的反面构造一次（把一次调用写成跨函数边界）。

## 十五、复验 `5b0a143` 的**加问四面**（PM 派单）：三面实测、一面未构造；★ 并按要求把「门关了」与「根因仍在」分栏

**工具**：`tools/acceptance_scope_faces_probe.py`（一进程一用例，输出 `out/acceptance/scope-<case>.log`）。被审文件同 §十四（冻结副本 `ce583302829c3b33`）。

### 15.1 问①「判词优先于日志」的反面——**成立（实测：表内计数被日志正文污染）**

**构造 `faceA`**：一行里 `t.Logf("实现未过判据：自陈日志 %v", …)`（正文＝**表内断言1 的前缀**）＋ `t.Errorf("验收面A 表外红：%v", …)`（**故意用表外文案**）。同行 ⇒ 该行种类＝判词（判词优先）⇒ 该行的**所有**输出文本都按判词算。

| 用例 | M2 那一行 | 真值 |
|---|---|---|
| `control`（未注入） | ✓ rc=1、**表内红 1 条** `['断言5 读法判别器']`、认不出 0 条 | M2 只红断言5 |
| `faceA`（注入） | ✗ rc=1、**表内红 2 条** `['断言1 实现过判据', '断言5 读法判别器']`、认不出 1 条 | **断言1 在 M2 下不红** |

⇒ 多出来的「断言1」**只可能来自那行 Logf 的正文**（同行的 Errorf 进了「认不出」栏）⇒ **日志正文进了表内计数**，把「恰红 1 条」这条**分辨率**打破（**假红**）。
⇒ 代价有两面：**① 多报 unknown（方向安全）**；**② 表内计数被污染（分辨率失效）** —— 且 §14.1 那条「P7 控制组恒真」正是①的后果。**「一行一种类」这个模型表达不了「这一行会打出两种文本」。**
⇒ 修法：行给**候选集合**，最终归类再用**消息前缀**逐条判定（匹配不上判词前缀的一律进 ⊘ 未定性，且**不参与计数与控制组判定**）。

### 15.2 问②`spans_stay_inside_funcs` 会不会误伤——**不误伤，且它不是死代码**

* **不误伤（`faceB`）**：合法的**多行调用**、实参含**匿名函数字面量**（放 `if false` 死代码里）⇒ 它 **rc=0**、四个变异行与 `control` **逐行相同**、输出里**无**「跨过」。
* **敏感性有实测（`faceC`）**：真让它失控时，它**当场大声失败** ——
  `✗ 结构自校验失败（调用跨度跨过函数边界 ⇒ 括号配平失控 ⇒ 种类标注不可信）：chain_test.go:179-263 的调用跨度跨过了 joinLines（第 211 行）`。
* **残余（未构造）**：`FUNC_RE = ^func\s+(\w+)\(` 只认**顶格具名函数**边界 ⇒ **同一函数内**的配平失控它抓不到。

### 15.3 问③`_strip_literals` 会不会把真问题换成假绿——**不会，是换成了大声失败**

`_strip_literals` 处理 `"`／`'`／反引号 与 `//` **行注释**，**不处理 `/* */` 块注释**。`faceC` 就是在多行调用实参里放 `/* ( */` ⇒ 那个 `(` 仍参与配平 ⇒ 跨度失控 ⇒ **被 15.2 那条守卫当场判红**（rc=1、具名）。
⇒ **真问题没有变成假绿**，变成了**在早阶段大声失败**——这正是可恢复性的前提（`e7be3da5`）。

### 15.4 ★ 问④（最重要）G5 那 34 个哈希算的是**哪棵树**——**算的是 `SRC` 树**

`tree_hashes()`：`files = sorted(p for p in SRC.rglob("*") if p.is_file())` ⇒ **范围跟着 `SRC` 走**。

* 它**自己**跑时 `SRC = ROOT/"rios-sim"` ＝ **产品树** ⇒ G5 恰当地取证了产品树 ✓；
* 我**把 `SRC` 指到 `out/` 拷贝树**时（我的探针就这么做）⇒ **G5 量的是拷贝树**；而 `git_evidence()` 此时**正确地判「不适用」**（四例日志逐条印证：`不适用 —— scope-faces\faceX\rios-sim 不在 git 索引里（拷贝树/被忽略）`）⇒ **第三态在我这种注入下真的生效了** ✓
* ⇒ **那一次运行里「产品树只读」并没有被取证**（一个证据指向别处、一个不适用）。但**产品树在结构上未被触碰**：脚本的写路径只有 `SRC`／`WORK`（`stage()` 只读 `SRC`，`mutate()` 只写 `SRC`）。
* ⇒ ★★ **「门关着」与「门被取证过」是两件事**，必须分开写（见 15.5）。
* 建议：G5 打印**具名两棵树**，`SRC ≠ 产品树` 时写明「产品树本次未被触碰（写路径只有 SRC/WORK），但**未被哈希取证**」——**「不适用」必须具名它量的是谁**。

### 15.5 ★ 分栏结论（PM 要求）

**① 三扇门是否都关上了（本轮验收对象）——都关上了，逐条实测双向**：G6（face F：rc=0→rc=1 具名）、P7b（rc=0→rc=1 具名）、配平外溢（`faceC`：失控 ⇒ 当场 rc=1 具名）。**且 G4 未放宽**（4/4 各恰 1 条）、G3／H／I／J 未回退。

**② 根因是否仍在（登记项、不在本轮）——仍在，一处没动**：**「红行总数」没有第二个来源**（`known＋unknown` 只与常量 `1` 比）。三扇门关掉的是**已构造出来的三个入口**，不是**这一类**：本轮我又从「修法自己带来的新面」里取到**第四个入口**（15.1：日志正文进表内计数）与**一条恒真控制组**（§14.1）。⇒ **不许把「三扇门关了」读成「这一族没问题了」。**

### 15.6 两条不确定项（**不许算通过**）

* 后端2 自陈「门②在我那份探针日志到达前就已发现并修了」——**⊘ 痕不足、不可判定**。并列痕：它的工作区最后写 **07:53:29**、提交 `5b0a143` = **07:53:45**；我的真读数日志 **07:47:57**、发给 PM 的简报 **07:50:01**；它 **07:42:52** 给我的信里只提 G6。**并列时间点，不判它说谎。**
* 15.2 的「同一函数内配平失控」——**未构造**，不写「通过」。

### 15.7 我本轮自己的仪器错（第五次）

`faceB`／`faceC` 首跑在**打印之后**崩于 `NameError: name 'v' is not defined`（我改了打印行却留了旧变量）⇒ 我用脚本 rc 与「它 rc」两个数分别复核，并**四例重跑**后采信。**读数在被打印之后崩掉仍有效，但那份日志不能当交付**——重跑前我特意确认这点，避免把「崩了」与「读数假」混成一个判断。

## 十六、复验 `1b07c85`（问①修法 ＋ 问④修法）：修法生效，★ 但露出**第五个入口**与**两处新缺陷**

**冻结身份**：`tools/chain_judge_independence.py` @ `1b07c85` ⇒ `out/acceptance/pinned/chain_indep.1b07c85.py`，`sha256[:16] = 8ec701cc3478b68b`、**704 行** —— 与它自报**逐项一致** ✓。读数：`out/acceptance/probe-v11.log`（主探针）、`scope5-*.log`（四面探针）、`indep-v8.log`（它自己跑）。
**复验器**：`tools/acceptance_chain_indep_probe.py`、`tools/acceptance_scope_faces_probe.py`（均为我自己的仪器）。

### 16.1 三扇门＋新桶：主探针双向仍全绿

`C`／`D`／`E`／`F`（同包另一个 `_test.go`）／`P7`（同行 Logf+Errorf）**逐段 rc=1** ✓；`G`／`H`／`I`（13＝5＋8）／`J`（7 文件哈希无差）全对 ✓；`faceC`（块注释致配平失控）**仍当场 rc=1 具名** ✓。G4 未放宽。

### 16.2 问① 修法**实质生效**（我上一轮的构造被它抄进脚本内置控制组）

同一棵 `faceA` 树、同一变异 M2：

| 版本 | 表内红 | 未认出 | ⊘ 未定性 |
|---|---|---|---|
| `5b0a143`（旧） | **2 条 `[断言1, 断言5]`** | 1 | （无此桶） |
| `1b07c85`（新） | **1 条 `[断言5]`** ✓ | 0 | **2 条，都具名** |

⇒ **日志正文不再进表内计数** ✓（我的差分构造与它的复现读数**逐字相同**，两方独立）。

### 16.3 ★★ 但露出**第五个入口**：同行多调用行上，「未认出」这一栏**结构性恒为 0**

它在脚本内的控制组逐条印出了 ⊘ 的具名原因，里面**有两条是真红**：

```
↳ 具名（⊘ 未定性）：chain_test.go:178: P7 同行被吞的红：78.75（… 同号多调用、消息归属不定 …）
↳ 具名（⊘ 未定性）：chain_test.go:178: faceA 隔离用：表外文案（… 同号多调用、消息归属不定 …）
```

`P7 同行被吞的红：78.75` 与 `faceA 隔离用：表外文案` **都是 `t.Errorf` 的真红、且都匹配不上判词表** —— 按这一族的定义，它们**应当进「未认出」**（第一扇门的本名就叫「表外判词不计入」），现在被贴上 **⊘ 未定性**。
⇒ **这不是静默**（rc 仍然红：矩阵行要求 0 未定性、基线要求 0、控制组 `undet_min`——三条出口我在它的运行里逐条核过，它自陈「流程内没有静默路径」**成立** ✓）。**错的是标签**：一条「表外判词」被写成「归属不定」，会把诊断方向引到「这行是不是有两个调用」，而真相是「这条红不在判词表里」。
⇒ **准入条件用错了东西**：⊘ 今天收的是「**前缀匹配不上表**」的文本，而不是「**结构上不可归属**」的文本。而这一行的两条文本**本来可以按各自调用的字面前缀归属**（字面量就在源码里）⇒ 收窄后应为 **0 条 ⊘**，那两条真红应回到「未认出」。

### 16.4 ★ 新缺陷 F1：`⊘ 表内期望不适用` 的判据是**路径**，不是**内容**

`scope5-control` 跑在**一棵逐字节相同于产品树的拷贝树**上（`rios-sim/go.mod` ＋ `mech/*.go` 原样复制），M1–M4 的读数与产品树**逐项相同**（各恰红 1 条、0 未认出、0 未定性）——**它却连打 5 次**：

```
⊘ 表内期望不适用：SRC ≠ 产品树（rios-sim）⇒ 判词文案可能被外部改过，**这一栏不计入判定**
```

⇒ 判据是 **`SRC` 路径 ≠ 产品树路径**，而它想表达的是 **`ASSERTIONS` 各前缀在该树里是否原样存在**。**内容相同却宣布「看不清」**——这是把「看得见」误报成「看不见」。修法：判据换成**内容**（比对各判词前缀在该树 `chain_test.go` 里的存在与唯一性，或直接比该文件 sha），路径只用来决定「要不要做这项比较」。

### 16.5 ★ 新缺陷 F2：汇总行与明细**不同口径**（`45fab0e7` 同族）

同一轮 `scope5-control` 里并排出现：

* 明细：**5 次** `⊘ 表内期望不适用 …这一栏不计入判定`
* 汇总：`== 判定：4/4 变异各恰红 1 条表内断言（0 未认出、0 未定性）、5/5 控制组都按自己的期望红并具名 …==`

⇒ **同一份产物里，同一栏（表内）既被判「不计入判定」，又被汇总成「4/4 成立」**。读者无从知道该信哪个。修法：汇总必须**分列**——`表内 ⊘ 不适用（N 条控制组／M 条变异行）`，或至少在括注里写明「其中表内一栏 ⊘」。★ 这一条与 F1 合起来才致命：**F1 让它无故宣布不适用，F2 让这声宣布不进汇总** ⇒ 「不适用」两侧都不落地（既没理由，也没计数）。

### 16.6 ★ 我对它两处请示的裁定

**问 A（外部树上记 `⊘ 表内期望不适用`，通道判据照旧）——**算正确落法**，但附三条硬条件**：

1. **判据必须机器可判且是内容**（见 16.4；今天的路径判据是**误报**）；
2. **必须具名原因并可观察**（它做到了 ✓：`SRC ≠ 产品树 ⇒ 判词文案可能被外部改过`，外加 `★ 另有 1 处控制组「不适用」⇒ 本树上通道证明不完整`——**这句认识是对的**）；
3. **汇总必须分列**（见 16.5），且 `⊘ 表内期望` **只允许在它成立的那个条件下出现**——在**内容相同**的树上出现即判红（否则「不适用」会静默扩大成控制组放水的后门）。

**问 B（`⊘ 未定性` 要不要驱动 rc）——**不必**，但要**收窄准入**（二选一，不许既宽又不驱动）**：

* **不驱动 rc 我认**：它自陈的三条出口（基线把 ⊘ 算进「本该为 0」／矩阵行要求 0 未定性／控制组 `undet_min`）**我在它的运行里逐条核过、都拦得住** ✓ ⇒ 流程内确实没有静默路径。
* **但准入必须从「前缀匹配不上」换成「结构上不可归属」**（见 16.3）：只有**同一行内两条调用的字面前缀相同／互相覆盖**时才进 ⊘；其余按各调用的字面前缀归位。
* **验收判据（反向守卫）**：*在同一行里放一条**表外判词**（与一条 `Logf` 同行）⇒ 它的红**必须进「未认出」**且 rc≠0，**不许进 ⊘***。★ **今天跑这条守卫必红**（16.3 那两条就是反例）⇒ 它证明 ⊘ 现在是**桶**、还不是第三态。
* **若选择不收窄**：那 **⊘ 必须直接驱动 rc**（`7bd9ca65`：宁可假红）。今天的直接代价是「合法同行多调用 ⇒ 矩阵行必红」——它自己量过产品树上此类行 **0 行**，所以这条路今天也走得通。

### 16.8 ★ 新缺陷 F3：问④ 只落地了一半——**恰好在需要它的那个分支上缺席**

| 运行 | `G5 具名身份`（两棵树 ＋ `SRC 就是产品树`） | `产品树未被本次哈希取证` 那句 | 汇总 |
|---|---|---|---|
| `SRC == 产品树`（它自己跑，`indep-v8.log`） | **印了** ✓ `SRC = …rios-sim／产品树 = …rios-sim／SRC 就是产品树：True` ＋ `全树 34 个文件` ＋ `git 证据：干净` | 不需要 | `…产品树只读 ⇒ 通道与矩阵都成立` |
| `SRC ≠ 产品树`（我的探针，`scope5-control.log`） | **没印** ✗ | **没印** ✗ | `…**产品树只读** ⇒ 通道与矩阵都成立` ✗ |

同一轮的外部树上，它自己的 git 证据那一行明明写着 `**不适用** —— scope-faces\control\rios-sim 不在 git 索引里（拷贝树/被忽略）` —— **即那一轮产品树既没被哈希、也没被 git 取证**（正是我 §15.4 指出的那一次），而**汇总照旧宣称「产品树只读」**。
⇒ **F3 与 F2 同形**：第三态写进了明细，**没写进汇总**；而且在**恰好需要它的分支上，明细本身也没印**。
⇒ 修法（三条，都很小）：① `SRC ≠ 产品树` 时**也**印两棵树身份 ＋ 我 §15.4 那句「产品树本次未被触碰（写路径只有 `SRC`／`WORK`），但**没有被本次哈希取证**」；② 汇总行**按实际取证的栏生成**（产品树未被取证 ⇒ 不许出现「产品树只读」）；③ 与 F1／F2 合并成一条纪律：**「不适用」必须在明细、括注、汇总三处同口径**——只落一处就等于没落。

### 16.9 未验／不确定

* **F1 的修法我未构造验收**（我没有它「按内容判」的实现）——只给判据与今天的误报实测。
* 问② 的残余（同一函数内配平失控）**仍未构造**；问③ 的块注释面 PM 已登记、不要求它动。
* 根因（**红行总数没有第二个来源**）**仍未动**：本轮它修的是**入口**与**桶**——这正是 PM 说的「每修一处，露出下一处」。

## 十七、复验 `0a32629`（F1／F2／F3 ＋ 问A／问B ＋ 第二来源）：**三处修法逐条通过、第二来源行使成功**，★ 另量到 F4 与**我自己的一次假红**

**冻结身份**：`tools/chain_judge_independence.py` @ `0a32629` ⇒ `out/acceptance/pinned/chain_indep.0a32629.py`，`sha256[:16] = d2d14aa28f69c4fd`、**877 行** —— 与自报逐项一致 ✓。读数：`raw-new0a32629.log`（**未过滤原文**）、`probe-v12.log`、`scope6-*.log`、`indep-v9.log`、`na-guards.log`、`revguard-raw.log`。

### 17.1 ★★ 先更正我自己：F3 的前半是**我造成的假红**（第六次仪器错）

我在 §16.8 写「`SRC ≠ 产品树` 时那两行**没印**」。后端2 指出那是**我的过滤器**造成的。我按**未过滤的原文**重取（把冻结副本当模块载入、`redirect_stdout` 直接落盘，不经任何 `pats`），旧版 `1b07c85` 在外部树上**三句都在**：

```
== G5 具名身份：SRC = …\scope-faces\control\rios-sim
              产品树 = …\out\acceptance\rios-sim
              SRC 就是产品树：**False** ==
   ★ SRC ≠ 产品树 ⇒ **产品树本次未被触碰**（写路径只有 SRC ／ WORK），**但它没有被本次哈希取证**（上面那份哈希量的是 SRC 树）
   ★ 「门关着」≠「门被取证过」：要取证产品树只读，请在 SRC ＝ 产品树 的那次运行里看这一行。
```

⇒ **它是对的，我错了。** 根因在我自己的两件仪器上：主探针 `show()`（`tools/acceptance_chain_indep_probe.py` L237-247）**按 `pats` 过滤**（无命中时只印末尾 4 行），scope 探针按行首与关键词过滤 —— 我**拿被过滤过的日志去判「有没有印」**，**把仪器的视图当成了对象的行为**（与 `556104e7`「仪器坏了会伪装成对象不存在」同族，只是这次**伪装成「对象没印」**）。
★ **实质那一半成立**（且后端2 也认了）：同一轮里**汇总照旧宣称「产品树只读」**，而同轮 git 证据自陈「不适用」。
★ **纪律（写给下一个人）**：**「没印」是一个关于原文的断言——它只能用原文复核，不能用任何带过滤的日志复核**；而这一次是**对方替我抓到的**（`0258f567` 独立复现的价值，反向也成立）。

### 17.2 三处修法：逐条实测通过（且都在**外部树**上验，那正是旧版弱的分支）

| 项 | 旧版读数 | 新版读数（我实跑） |
|---|---|---|
| **F1** 表内期望适用性 | 逐字节相同的拷贝树上连报 **5 次**「不适用」 | `== 表内期望的适用性（按**内容**判，不按路径）：… = **适用** ==`、`⊘ 表内期望不适用` **0 处** ✓ |
| **F2** 汇总口径 | 明细 ⊘×5 与汇总「4/4 表内断言」并排 | 汇总按**实际取证的栏**生成 ＋ 另起 `★ 汇总口径声明：本轮产品树未被哈希取证、也未被 git 取证` ✓ |
| **F3** 问④ 落没落 | 汇总称「产品树只读」（证据说未被取证） | 外部树印两棵树身份 ＋ 「**未被本次哈希取证**」；汇总写 `**产品树本次未被哈希取证**（哈希量的是 SRC 树 ⇒ 本条不写「产品树只读」）` ✓ |

### 17.3 问B：**取收窄准入**，实测到位

我的 faceA 外部树上，新版 M2 那一行：`rc=1（rc⇔桶 对账 ✓（rc=1、桶合计 2））、表内红 1 条 ['断言5']、**认不出 1 条**、**⊘ 未定性 0 条**` ⇒ ★ **那条表外真红回到了「未认出」** ✓（旧版是 `未认出 0、⊘ 2`）。它自己的守卫脚本我实跑：**6/6 rc=0**，含「④问B 反向守卫：旧版（`1b07c85`）必须把表外真红丢进 ⊘（未认出=0）」＋「④问B 正向守卫：新版必须把它归到未认出（⊘＝0）」＋ **P9**（同行两条调用**前缀相同** ⇒ 必须进 ⊘）⇒ **两侧都配了守卫**：一侧守「不许吞不该吞的」，一侧守「不许是死桶」 ✓。

### 17.4 ★★ 第二来源（我给的最便宜判据）：**我造了反向守卫，它行使成功**

**构造**：把产品树拷一份，往 `mech/chain.go` 末尾追加一行语法错（`func brokenRevGuard( {`）⇒ `go test` 必 rc≠0，而 `RED_RE`（要求 `\s+\S+_test\.go:(\d+):`）**一条都匹配不到** ⇒ **桶合计 0**。这正是我预言的静默实例。

**读数（`revguard-raw.log`）**：
```
== 基线：rc=1（rc⇔桶 对账 ✗（rc=1、桶合计 0））、表内红 0 条 []、认不出 0 条、⊘ 未定性 0 条 ==
✗ 基线不是全绿（含 ⊘ 未定性）⇒ 后面的矩阵无意义        ⇒ 脚本 rc=1
```
⇒ ★★★ **当场判红**。而且它把它印进了**每一行**（`rc⇔桶 对账 ✓（rc=…、桶合计 …）`）——**驱动判定的那个数就是印在最显眼处的那个数**（`45fab0e7` 的正解）。**这一笔是本轮最重要的修法，我按自己的判据反向量过，成立。**

### 17.5 ★ F4（新，我在新版上量到的）：**「产品树」这个身份仍是从脚本位置推的**

`ROOT = Path(__file__).resolve().parent.parent`（L75）⇒ `PRODUCT = ROOT / "rios-sim"`（L78）；`judge_file_sha()` 取不到时**返回哨兵串** `（文件不存在）`。

**实测**：真产品树 `rios-sim/mech/chain_test.go` ＝ `55eee15d0a33e8f3`；我的拷贝树 ＝ **同一个** `55eee15d0a33e8f3`（**逐字节相同**）；而脚本冻到 `out/acceptance/pinned/` 后印出的「产品树」＝ `out/acceptance/rios-sim/…` ＝ **不存在**。

⇒ 两个后果：
1. 新版印的 `（与产品树该文件逐字节相同：False）` 是**假读数**（真值 True）——**取不到被静默当成了「内容不同」**（`1b6ee8e9`：两种不同的空压成一个值）；
2. ★ **F1 那条硬条件**（`same_content and not known_ok_content ⇒ 判红`）**在这种运行里永远不可能触发** ⇒ **零信息量的绿，且没有反向守卫**。★ 而**验收方（我）恰恰就是这么用它的**（`CHAIN_INDEP_PIN` 冻结副本）——**判据在它被设计来服务的那个用法里失效。**

**修法三条**：① 产品树身份**不许从脚本位置推**——用 `git rev-parse --show-toplevel` 或由调用方显式传入（环境变量）；② 取不到 ⇒ **大声失败**，不许退化成「内容不同」；③ 拿它做判据的地方要**三态**（相同／不同／**不可得**），并给那条硬条件**配一条反向守卫**（人为构造「内容相同却判不适用」⇒ 必须红）。

### 17.6 后端2 改期望值那件事：**准，不必回退**

它自陈把 `facea_old_vs_new.py` 的第三个期望从 `⊘==2` 改成新读数。我读了那一处：**L72-74 写着「★ 本判据随验收裁定「问B」（准入收窄）**改过一次期望**，如实留痕：旧准入＝「该行候选种类>1 就进 ⊘」⇒ 当时期望 ⊘==2；收窄后它按**自己的**字面前缀归属 ⇒ **未认出**（具名 + rc≠0），⊘ == 0。」** ⇒ 这正是 `d65f6823`（放宽/改动判据必须写下它不能再接受什么并留痕）的做法。**旧版侧（表内 2 条含断言1）一字未动** ✓，「旧准入会把真红丢进 ⊘」这条由守卫④继续用 `1b07c85` 量着 ✓ ⇒ **准**。

### 17.7 我的复验命令与读数

`CHAIN_INDEP_PIN=out/acceptance/pinned/chain_indep.0a32629.py python tools/acceptance_chain_indep_probe.py` ⇒ rc=0：C／D／E／F／P7 **逐段 rc=1**、G rc=1、H（`SystemExit`、不含「矩阵」）、I（13＝5＋8）、J（7 文件哈希无差）。
scope 探针：`control` 它 rc=0（4/4 各行 `rc⇔桶 ✓`、**表内断言（适用）**、7/7 控制组）；`faceA` 它 rc=1（表内 1、**未认出 1**、⊘ 0）；`faceC` 它 rc=1（配平失控仍当场具名）。
它自己跑 `tools/chain_judge_independence.py` ⇒ **rc=0**，基线 `rc=0（对账 ✓（rc=0、桶合计 0））`、4/4 变异行 `rc=1（对账 ✓（rc=1、桶合计 1））`。

### 17.8 未验／不确定

* **F4 的修法未构造验收**（我没有它「按 `git` 定身份」的实现）——只给判据 ＋ 今天的假读数实测。
* 问② 的残余（**同一函数内**配平失控）**仍未构造**；问③ 块注释面 PM 已登记不要求动。
* **根因那笔仍未动完**：本轮动的是它给出的**最便宜的第二来源**（`rc⇔桶`，已行使 ✓）；**强版本（`go test -json` 的失败集合逐条对账）尚未开跑**。

## 十八、复验 `f69fe31`（F4）＋ `1dbab9d`（根因强版本）：F4 双向成立、**「恰红一条」在 `-run` 范围内有了第二来源**；★ 另量到 **F5＝`-run Chain` 边界**，并**撤回**我自己上一轮的一个怀疑

**冻结身份**：`f69fe31` ⇒ `sha256[:16] = 93c7c2bcb758b2a4`／**951 行** ✓；`1dbab9d` ⇒ `84283acaa227f832`／**1167 行** ✓（副本在 `out/acceptance/pinned/`）。读数：`raw-F4old.log`、`raw-F4new.log`、`raw-silentfail-*.log`、`raw-silentnamed-*.log`、`probe-v14.log`、`scope7-control.log`、`na-guards2.log`、`indep-v10.log`。

### 18.1 F4 双向（**我的构造、我的用法**：脚本冻结在 `out/acceptance/pinned/` 后从那里载入）

| | 旧版 `0a32629` | 新版 `f69fe31` |
|---|---|---|
| 产品树身份 | `产品树 = …\out\acceptance\rios-sim`（**不存在**） | `产品树身份 = **可读**（来源：git rev-parse --show-toplevel（<仓库根>））` ✓ |
| 产品树判词文件 | —— | `sha16 = 55eee15d0a33e8f3` —— **与我上一轮独立量到的真产品树逐位相同** ✓ |
| 内容判据 | `（与产品树该文件逐字节相同：False）` ✗ **假读数** | `（与产品树该文件：**相同**；产品树身份取自 git rev-parse…）` ✓ 并**具名来源** |
| rc | **0**（假读数与假绿同时发生） | 0（**在本树上无假**） |

⇒ **F4 修好**；它把我的用法做成了守卫⑤**双向**，我实跑确认 ✓。

### 18.2 ★ 撤回我上一轮的一个怀疑：F1 的「硬条件」**不是**必然绿

我上一轮想过：`same_content and not 适用 ⇒ 判红` 在「同一文件同一方法」下**结构上不可能红**（`fc4263c1`）。**不成立，撤回。** 它的守卫⑥实测：**在内容相同的树上，人为往 `ASSERTIONS` 加一条树里不存在的前缀 ⇒ 判红**（我实跑：`rc=1、含「硬条件被破」=True`）。
⇒ 该条件守的是**判词表自己与产品树失配**这件事，**可被行使**。★ 我把它记在这里而不是删掉——**「我怀疑过、然后被证否」也是这一轮的读数**。

### 18.3 ★★ 根因强版本：「恰红一条」的**运行器侧第二来源**——我用**两个构造**双向量过

它的对账三条（①运行器说失败的每个 Test ⇒ 必须能在它的函数区间里指出 ≥1 条红行；②说通过的 Test 不许有红行归进去；③编译失败 ⇒ 桶合计 0 而 rc≠0 ⇒ 当场具名判红）我逐条在读数里见到。**独立构造如下**（都是我造树、我跑、我读原文）：

**构造 A —— 静默失败的测试，名字不含 `Chain`**
往 `chain_test.go` 追加 `func TestZZSilentRunner(t *testing.T) { t.Fail() }`（**只调 `t.Fail()`，不打任何 `file:line` 红行**）。
* **夹具确实到达了目标**（`97a6a94f` 的纪律）：`SRC` 树 L261 有它、跑完后 `WORK` 树 L261 也有它（`stage()` 从 `SRC` 拷，L347-349）。
* **我自己不过滤地跑 `go test -json ./mech`**：`Action:"run"` → `--- FAIL: TestZZSilentRunner` → `Action:"fail"`、`go test rc=1` ⇒ **它真的在失败**。
* 而**它两边都报**：`运行器判决：失败 Test **0** 个 []／通过 2 个／包判决=pass` ⇒ **rc=0（旧版和新版都是 0）**。
⇒ ★★★ **原因就在它自己的命令行里**（L362）：`go test ./mech/ -run **Chain** -count=1 -v -json` —— 名字不含 `Chain` 的测试**根本不跑**，两个来源都看不见。

**构造 B —— 同一个测试改名为 `TestChainSilentZZ`（含 `Chain`）**
* 新版：`基线：rc=1（rc⇔桶 对账 ✗（rc=1、桶合计 0））、…运行器判决：失败 Test **1** 个 ['TestChainSilentZZ']／通过 2 个／包判决=fail／编译失败=否（对账 ✗）` ⇒ rc=1 **且具名到测试名** ✓。
* 旧版：**rc=1** —— ★ 诚实记：这一条**旧版也红**，但红的是**弱来源**（`rc≠0 且桶合计 0`），**说不出是哪个测试**。⇒ 构造 B 证明的是「运行器侧来源**活着且具名**」，**不是**「旧版对这一点瞎」。
* ★ **我未能隔离的行级场合**：`桶非零 ＋ 多一个失败测试`（那才是运行器侧独有的分辨力）。因为一个**恒失败**的测试会先让**基线**判红、走不到变异行。**记为未构造**，不写「通过」。

### 18.4 ★ F5（新，我在构造 A 上量到的）：**`-run Chain` 钉死了运行范围，而没有任何东西为它发声**

* 归类器扫**全部** `*_test.go`；运行器只跑**名字含 `Chain`** 的测试 ⇒ **两个来源的范围不同**；
* 而更关键的一层：**运行范围 < 树上真实存在的测试集** —— ★ 这是「取证范围 ≥ 运行范围」（`60dc63ca`）的**镜像**：那一次是**取证**窄于运行，这一次是**运行**本身就窄，**两边都看不见**：树上有一个正在失败的测试，没有任何一组读数提到它，`rc=0`。
* ⇒ **这就是同一根因在第二来源上重新长出来的边界**：新增的第二个来源**自带一个未声明的范围**，而窄掉的那部分**没有守卫**。
* **修法**（三条）：① `-run` 改为**现算**（从树上列出全部 `^func Test` 名，拼成 `-run '^(A|B|C)$'`）或**不写** `-run`；② 无论哪种，加一条**范围对账**：`go test -json` 报出的 **Test 总数**必须等于**从树上现算**的 `^func Test` 数，**少一个就具名报出缺了谁**；③ **反向守卫**：树上放一个**名字不匹配 `-run`** 的失败测试 ⇒ **必须判红**（**今天 rc=0 ⇒ 跑它必红**）。

### 18.5 齐备性（都在 `1dbab9d` 上实跑）

* 它的守卫脚本 **12/12 rc=0** ✓（含 ⑤F4 双向、⑥硬条件反向、⑦三态、⑧运行器对账双向）。
* 我的主探针：**rc=0**，C／D／E／F／P7 逐段 rc=1、G rc=1、H（`SystemExit`、不含「矩阵」）、I（13＝5＋8）、J（7 文件哈希无差）⇒ **接口（`main`／`MUTATIONS`／`SRC`／`WORK`）未被破坏** ✓。
* scope `control`：**rc=0**，每行 `rc⇔桶 对账 ✓ ＋ **运行器对账 ✓（失败 Test 恰 1 个）**`、**8/8 控制组**、汇总口径正确 ✓。
* 它自己跑：**rc=0**，同样每行 `运行器对账 ✓（失败 Test 恰 1 个）` ⇒ ★ **「恰红一条」现在确实有两个来源**（运行器侧 1 个 Test ／ 归类器侧 1 条表内红），**根因那笔在 `-run` 范围内闭合** ✓。

### 18.6 未验／不确定

* **`桶非零＋多一个失败测试`** 的行级场合**未构造**（见 18.3）。
* 问② 残余（**同一函数内**配平失控）**仍未构造**。
* F5 的修法**未验收**（我只有今天的 rc=0 实测与必红判据）。
* **`-run` 之外**是否还有别的未声明范围（如 `-count=1` 之外的缓存、其它包）——**未核**。

## 十九、复验 `18b01ce`（F5 修法）：**双向成立**、⑨b 逐字核到、★ 另量到 **F6**；并**作废我自己上一屏的四个时间读数**

**冻结身份**：`18b01ce` ⇒ `sha256[:16] = f0d86746ea7c85cb`／**1234 行** ✓（副本 `out/acceptance/pinned/chain_indep.18b01ce.py`）。读数：`raw-f5-old.log`、`raw-f5-new.log`、`raw-f5-prod.log`、`raw-f5-extraone.log`、`raw-f5-9b.log`、`timing2-*.log`。

### 19.1 F5 双向（**我的构造 A 现场跑**：产品树 ＋ `chain_test.go` 追加 `func TestZZSilentRunner(t *testing.T) { t.Fail() }`）

| 同一棵树 | 旧版 `1dbab9d` | 新版 `18b01ce` |
|---|---|---|
| rc | **0** | **1** ✓ |
| 运行器判决 | `失败 0 个／通过 2 个` | `失败 1 个 ['TestZZSilentRunner']／通过 3 个／跳过 0 个` |
| 输出里提到该测试 | **0 次** | 具名 ✓ |
| 运行范围对账 | 无此概念 | `树上现算 4 个 ⇔ 判决提到 4 个（失败1＋通过3＋跳过0）` ✓ |
| 具名原因 | —— | `运行器说 TestZZSilentRunner 失败（在 chain_test.go），但我把 0 条红行归进它 ⇒ 取证范围 < 运行范围` |

### 19.2 ⑨b：**我自己把缺陷放回去**（不引它的读数）

把新版 `subprocess` 猴补成给 `go test` 加 `-run Chain`（别的什么都不改），在**同一棵构造 A 树**上跑 ⇒ **rc=1**，原文：

```
↳ 运行范围对账：**运行范围 < 树上真实测试集**：树上有 4 个测试，运行器判决里没有 2 个
  ⇒ 缺 ['TestFarmlandGolden', 'TestZZSilentRunner']
```
与它引的那句**逐字一致** ✓（我这边多一句尾注「它们没有被跑…」，它没引，不算不符）。

★★ **并且这一跑量出一层它没说、而我要单独记的事：两条判据的盲区互补。**
| 场合 | `rc⇔桶` | `运行器对账`（归属） | `运行范围对账` | 谁红 |
|---|---|---|---|---|
| 新版 · 范围放开（构造 A） | ✓ | **✗**（说它失败，我却指不出红行） | ✓ | **归属对账** |
| 猴补 · 范围收窄（同一棵树） | ✓（rc=0、桶 0） | **✓**（它没跑 ⇒ 不进判决 ⇒ 无从冲突） | **✗**（缺 2 个） | **范围对账** |
⇒ ★★ **范围放开时，测试跑了，抓人的是归属对账；范围收窄时，测试压根没跑，归属对账结构上发不出声——只有范围对账能喊。** 两条判据**各自不可替代**，因为**一条的盲区恰是另一条的定义域**。

**PM 要的三问，逐条答**：
* **(a) 成立**：「一条正确的判据，可以被上层的范围过滤器整个废掉」——它与我的 F5 是同一纪律的**另一面**。
* **(b) 比「两个方向」更强**：不只是同一纪律的两个方向，而是**两条判据的盲区互补**（上表两侧都是我本轮实测）。★ 所以「抓人的不是新的那条」这句话，**在「范围放开」这个场合里对**，但在「范围收窄」场合**恰好反过来**——两个说法都真，取决于被测对象在不在运行范围里。**这正是「盲区互补」要写下来的原因。**
* **(c) 该单列**，且**单列时必须连「互补」一起写**；只写「抓人的不是新的那条」会被读成「新的那条没用」，那与实测相反。

### 19.3 ★ 作废我自己上一屏的四个时间读数（**我的第 8 次仪器错**）

我曾在仓根 `<仓库根>` 跑 `go test ./mech/ -count=1` 四次，读到 **0.08~0.09s**，并据此写下「两档相同」。**全部作废**：日志原文是
```
go: cannot find main module, but found .git/config in <仓库根>
```
⇒ **模块在 `rios-sim/go.mod`，那四条命令一次都没跑起来（rc=1）** ⇒ 我量的是「命令失败得多快」。★ 抓到它**只因为**第二次我加了 rc 检查（同族 `ab0483bd`：读数没变有两个来源；`45fab0e7`：驱动结论的数必须是那个量）。

**重做（cwd＝`rios-sim`，各自全新 GOCACHE 放在仓外，带 rc）**：

| | 整包 | `-run Chain` |
|---|---|---|
| 冷缓存（各 1 次） | **12.57s**（rc=0，缓存 1051 文件） | **9.28s**（rc=0，缓存 1051 文件） |
| 热缓存 | 1.25s／三次中位 **1.14s**（1.13, 1.16, 1.14） | 1.15s／三次中位 **1.17s**（1.18, 1.14, 1.17） |

⇒ **判**：它给的「整包 1.1s vs `-run Chain` 10.2s」= **一个热缓存、一个冷缓存**（我实测热 1.14／冷 9.28~12.57）⇒ ★ **两套分母不许并列**（`6dd6739c`），而且**配错了方向**（读起来像「范围窄更贵」）。★ 它的**结论仍然正确**：「范围宽没有代价」——我**同分母**实测热缓存两档中位 **1.14 vs 1.17（差 0.03s，在抖动内）**、冷缓存 12.57 vs 9.28（差属编译路径，不与范围成比例）⇒ **没有任何一档显示与范围成比例的成本** ✓。**结论对、证据跨了分母。**

### 19.4 ★ F6（**成因判断已撤回，见 §19.8；以下原文留痕不覆盖**）：原判「同一个计数印在两个文件的行上」——**错**

产品树上的原文（**今天就是如此，rc=0**）：
```
== 归类器覆盖面（现算，包内 *_test.go；判词表只覆盖 chain_test.go）==
   · chain_test.go：判词调用处 13 条、同行多调用 0 行 —— 判词表覆盖
   · farmland_golden_test.go：判词调用处 13 条、同行多调用 0 行 —— 未覆盖（红了 ⇒ 未认出 ⇒ rc=1）
   判词调用处共 13 条：表内能认 5 条、表外 8 条
```
* **同一块里自相矛盾**：两个文件各印 13，而**合计**也印 13 ⇒ 至多有一行是真的。
* 我**自己数**（`\bt\.(Errorf|Fatalf|Fatal|Error)\(`）：`chain_test.go` **13** 条、`farmland_golden_test.go` **14** 条 ⇒ ★ 印在 `farmland_golden_test.go` 那一行上的 `13` 是**另一份文件的数**（它的真值至少不是 13）。
* **影响**：这一节正是读者用来判断**取证范围**的地方 —— 一个按文件分行的表，行上的数不是该文件的，等于**把范围印错**。它今天不进判定（rc=0），所以**只有「看这一节的人」会被它误导**。
* **修法**：分行计数必须**逐文件现算**（并自校验 `Σ 分行 == 汇总`，不等就判红）；配一条反向守卫（把某文件的调用处人为 +1 ⇒ 汇总必须跟着变）。
* ★ 附：我上一屏用 `Select-String` 过滤看这一段，**第一眼读错了**——正是我自己刚立的纪律（「没印」只能用原文复核）在同一个回合里救了我一次。

### 19.5 ④ 单一尺子 ✓

`verdict_from_events` 在**被测脚本里只定义一次**（`chain_judge_independence.py:398`），守卫脚本**import 并调用它**（`out/backend2-b1/na_admission_guards.py:283 new.verdict_from_events(...)`，同文件 L268 有「不另写一份解析器」的注释）⇒ **没有第二把尺子** ✓。

### 19.6 ⑥ `TestFarmlandGolden`：判「失败被藏」与「从未被跑到」是两个状态 ✓，并**更正我自己**

* 读数（真产品树，新版）：`运行器判决：失败 0 个 []／通过 **3** 个／跳过 0 个`、`树上现算 **3** 个 ⇔ 判决提到 **3** 个` ✓ ⇒ 它**通过**，且**今天才第一次出现在读数里**（旧版 `通过 2 个` 就是全部）。
* **这个区分重要**，而且分两层：
  1. **「失败被藏」＝判据能力问题**（有红而读数不提，F1~F5 的旧病）⇒ 修**分类器／判据**；
  2. **「从未被跑到」＝基线定义问题**（连它通过与否都**不知道**）⇒ 修**运行范围**。
  ⇒ 两者**修的地方不同**；而且**「未知」被读成「绿」**这一点比「红被藏」更隐蔽——`基线全绿` 当时的**取证范围是 2/3 个测试**，却按「全部都绿」被读。
* ★★ **我自己的更正**：§18.3 我写「`通过 2 个` = 产品自己的两个测试」——**我当时只扫了 `chain_test.go`**（`^func Test` 两个），**整包还有第三个**（`farmland_golden_test.go:63 TestFarmlandGolden`）⇒ **取证范围窄于结论范围**（`1865d35a`）在我自己身上又发生一次。**留痕，不覆盖。**

### 19.7 未验

* F6 的**成因判断已撤回**（§19.8）；它降级为「口径未声明」的可读性问题，是否处置归 PM。
* `桶非零 ＋ 多一个失败测试` 的行级场合**仍未构造**。
* 问② 残余（同一函数内配平失控）**仍未构造**。
* 冷缓存那一侧我各只跑 1 次（热缓存各 3 次）⇒ **冷缓存的差（12.57 vs 9.28）我不当作稳定读数**，只当作「同一量级」。

### 19.8 ★★ 撤回 §19.4（F6）：**行上的数跟着对象走**——推翻我自己的不是推理，是反向守卫

**怎么推翻的**：我把 §19.4 做成**会红的检查**（`tools/acceptance_coverage_count_probe.py`），夹具是「往某文件里**再加一处**会红调用点」（放进 `if false` ⇒ 永不执行、仍是调用点）。结果：

```
control：chain_test.go 印 13（我自数 13）、farmland_golden_test.go 印 13（我自数 14）
guard  ：chain_test.go 印 13（我自数 13）、farmland_golden_test.go 印 14（我自数 15）
✓ 反向守卫：那行印的数**跟着对象走**（13 → 14）
✓ 两棵树差值**恒定**（−1 / −1）
```
⇒ **它印的就是那个文件的量**；恒定差 −1 ＝ **两把尺子的口径偏移**（我的尺子是整文件 `t.(Errorf|Fatalf|Fatal|Error)` 的 findall，含注释与字符串；它的「判词调用处」谓词我没有取证）。

**我错在哪（两条，都留痕）**：
1. 我把「两个 `13` 并排」读成「**同一个数印了两遍**」⇒ 判成「行上的数不是该文件的」。**错**：那是**巧合**（chain 恰好 13、farmland 恰好也印 13），我却从**形状**下了因。
2. 「Σ(分行) 26 ≠ 合计 13 ⇒ 自相矛盾」——**也错**：那句合计的原话是「判词**调用处**共 13 条：表内能认 5 条、表外 8 条」，**完全可能只覆盖被判词表覆盖的那个文件** ⇒ 这是**口径未声明**，不是矛盾。
3. ★ 而这两条错是**同一个错**：**我先看到了「自相矛盾」的形状，却没先问「这两行的口径是不是同一个」** —— 即 `61d85582`：**两个口径给出两个数时，先问能否变成一个口径**。我拿自己的尺子去比它的数，正是我这一夜反复给别人的判据。

**活下来的（弱，不是零）**：同一块里 `chain_test.go 13`、`farmland_golden_test.go 13`、合计 `13` **并排出现，而三处各自的口径一处都没写出来** ⇒ **读者会把两个 13 读成「两文件各 13」**，而这一节正是读者判断**取证范围**的地方。**处置＝声明口径**（每行的谓词、合计覆盖哪些文件），**不是改数**。★ 是否要做、做多重，**归 PM 裁定**，我不再把它当缺陷计。

**探针的判据也随之改成只判一条**（`out/acceptance/covcount2.log`）：
* 判：**那一行印的数跟不跟着那个文件走** ＋ **两棵树差值是否恒定**；
* **不判**「印数 == 探针自数的数」（两把尺子，`61d85582`）；
* 「Σ 分行 vs 合计」降为 **⚠ 不适用/口径未声明**（`b3aaa881`：第三态要三处同口径）。

**检查我这一轮的顺序，值得记下**：我先写下结论（甚至已发出去），**才**去写判据；**是判据把我按住的**。⇒ 若要提前按住，做法不是「更小心」，而是**在写结论之前先问「这件事的必红判据长什么样」**——答得出来再写结论。

—— 验收会话（RIOS验收与守卫 / session-1a45cfee-9a65-4830-a327-03ac84285bfb）
